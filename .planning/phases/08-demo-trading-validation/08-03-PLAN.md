---
phase: 08-demo-trading-validation
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: []
autonomous: false
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04]
must_haves:
  truths:
    - "Pre-flight check passes with all [OK] on the actual demo environment"
    - "Bot starts successfully and runs through at least one full trading tick"
    - "Operator knows how to start, monitor, and generate reports for the demo run"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete demo trading system is ready for the 2-week validation run.

Purpose: All code hardening (Plan 01) and operational scripts (Plan 02) are complete. This plan verifies the system works end-to-end on the actual demo environment before committing to the 2-week run. The checkpoint ensures the human operator confirms everything is working.

Output: Confirmed demo readiness -- bot runs, pre-flight passes, operator knows the procedures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-trading-validation/08-RESEARCH.md
@.planning/phases/08-demo-trading-validation/08-01-SUMMARY.md
@.planning/phases/08-demo-trading-validation/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and pre-flight check to validate demo readiness</name>
  <files></files>
  <action>
1. Run the full test suite to confirm zero regressions after Plan 01 and 02 changes:
   ```
   python -m pytest tests/ -x --timeout=120 -q
   ```
   All tests must pass. If any fail, diagnose and fix before proceeding.

2. Run the pre-flight check to validate the environment:
   ```
   python scripts/preflight_check.py
   ```
   Document the results. If any checks fail, note them for the operator in the checkpoint.

3. Verify the demo report script runs without errors (even if no trades exist yet):
   ```
   python scripts/demo_report.py --days 1
   ```
   It should produce a valid report showing 0 trades (since the demo hasn't started yet).

4. Verify the PowerShell wrapper is syntactically valid:
   ```
   powershell -Command "Get-Content scripts/run_bot.ps1 | Out-Null; Write-Host 'Script is valid'"
   ```
  </action>
  <verify>
    <automated>python -m pytest tests/ -x --timeout=120 -q</automated>
  </verify>
  <done>Full test suite passes. Pre-flight check runs. Demo report script runs. PowerShell wrapper is valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms demo system readiness</name>
  <what-built>
Complete demo trading system with stability hardening and operational scripts:
- Broker session keepalive (prevents timeout during idle)
- Heartbeat tracking (proves 24+ hour uptime for DEMO-01)
- Default signal reasoning (ensures all trades have reasoning for DEMO-04)
- Daily stats aggregation (populates daily_stats table)
- Pre-flight check script (validates readiness)
- PowerShell restart wrapper (auto-restarts on crash)
- Demo validation report generator (proves DEMO-02, DEMO-03)
  </what-built>
  <how-to-verify>
1. **Pre-flight check:** Run `python scripts/preflight_check.py` and verify all checks show [OK]
   - If any show [FAIL], fix the underlying issue (e.g., set CAPITAL_DEMO=true in .env)

2. **Start the bot:** Run `powershell -ExecutionPolicy Bypass -File scripts/run_bot.ps1 -Preflight`
   - Verify it passes pre-flight and starts the trading loop
   - Let it run for at least 2-3 minutes
   - Check logs/trading.log for:
     - "Broker keepalive: session refreshed" (after 5 min)
     - "Heartbeat recorded" messages
     - No crash or unexpected errors

3. **Stop the bot:** Press Ctrl+C. Verify clean shutdown message.

4. **Check demo report:** Run `python scripts/demo_report.py --days 1`
   - Should show a valid report (0 trades is OK if just started)

5. **Confirm readiness:** Are you ready to start the 2-week demo run?

**For the actual 2-week demo:**
- Start with: `powershell -ExecutionPolicy Bypass -File scripts/run_bot.ps1 -Preflight`
- Monitor with: `python scripts/demo_report.py --days 7` (run periodically)
- If kill switch activates: reset via API `POST /api/v1/risk/kill-switch` with `{"activate": false}`
- Final report: `python scripts/demo_report.py --days 14` after 2 weeks
  </how-to-verify>
  <resume-signal>Type "approved" if demo system is ready, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full test suite passes (260+ tests, 0 regressions)
- Pre-flight check runs successfully on demo environment
- Bot starts and runs without crashes
- Operator confirms readiness for 2-week demo run
</verification>

<success_criteria>
- All automated tests pass
- Pre-flight check shows all [OK]
- Bot runs for at least 2-3 minutes without errors
- Operator has confirmed the system is ready for the demo
- Operator knows the start, monitor, and report procedures
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-trading-validation/08-03-SUMMARY.md`
</output>
