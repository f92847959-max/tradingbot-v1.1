---
phase: 12.7-ai-training
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - ai_engine/training/promotion_gate.py
  - ai_engine/training/pipeline.py
  - scripts/train_models.py
  - tests/test_training_promotion_gate.py
autonomous: true
requirements:
  - AITRAIN2-04
  - AITRAIN2-05
must_haves:
  truths:
    - "Candidate and champion metrics are compared on identical walk-forward windows"
    - "Promotion is blocked unless cost-adjusted PF, drawdown, calibration, confidence-bucket, and non-HOLD gates pass"
    - "Shadow-training artifacts can be written without changing live trading decisions"
  artifacts:
    - path: "ai_engine/training/promotion_gate.py"
      provides: "Promotion gate decision and shadow-training manifest writer"
      exports: ["PromotionGateConfig", "evaluate_training_promotion", "write_promotion_decision", "build_shadow_training_manifest"]
    - path: "tests/test_training_promotion_gate.py"
      provides: "Promotion pass/block and no-production-update tests"
  key_links:
    - from: "ai_engine/training/promotion_gate.py"
      to: "ai_engine/training/walk_forward.py"
      via: "consumes aggregate walk-forward metrics and serialized windows"
      pattern: "generate_training_report"
    - from: "scripts/train_models.py"
      to: "ai_engine/training/promotion_gate.py"
      via: "writes promotion decision after training report, before production pointer changes are allowed"
      pattern: "promotion_decision.json"
---

<objective>
Create the promotion gate and shadow-training workflow for Phase 12.7.

Purpose: A trained model should not be promoted just because training completed. Promotion must be a documented decision based on cost-adjusted walk-forward uplift, calibration, drawdown, and confidence-bucket quality.

Output: promotion gate helper, CLI/report integration, and tests proving failed candidates do not update production pointers.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.7-ai-training/12.7-RESEARCH.md
@.planning/phases/12.7-ai-training/12.7-VALIDATION.md
@.planning/phases/12.7-ai-training/12.7-PATTERNS.md
@ai_engine/training/walk_forward.py
@ai_engine/training/model_versioning.py
@ai_engine/training/pipeline.py
@scripts/train_models.py
</context>

<threat_model>
- T-12.7-05: Promotion passes on raw accuracy while trading quality or calibration degrades
  mitigation: gate uses PF, max drawdown, calibration error, confidence-bucket support, and non-HOLD quality
- T-12.7-06: A failed candidate still changes production artifacts
  mitigation: write decision first and block production pointer update when `approved` is false
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for promotion pass/block decisions</name>
  <files>tests/test_training_promotion_gate.py</files>
  <read_first>
    - .planning/phases/12.7-ai-training/12.7-VALIDATION.md
    - ai_engine/training/walk_forward.py
    - ai_engine/training/model_versioning.py
    - tests/test_walk_forward_calibration.py
  </read_first>
  <action>
    Add `tests/test_training_promotion_gate.py` with tests for:
    - candidate passes when `profit_factor >= 1.20`, `profit_factor >= champion_pf * 1.05`, `max_drawdown <= champion_drawdown * 1.10`, calibration error `<= 0.08`, and each actionable confidence bucket has support `>= 20`
    - candidate is blocked when PF improves but calibration error is `0.12`
    - candidate is blocked when drawdown exceeds champion by more than 10 percent
    - candidate is blocked when non-HOLD trade count is below the configured minimum
    - `write_promotion_decision()` writes `approved`, `reasons`, `gate_metrics`, `candidate_version`, and `champion_version`
    - failed promotion does not call or require `update_production_pointer`
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_training_promotion_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_training_promotion_gate.py exists with at least 5 test functions
    - tests assert literal keys `approved`, `reasons`, `gate_metrics`, `candidate_version`, and `champion_version`
    - at least one test asserts a failed gate returns `approved is False`
  </acceptance_criteria>
  <done>RED tests define promotion gate thresholds and blocked-candidate behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement promotion gate and shadow manifest helpers</name>
  <files>ai_engine/training/promotion_gate.py, tests/test_training_promotion_gate.py</files>
  <read_first>
    - ai_engine/training/walk_forward.py
    - ai_engine/training/model_versioning.py
    - ai_engine/training/causal_training_labels.py
    - tests/test_training_promotion_gate.py
  </read_first>
  <action>
    Create `ai_engine/training/promotion_gate.py` with:
    - `@dataclass PromotionGateConfig`
    - `evaluate_training_promotion(candidate_report: dict[str, Any], champion_report: dict[str, Any], *, config: PromotionGateConfig | None = None) -> dict[str, Any]`
    - `write_promotion_decision(decision: dict[str, Any], path: str | Path) -> str`
    - `build_shadow_training_manifest(decision: dict[str, Any], dataset_manifest: dict[str, Any], split_manifest: dict[str, Any]) -> dict[str, Any]`

    Default `PromotionGateConfig` values:
    - `min_candidate_profit_factor = 1.20`
    - `min_pf_uplift_ratio = 1.05`
    - `max_drawdown_worsening_ratio = 1.10`
    - `max_calibration_error = 0.08`
    - `min_confidence_bucket_support = 20`
    - `min_non_hold_trades = 20`

    The returned decision must contain:
    - `approved: bool`
    - `mode: "shadow_ready"` when approved else `"blocked"`
    - `reasons: list[str]`
    - `gate_metrics: dict[str, Any]`
    - `candidate_version`
    - `champion_version`
    - `created_at`
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_training_promotion_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/training/promotion_gate.py contains `class PromotionGateConfig`
    - ai_engine/training/promotion_gate.py contains `def evaluate_training_promotion(`
    - ai_engine/training/promotion_gate.py contains `def write_promotion_decision(`
    - ai_engine/training/promotion_gate.py contains `def build_shadow_training_manifest(`
    - `python -m pytest tests/test_training_promotion_gate.py -q` exits 0
  </acceptance_criteria>
  <done>The promotion gate helper passes pass/block and artifact-write tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrate promotion decision into training outputs without runtime changes</name>
  <files>ai_engine/training/pipeline.py, scripts/train_models.py, tests/test_training_promotion_gate.py</files>
  <read_first>
    - ai_engine/training/pipeline.py
    - scripts/train_models.py
    - ai_engine/training/model_versioning.py
    - ai_engine/training/promotion_gate.py
    - ai_engine/prediction/ensemble.py
  </read_first>
  <action>
    Update training output flow so that after a training report exists:
    - `TrainingPipeline.run()` adds `results["promotion_decision"]` when champion metrics are supplied, otherwise adds `results["promotion_decision"] = {"approved": false, "mode": "blocked", "reasons": ["champion_report_missing"]}`
    - `scripts/train_models.py` adds optional `--champion-report` path to a JSON report from the current champion
    - if `--champion-report` is provided, load it and call `evaluate_training_promotion(candidate_report=results["training_report"], champion_report=champion_report)`
    - write `promotion_decision.json` into `version_dir` when `version_dir` exists
    - write `shadow_training_manifest.json` into `version_dir` when the promotion decision is approved
    - do not modify `trading/trading_loop.py`, `ai_engine/prediction/ensemble.py`, or live order-management files in this phase

    If existing `update_production_pointer()` is called before the promotion decision is available, move the call behind an explicit `approved` check or add a `promote: bool` argument defaulting to the current behavior only for legacy paths. Failed decisions must not update production pointers.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_training_promotion_gate.py tests/test_walk_forward_calibration.py -q</automated>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m compileall ai_engine/training/promotion_gate.py ai_engine/training/pipeline.py scripts/train_models.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/train_models.py contains `--champion-report`
    - scripts/train_models.py contains `promotion_decision.json`
    - scripts/train_models.py contains `shadow_training_manifest.json`
    - ai_engine/training/pipeline.py contains `promotion_decision`
    - no Phase 12.7 task modifies trading/trading_loop.py
    - `python -m pytest tests/test_training_promotion_gate.py tests/test_walk_forward_calibration.py -q` exits 0
  </acceptance_criteria>
  <done>Training writes a promotion decision and optional shadow manifest without changing live runtime behavior.</done>
</task>

</tasks>

<verification>
Run:
- `python -m pytest tests/test_training_promotion_gate.py tests/test_walk_forward_calibration.py -q`
- `python -m compileall ai_engine/training/promotion_gate.py ai_engine/training/pipeline.py scripts/train_models.py`
- `python -m pytest tests/test_training_data_coverage.py tests/test_causal_training_labels.py tests/test_training_promotion_gate.py tests/test_walk_forward_calibration.py -q`
</verification>

<success_criteria>
- Promotion decision has deterministic pass/block reasons
- Failed candidates cannot update production pointers
- Approved candidates produce shadow-training manifests only
- Candidate/champion comparison requires identical windows and cost-aware metrics
- No live trading runtime behavior changes are introduced
</success_criteria>
