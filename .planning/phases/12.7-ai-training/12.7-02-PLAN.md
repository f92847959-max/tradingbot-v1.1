---
phase: 12.7-ai-training
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ai_engine/training/causal_training_labels.py
  - ai_engine/training/pipeline.py
  - ai_engine/training/label_generator.py
  - tests/test_causal_training_labels.py
autonomous: true
requirements:
  - AITRAIN2-03
  - AITRAIN2-04
must_haves:
  truths:
    - "Existing triple-barrier entry labels remain the source of BUY/HOLD/SELL training targets"
    - "Additional label columns are explicit, documented, and generated without future feature leakage"
    - "Training outputs include split/window metadata sufficient to compare candidate and champion on identical windows"
  artifacts:
    - path: "ai_engine/training/causal_training_labels.py"
      provides: "Causal label frame and label manifest builder"
      exports: ["CausalLabelConfig", "build_causal_label_frame", "build_label_manifest", "build_split_manifest"]
    - path: "tests/test_causal_training_labels.py"
      provides: "Label schema, future-mutation safety, and split manifest tests"
  key_links:
    - from: "ai_engine/training/causal_training_labels.py"
      to: "ai_engine/training/label_generator.py"
      via: "wraps existing `LabelGenerator.generate_labels()` output as `entry_label`"
      pattern: "generate_labels"
    - from: "ai_engine/training/pipeline.py"
      to: "ai_engine/training/walk_forward.py"
      via: "persists serialized window specs and purge gap in training results"
      pattern: "serialize_window_spec"
---

<objective>
Create the causal label and split-manifest layer for Phase 12.7.

Purpose: Training needs explicit labels and split metadata so future promotion decisions can prove they are based on causal targets and identical walk-forward windows.

Output: causal label helper, label/split manifests in training results, and tests proving label schema and leakage boundaries.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.7-ai-training/12.7-RESEARCH.md
@.planning/phases/12.7-ai-training/12.7-VALIDATION.md
@.planning/phases/12.7-ai-training/12.7-PATTERNS.md
@ai_engine/training/label_generator.py
@ai_engine/training/pipeline.py
@ai_engine/training/walk_forward.py
@ai_engine/training/decision_distill_dataset.py
</context>

<threat_model>
- T-12.7-03: Label builder leaks future outcome columns into training features
  mitigation: label helper returns label columns and manifest separately, with future-row mutation tests
- T-12.7-04: Candidate/champion comparison cannot prove window parity
  mitigation: split manifest records window specs, purge gap, feature set, label schema, and sample counts
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for causal label schema and future-mutation safety</name>
  <files>tests/test_causal_training_labels.py</files>
  <read_first>
    - ai_engine/training/label_generator.py
    - tests/test_decision_snapshot_capture.py
    - tests/test_exit_ai_labels.py
    - .planning/phases/12.7-ai-training/12.7-VALIDATION.md
  </read_first>
  <action>
    Add `tests/test_causal_training_labels.py` with tests for:
    - `build_causal_label_frame()` returns columns `entry_label`, `abstain_label`, `exit_label`, `confidence_target`, and `risk_bucket`
    - `entry_label` matches the existing `LabelGenerator.generate_labels()` output for a deterministic OHLC frame
    - mutating future rows after the current row does not change current-row feature columns or metadata used as model inputs
    - `build_label_manifest()` contains `schema_version`, `label_columns`, `entry_label_source`, `max_holding_candles`, `spread_pips`, and `generated_at`
    - `build_split_manifest()` contains `purge_gap`, `window_count`, `feature_names`, `label_columns`, and serialized `windows`
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_causal_training_labels.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_causal_training_labels.py exists with at least 4 test functions
    - tests assert literal column names `entry_label`, `abstain_label`, `confidence_target`, and `risk_bucket`
    - tests contain a future-row mutation assertion
  </acceptance_criteria>
  <done>RED tests define the causal label and split manifest contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement causal training label helper</name>
  <files>ai_engine/training/causal_training_labels.py, tests/test_causal_training_labels.py</files>
  <read_first>
    - ai_engine/training/label_generator.py
    - ai_engine/training/data_coverage.py
    - tests/test_causal_training_labels.py
  </read_first>
  <action>
    Create `ai_engine/training/causal_training_labels.py` with:
    - `@dataclass CausalLabelConfig`
    - `build_causal_label_frame(df: pd.DataFrame, label_generator: LabelGenerator, config: CausalLabelConfig | None = None) -> pd.DataFrame`
    - `build_label_manifest(config: CausalLabelConfig, label_columns: list[str]) -> dict[str, Any]`
    - `build_split_manifest(windows: list[Any], *, purge_gap: int, feature_names: list[str], label_columns: list[str]) -> dict[str, Any]`

    Implementation rules:
    - call existing `label_generator.generate_labels(df)` and store it as `entry_label`
    - set `abstain_label` to `1` where `entry_label == 0`, else `0`
    - set `exit_label` to `"UNAVAILABLE"` unless an existing exit label column is supplied by config
    - derive `confidence_target` from realized label actionability only: `1.0` for non-HOLD labels, `0.0` for HOLD, unless config supplies bucket thresholds
    - derive `risk_bucket` from current-row `atr_14` if present, otherwise `"unknown"`
    - never add future outcome fields to `feature_names`
    - use `serialize_window_spec()` from `walk_forward.py` inside `build_split_manifest()`
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_causal_training_labels.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/training/causal_training_labels.py contains `class CausalLabelConfig`
    - ai_engine/training/causal_training_labels.py contains `def build_causal_label_frame(`
    - ai_engine/training/causal_training_labels.py contains `def build_label_manifest(`
    - ai_engine/training/causal_training_labels.py contains `def build_split_manifest(`
    - `python -m pytest tests/test_causal_training_labels.py -q` exits 0
  </acceptance_criteria>
  <done>The causal label helper passes its schema and leakage tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire label and split manifests into TrainingPipeline results</name>
  <files>ai_engine/training/pipeline.py, ai_engine/training/label_generator.py, tests/test_causal_training_labels.py</files>
  <read_first>
    - ai_engine/training/pipeline.py
    - ai_engine/training/walk_forward.py
    - ai_engine/training/causal_training_labels.py
    - tests/test_walk_forward_calibration.py
  </read_first>
  <action>
    Update `TrainingPipeline.run()` so training results include:
    - `results["label_manifest"]` from `build_label_manifest(...)`
    - `results["split_manifest"]` from `build_split_manifest(...)` after `WalkForwardValidator.calculate_windows(len(X))`
    - `results["row_loss"]["feature_ready"]` after feature creation
    - `results["row_loss"]["label_ready"]` after warmup removal and label generation

    Preserve the existing `df["label"]` behavior for model fitting. Do not switch the model target away from `label` in this plan. Add a lightweight method or property to `LabelGenerator` only if needed to expose existing parameters for the manifest.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_causal_training_labels.py tests/test_walk_forward_calibration.py -q</automated>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m compileall ai_engine/training/causal_training_labels.py ai_engine/training/pipeline.py ai_engine/training/label_generator.py</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/training/pipeline.py contains `label_manifest`
    - ai_engine/training/pipeline.py contains `split_manifest`
    - ai_engine/training/pipeline.py contains `label_ready`
    - `python -m pytest tests/test_causal_training_labels.py tests/test_walk_forward_calibration.py -q` exits 0
  </acceptance_criteria>
  <done>Training results now carry label and split metadata needed for fair promotion decisions.</done>
</task>

</tasks>

<verification>
Run:
- `python -m pytest tests/test_causal_training_labels.py tests/test_walk_forward_calibration.py -q`
- `python -m compileall ai_engine/training/causal_training_labels.py ai_engine/training/pipeline.py ai_engine/training/label_generator.py`
</verification>

<success_criteria>
- Causal label schema exists and is tested
- Existing entry label behavior is preserved
- Future-mutation safety is covered by tests
- Training output contains label and split manifests
- No live trading runtime files are modified
</success_criteria>
