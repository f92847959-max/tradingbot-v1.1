---
phase: 12.7
slug: ai-training
status: verified
verified_at: 2026-04-29
excluded_phases: [6]
---

# Phase 12.7 Verification

Phase 12.7 is verified complete without live trading runtime changes and without executing Phase 6 work.

## Requirement Coverage

| Requirement | Evidence |
|-------------|----------|
| AITRAIN2-01 | `calculate_trainable_span()` raises `DataCoverageError` before fitting when the trainable span is too short. |
| AITRAIN2-02 | `build_row_loss_report()` and dataset manifests preserve received/saved/normalized/feature-ready/label-ready counts and drop reasons. |
| AITRAIN2-03 | `build_causal_label_frame()` creates entry, abstain, exit, confidence, and risk label columns while keeping label columns out of feature names. |
| AITRAIN2-04 | `build_split_manifest()` serializes walk-forward windows and purge gaps; promotion evaluation can compare candidate/champion window identity. |
| AITRAIN2-05 | `evaluate_training_promotion()` gates approval on PF, drawdown, calibration error, confidence-bucket support, and non-HOLD trade count. |

## Automated Evidence

- `python -m pytest tests\test_training_data_coverage.py tests\test_training_data_source.py tests\test_causal_training_labels.py tests\test_training_promotion_gate.py tests\test_walk_forward_calibration.py -q` - 24 passed.
- `python -m compileall ai_engine\training\data_coverage.py ai_engine\training\causal_training_labels.py ai_engine\training\promotion_gate.py ai_engine\training\data_source.py ai_engine\training\pipeline.py ai_engine\training\trainer.py scripts\train_models.py` - passed.

## Phase 6 Exclusion Check

No Phase 6 files were modified as part of this implementation.
