---
phase: 12.7
plan: 02
subsystem: training
requirements: [AITRAIN2-03, AITRAIN2-04]
status: complete
completed: 2026-04-29
---

# Phase 12.7 Plan 02: Causal Labels And Split Manifests Summary

Plan 12.7-02 adds explicit label metadata and split manifests without changing the existing model target behavior.

## Files

- `ai_engine/training/causal_training_labels.py` defines `CausalLabelConfig`, causal label frame generation, label manifests, and split manifests.
- `ai_engine/training/pipeline.py` now records `label_manifest`, `split_manifest`, and `row_loss` in training results and version metadata.
- `ai_engine/training/trainer.py` forwards optional manifest and promotion context into `TrainingPipeline`.
- `tests/test_causal_training_labels.py` covers schema, existing `LabelGenerator` parity, future-row metadata safety, label manifest keys, and split manifest serialization.

## Verification

- `python -m pytest tests\test_training_data_coverage.py tests\test_training_data_source.py tests\test_causal_training_labels.py tests\test_training_promotion_gate.py tests\test_walk_forward_calibration.py -q` - 24 passed.
- `python -m compileall ai_engine\training\data_coverage.py ai_engine\training\causal_training_labels.py ai_engine\training\promotion_gate.py ai_engine\training\data_source.py ai_engine\training\pipeline.py ai_engine\training\trainer.py scripts\train_models.py` - passed.

## Notes

`df["label"]` still uses the existing triple-barrier entry label. The new label frame and manifests document the schema and prevent label columns from entering `feature_names`.
