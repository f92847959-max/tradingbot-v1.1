---
phase: 12.7-ai-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/training/data_coverage.py
  - ai_engine/training/data_source.py
  - scripts/train_models.py
  - tests/test_training_data_coverage.py
autonomous: true
requirements:
  - AITRAIN2-01
  - AITRAIN2-02
must_haves:
  truths:
    - "Training aborts before model fitting when trainable historical span is below the configured minimum"
    - "Training diagnostics separate received, saved, normalized, feature-ready, and label-ready row counts"
    - "Substantial real backfills prefer DB/file historical sources; broker data is treated as incremental/fresh data"
  artifacts:
    - path: "ai_engine/training/data_coverage.py"
      provides: "Data span preflight, row-loss report, and dataset manifest writer"
      exports: ["DataCoverageError", "calculate_trainable_span", "build_row_loss_report", "build_dataset_manifest", "write_dataset_manifest"]
    - path: "tests/test_training_data_coverage.py"
      provides: "Data coverage, row-loss, and manifest tests"
  key_links:
    - from: "scripts/train_models.py"
      to: "ai_engine/training/data_source.py"
      via: "DB/file source selection before broker fallback"
      pattern: "load_auto"
    - from: "scripts/train_models.py"
      to: "ai_engine/training/data_coverage.py"
      via: "preflight before ModelTrainer.train_all"
      pattern: "calculate_trainable_span"
---

<objective>
Create the dataset coverage and row-loss preflight layer for Phase 12.7.

Purpose: Training must fail early with useful diagnostics when the available trainable history is too short, instead of failing late after broker fetch, feature building, or partial artifact work.

Output: `data_coverage.py`, CLI/source integration, and tests proving short-span aborts and row-loss manifest output.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.7-ai-training/12.7-RESEARCH.md
@.planning/phases/12.7-ai-training/12.7-VALIDATION.md
@.planning/phases/12.7-ai-training/12.7-PATTERNS.md
@scripts/train_models.py
@ai_engine/training/data_source.py
@ai_engine/training/data_preparation.py
@market_data/historical.py
@database/repositories/candle_repo.py
</context>

<threat_model>
- T-12.7-01: Short broker history reaches model fitting and creates misleading or wasted training work
  mitigation: preflight `calculate_trainable_span()` runs before feature, label, walk-forward, or model-save steps
- T-12.7-02: Sample collapse is invisible between broker fetch, CSV save, normalization, features, and labels
  mitigation: structured row-loss stages and dataset manifest with stage counts and reasons
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for trainable-span preflight and row-loss telemetry</name>
  <files>tests/test_training_data_coverage.py</files>
  <read_first>
    - .planning/phases/12.7-ai-training/12.7-VALIDATION.md
    - tests/test_training_data_source.py
    - ai_engine/training/data_preparation.py
  </read_first>
  <action>
    Add `tests/test_training_data_coverage.py` with tests for:
    - a 5-minute DataFrame from `2026-04-13T00:00:00Z` to `2026-04-14T20:15:00Z` with `min_months=1` raises `DataCoverageError`
    - the raised message contains `trainable_days`, `minimum_days`, `start_timestamp`, and `end_timestamp`
    - `build_row_loss_report({"received": 1216, "saved": 520, "normalized": 520, "feature_ready": 480, "label_ready": 430})` reports drops for `received_to_saved`, `normalized_to_feature_ready`, and `feature_ready_to_label_ready`
    - `build_dataset_manifest(...)` returns keys `source`, `timeframe`, `received_rows`, `saved_rows`, `feature_ready_rows`, `label_ready_rows`, `trainable_days`, `start_timestamp`, `end_timestamp`, and `dropped_rows`
    - `write_dataset_manifest()` writes JSON that can be loaded back with the same `label_ready_rows`
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_training_data_coverage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_training_data_coverage.py exists with at least 4 test functions
    - tests reference `DataCoverageError`
    - tests assert the literal keys `trainable_days`, `minimum_days`, and `dropped_rows`
  </acceptance_criteria>
  <done>Wave-1 RED tests define the data coverage contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement data coverage helper and manifest writer</name>
  <files>ai_engine/training/data_coverage.py, tests/test_training_data_coverage.py</files>
  <read_first>
    - ai_engine/training/data_source.py
    - ai_engine/training/data_preparation.py
    - tests/test_training_data_coverage.py
  </read_first>
  <action>
    Create `ai_engine/training/data_coverage.py` with:
    - `class DataCoverageError(ValueError)`
    - `calculate_trainable_span(df: pd.DataFrame, min_months: int = 6) -> dict[str, Any]`
    - `build_row_loss_report(stage_counts: dict[str, int]) -> dict[str, Any]`
    - `build_dataset_manifest(source_details: dict[str, Any], df: pd.DataFrame, row_loss: dict[str, Any], *, feature_set: str, label_set: str, min_months: int) -> dict[str, Any]`
    - `write_dataset_manifest(manifest: dict[str, Any], path: str | Path) -> str`

    `calculate_trainable_span()` must:
    - accept either a `DatetimeIndex` or a `timestamp` column
    - sort timestamps and ignore null timestamps
    - compute `trainable_days`, `trainable_hours`, `available_months`, `minimum_days`, `start_timestamp`, `end_timestamp`, and `rows`
    - raise `DataCoverageError` when `trainable_days < min_months * 30.44`

    `build_row_loss_report()` must use ordered stages: `received`, `saved`, `normalized`, `feature_ready`, `label_ready` and include per-transition `from`, `to`, `dropped_rows`, `drop_pct`, and `reason`.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_training_data_coverage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/training/data_coverage.py contains `class DataCoverageError`
    - ai_engine/training/data_coverage.py contains `def calculate_trainable_span(`
    - ai_engine/training/data_coverage.py contains `def build_row_loss_report(`
    - ai_engine/training/data_coverage.py contains `def write_dataset_manifest(`
    - `python -m pytest tests/test_training_data_coverage.py -q` exits 0
  </acceptance_criteria>
  <done>The pure preflight and manifest helpers pass their contract tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrate preflight and DB/file source preference into train_models</name>
  <files>scripts/train_models.py, ai_engine/training/data_source.py, tests/test_training_data_coverage.py</files>
  <read_first>
    - scripts/train_models.py
    - ai_engine/training/data_source.py
    - market_data/historical.py
    - ai_engine/training/data_coverage.py
  </read_first>
  <action>
    Update `scripts/train_models.py` while preserving legacy `--broker`, `--csv`, and `--synthetic` behavior:
    - add `--source` with choices `auto`, `db`, `csv`, `broker`, `synthetic`; when omitted, keep current legacy branch behavior
    - add `--manifest-out` defaulting to `None`
    - add `--allow-short-data` defaulting to `False`
    - for `--source auto`, call `ai_engine.training.data_source.load_auto(timeframe=args.timeframe, count=args.count, file_path=args.csv, with_indicators=False)`
    - for `--source db`, call `load_from_db(timeframe=args.timeframe, count=args.count, with_indicators=False)`
    - for `--source csv`, require `--csv` and call `load_from_file(args.csv, timeframe=args.timeframe, with_indicators=False)`
    - keep `--source broker` mapped to the existing broker fetch path
    - before `trainer.train_all()` or `trainer.train_from_csv()`, call `calculate_trainable_span(df, min_months=args.min_data_months)` unless `--allow-short-data` is set
    - build a row-loss manifest with at least `received`, `saved`, `normalized`, `feature_ready`, and `label_ready`; before plans 12.7-02/03 exist, set `feature_ready` and `label_ready` to `len(df)` at the script preflight boundary
    - when `--manifest-out` is provided, write the manifest via `write_dataset_manifest()`

    Extend `data_source.summarize_dataframe()` to include `duration_days` and `duration_hours` when at least two timestamps are present.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_training_data_coverage.py tests/test_training_data_source.py -q</automated>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m compileall scripts/train_models.py ai_engine/training/data_coverage.py ai_engine/training/data_source.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/train_models.py contains `--source`
    - scripts/train_models.py contains `--manifest-out`
    - scripts/train_models.py contains `--allow-short-data`
    - scripts/train_models.py contains `calculate_trainable_span(`
    - ai_engine/training/data_source.py contains `duration_days`
    - `python -m pytest tests/test_training_data_coverage.py tests/test_training_data_source.py -q` exits 0
  </acceptance_criteria>
  <done>Operator training now fails early on insufficient span and can emit a dataset manifest.</done>
</task>

</tasks>

<verification>
Run:
- `python -m pytest tests/test_training_data_coverage.py tests/test_training_data_source.py -q`
- `python -m compileall scripts/train_models.py ai_engine/training/data_coverage.py ai_engine/training/data_source.py`
</verification>

<success_criteria>
- Short-span data raises before fitting
- Row-loss report includes stage transitions and dropped counts
- Dataset manifest can be written and loaded as JSON
- DB/file historical paths are available from the training CLI without removing legacy flags
- No runtime trading files are modified
</success_criteria>
