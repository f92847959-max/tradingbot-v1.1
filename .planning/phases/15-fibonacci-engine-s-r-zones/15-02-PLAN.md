---
phase: 15-fibonachi
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified: [ai_engine/analysis/trendline_engine.py, ai_engine/analysis/confluence_engine.py, tests/test_trendline_engine.py, tests/test_confluence_engine.py]
autonomous: true
requirements: [TREND-01, CONF-01]
must_haves:
  truths:
    - "TrendlineEngine detects diagonal structure using Hough Transforms"
    - "ConfluenceScorer aggregates multiple level types into weighted zones"
    - "Confluence scoring penalizes isolated levels and rewards overlapping ones"
  artifacts:
    - path: "ai_engine/analysis/trendline_engine.py"
      provides: "Diagonal trendline detection"
    - path: "ai_engine/analysis/confluence_engine.py"
      provides: "Level aggregation and scoring"
  key_links:
    - from: "ai_engine/analysis/confluence_engine.py"
      to: "ai_engine/analysis/sr_engine.py"
      via: "level input"
    - from: "ai_engine/analysis/confluence_engine.py"
      to: "ai_engine/analysis/fibonacci_engine.py"
      via: "level input"
---

<objective>
Enhance market structure analysis by adding diagonal trendline detection and a centralized confluence engine that aggregates all structural levels into high-probability zones.

Purpose: Reduce noise by identifying where multiple technical indicators agree.
Output: Trendline engine and a sophisticated confluence scoring module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15 fibonachi/15-RESEARCH.md
@.planning/phases/15 fibonachi/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TrendlineEngine (trendln)</name>
  <files>ai_engine/analysis/trendline_engine.py, tests/test_trendline_engine.py</files>
  <action>
    - Create `ai_engine/analysis/trendline_engine.py`.
    - Implement `TrendlineEngine.get_lines(df)`:
      1. Use `trendln.get_lines` to detect diagonal support and resistance lines.
      2. Filter for lines with at least 3 touches and reasonable slopes.
      3. Return a list of line objects (slope, intercept, touch_count).
    - Create `tests/test_trendline_engine.py` with a trending price series and verify diagonal lines are detected.
  </action>
  <verify>
    <automated>pytest tests/test_trendline_engine.py</automated>
  </verify>
  <done>TrendlineEngine identifies diagonal structure from price action.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ConfluenceScorer</name>
  <files>ai_engine/analysis/confluence_engine.py, tests/test_confluence_engine.py</files>
  <action>
    - Create `ai_engine/analysis/confluence_engine.py`.
    - Implement `ConfluenceScorer.score_zones(sr_levels, fib_levels, trend_lines, current_price, atr)`:
      1. Group all levels/lines that are within a proximity threshold (e.g., 0.5 * ATR).
      2. Assign weights to level types (e.g., Fib 0.618 = 1.5, S/R Zone = 1.0, Trendline = 0.8).
      3. Calculate a "Confluence Score" for each clustered zone based on the sum of weights.
      4. Filter for zones with scores above a defined threshold.
      5. Return a list of `ConfluenceZone` objects with median price and score.
    - Create `tests/test_confluence_engine.py` verifying that overlapping Fib and S/R levels produce a higher score than isolated ones.
  </action>
  <verify>
    <automated>pytest tests/test_confluence_engine.py</automated>
  </verify>
  <done>ConfluenceScorer identifies high-probability zones where multiple levels agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trendline Fitting | Numerical fitting of lines to price points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Denial of Service | Trendline Fitting | mitigate | Limit the lookback period and maximum number of detected lines to process |
</threat_model>

<verification>
Ensure the confluence logic handles varying ATR (volatility) correctly and doesn't produce thousands of zones in noisy markets.
</verification>

<success_criteria>
- Trendlines are detected with statistical validity (touch counts).
- Confluence zones correctly aggregate disparate technical levels.
- Integration tests pass for multi-level scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/15 fibonachi/15-02-SUMMARY.md`
</output>
