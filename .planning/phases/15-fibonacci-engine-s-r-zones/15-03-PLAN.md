---
phase: 15-fibonachi
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified: [ai_engine/features/feature_engineer.py, trading/signal_generator.py, tests/test_structural_features.py, tests/test_structural_signal_integration.py]
autonomous: true
requirements: [FEAT-15]
must_haves:
  truths:
    - "Structural features (distance to zone, confluence score) are correctly added to the feature set"
    - "SignalGenerator uses confluence zones to adjust prediction confidence"
    - "Bot avoids buying directly into high-score resistance zones"
  artifacts:
    - path: "ai_engine/features/feature_engineer.py"
      provides: "New structural feature group"
  key_links:
    - from: "trading/signal_generator.py"
      to: "ai_engine/analysis/confluence_engine.py"
      via: "confluence zone lookup"
---

<objective>
Integrate the Fibonacci and S/R analysis engines into the core trading system by exposing structural features to ML models and decision-gates to signal generation.

Purpose: Ground AI decisions in market structure "gravity" and structural boundaries.
Output: Integrated structural analysis pipeline within the trading loop.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15 fibonachi/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose Structural Features to ML</name>
  <files>ai_engine/features/feature_engineer.py, tests/test_structural_features.py</files>
  <action>
    - Modify `ai_engine/features/feature_engineer.py`.
    - Add a new group `structural_features`:
      1. `dist_to_nearest_sr`: Normalized distance (by ATR) to the closest S/R cluster center.
      2. `dist_to_nearest_fib`: Normalized distance to the closest Fibonacci level (0.382, 0.5, 0.618).
      3. `nearest_confluence_score`: The score of the nearest confluence zone within 2.0 * ATR.
      4. `is_near_confluence`: Boolean flag if price is within a confluence zone.
    - Create `tests/test_structural_features.py` verifying that features are non-null and logically mapped to price proximity.
  </action>
  <verify>
    <automated>pytest tests/test_structural_features.py</automated>
  </verify>
  <done>ML models now receive market structure context as input features.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Confluence Gates in SignalGenerator</name>
  <files>trading/signal_generator.py, tests/test_structural_signal_integration.py</files>
  <action>
    - Modify `trading/signal_generator.py`.
    - Inject `ConfluenceScorer` into the signal generation logic.
    - Add a structural validation step to `generate_signal()`:
      1. If a BUY signal is generated but price is within 0.5 * ATR of a "Resistance" confluence zone (High Score Zone > Current Price), apply a `STRUCTURAL_RESISTANCE` veto or reduce confidence.
      2. If a SELL signal is generated but price is near "Support" confluence, apply `STRUCTURAL_SUPPORT` veto.
    - Create `tests/test_structural_signal_integration.py` simulating a BUY signal at a resistance zone and verifying it gets downgraded or blocked.
  </action>
  <verify>
    <automated>pytest tests/test_structural_signal_integration.py</automated>
  </verify>
  <done>SignalGenerator respects structural boundaries, preventing trades into high-probability reversal zones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signal Generation | Decision logic consuming structural analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Logic Bypass | SignalGenerator | mitigate | Ensure structural gates cannot be bypassed by ML confidence alone unless a 'breakout' regime is detected |
</threat_model>

<verification>
Perform an end-to-end dry run (or backtest snippet) to ensure that the inclusion of structural features doesn't cause feature leakage or extreme latency in the trading tick.
</verification>

<success_criteria>
- Structural features correctly reflect market context in the feature vector.
- SignalGenerator successfully blocks or modifies signals based on structural confluence.
- All integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/15 fibonachi/15-03-SUMMARY.md`
</output>
