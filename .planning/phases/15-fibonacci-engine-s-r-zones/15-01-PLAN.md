---
phase: 15-fibonachi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, ai_engine/analysis/sr_engine.py, ai_engine/analysis/fibonacci_engine.py, tests/test_sr_engine.py, tests/test_fib_engine.py]
autonomous: true
requirements: [SR-01, FIB-01]
must_haves:
  truths:
    - "SREngine identifies price clusters as zones using MeanShift"
    - "FibonacciEngine anchors retracements to confirmed ZigZag pivots"
    - "Engines handle missing data and low volatility gracefully"
  artifacts:
    - path: "ai_engine/analysis/sr_engine.py"
      provides: "Support/Resistance zone detection"
    - path: "ai_engine/analysis/fibonacci_engine.py"
      provides: "ZigZag and Fibonacci calculation"
  key_links:
    - from: "ai_engine/analysis/sr_engine.py"
      to: "sklearn.cluster.MeanShift"
      via: "clustering algorithm"
    - from: "ai_engine/analysis/fibonacci_engine.py"
      to: "zigzag"
      via: "pivot detection library"
---

<objective>
Build the foundational core for automated market structure analysis by implementing Support/Resistance zone clustering and Fibonacci retracement/extension engines.

Purpose: Provide the AI with structural "gravity" points where price is likely to react.
Output: Two core analysis engines with high unit test coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15 fibonachi/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure & Dependencies</name>
  <files>requirements.txt, ai_engine/analysis/__init__.py</files>
  <action>
    - Add `trendln==0.1.18`, `scikit-learn>=1.0.0`, `scipy>=1.7.0`, and `zigzag==0.3.2` to `requirements.txt`.
    - Create the `ai_engine/analysis/` directory.
    - Create `ai_engine/analysis/__init__.py` to expose the future engines.
  </action>
  <verify>
    <automated>pip install -r requirements.txt && python -c "import trendln, sklearn, scipy, zigzag; print('Success')"</automated>
  </verify>
  <done>Dependencies installed and directory structure ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SREngine (MeanShift)</name>
  <files>ai_engine/analysis/sr_engine.py, tests/test_sr_engine.py</files>
  <action>
    - Create `ai_engine/analysis/sr_engine.py`.
    - Implement `SREngine.find_zones(df, sensitivity=0.1)`:
      1. Use `scipy.signal.argrelextrema` to find local peaks and troughs in OHLC data.
      2. Flatten extrema into a 1D array.
      3. Use `sklearn.cluster.MeanShift` with `estimate_bandwidth` to cluster price points.
      4. Return a list of zones (cluster centers) sorted by price.
    - Create `tests/test_sr_engine.py` with synthetic price data (e.g., price bouncing between 1900 and 2000) and verify clusters are found at 1900 and 2000.
  </action>
  <verify>
    <automated>pytest tests/test_sr_engine.py</automated>
  </verify>
  <done>SREngine identifies horizontal zones accurately from price action.</done>
</task>

<task type="auto">
  <name>Task 3: Implement FibonacciEngine (ZigZag Anchoring)</name>
  <files>ai_engine/analysis/fibonacci_engine.py, tests/test_fib_engine.py</files>
  <action>
    - Create `ai_engine/analysis/fibonacci_engine.py`.
    - Implement `FibonacciEngine.get_levels(df, threshold=0.02)`:
      1. Use `zigzag.zigzag` to find pivot highs and lows.
      2. Identify the most recent **confirmed** swing (last High to last Low or vice versa).
      3. Calculate standard levels: 0.236, 0.382, 0.5, 0.618, 0.786 (Retracements) and 1.272, 1.618 (Extensions).
      4. Return a dictionary of named levels.
    - Create `tests/test_fib_engine.py` using a known price swing (e.g., 100 to 200) and verify the 0.5 level is 150.
  </action>
  <verify>
    <automated>pytest tests/test_fib_engine.py</automated>
  </verify>
  <done>FibonacciEngine anchors levels to pivots without look-ahead bias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data -> Engine | Untrusted OHLCV data from broker/DB is processed by numerical engines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | MeanShift | mitigate | Cap input size (extrema count) and set a timeout for clustering |
| T-15-02 | Information Disclosure | Engine Outputs | accept | Analysis levels are non-sensitive technical data |
</threat_model>

<verification>
Check that both engines return consistent results for the same input and handle Edge cases (flat market, missing candles).
</verification>

<success_criteria>
- SREngine clusters price extrema into logical zones.
- FibonacciEngine calculates retracements/extensions from confirmed pivots.
- Unit tests pass with >90% coverage for the new modules.
</success_criteria>

<output>
After completion, create `.planning/phases/15 fibonachi/15-01-SUMMARY.md`
</output>
