---
phase: 01-code-cleanup-project-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - trading/__init__.py
  - trading/lifecycle.py
  - trading/trading_loop.py
  - trading/signal_generator.py
  - trading/monitors.py
autonomous: true
requirements:
  - CODE-01
  - CODE-03

must_haves:
  truths:
    - "main.py is under 200 lines and TradingSystem is still importable as `from main import TradingSystem`"
    - "Trading logic lives in separate modules under trading/"
    - "No lazy imports inside methods (except the intentional AIPredictor factory pattern)"
    - "Application starts without import errors"
  artifacts:
    - path: "main.py"
      provides: "TradingSystem shell class + entry point"
      min_lines: 50
      contains: "class TradingSystem"
    - path: "trading/__init__.py"
      provides: "Package init"
    - path: "trading/lifecycle.py"
      provides: "SystemLifecycle mixin (__init__, _health_check, start, stop, set_trading_mode)"
      min_lines: 100
    - path: "trading/trading_loop.py"
      provides: "TradingLoop mixin (_trading_loop, _trading_tick, _fetch_mtf_parallel)"
      min_lines: 80
    - path: "trading/signal_generator.py"
      provides: "SignalGenerator mixin (_generate_signal, _save_signal)"
      min_lines: 40
    - path: "trading/monitors.py"
      provides: "Monitor mixin (_daily_cleanup_loop, _position_monitor_loop, _handle_position_closed)"
      min_lines: 40
  key_links:
    - from: "main.py"
      to: "trading/lifecycle.py"
      via: "mixin inheritance"
      pattern: "class TradingSystem.*LifecycleMixin"
    - from: "main.py"
      to: "trading/trading_loop.py"
      via: "mixin inheritance"
      pattern: "class TradingSystem.*TradingLoopMixin"
    - from: "tests/test_lifecycle.py"
      to: "main.py"
      via: "import"
      pattern: "from main import TradingSystem"
    - from: "api/dependencies.py"
      to: "main.py"
      via: "TYPE_CHECKING import"
      pattern: "from main import TradingSystem"
---

<objective>
Refactor main.py from 824-line monolith into a thin shell class using mixin composition, with trading logic extracted into focused modules under a new trading/ package. Simultaneously fix all lazy imports.

Purpose: Satisfy CODE-01 (main.py < 200 lines, trading logic in separate modules) and CODE-03 (no lazy imports inside methods).
Output: main.py as thin TradingSystem shell, 4 new files under trading/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-cleanup-project-foundation/01-RESEARCH.md
@main.py
@api/dependencies.py
@tests/test_lifecycle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lazy imports and extract trading modules</name>
  <files>main.py, trading/__init__.py, trading/lifecycle.py, trading/trading_loop.py, trading/signal_generator.py, trading/monitors.py</files>
  <action>
This is one combined task because fixing imports and extracting modules are intertwined — the extracted modules need the correct top-level imports.

**Step 1: Create trading/ package**

Create `trading/__init__.py` (empty or with a docstring).

**Step 2: Extract lifecycle.py**

Create `trading/lifecycle.py` containing a `LifecycleMixin` class with these methods extracted verbatim from main.py:
- `__init__` (lines ~73-167)
- `_health_check` (lines ~168-203)
- `start` (lines ~204-346)
- `stop` (lines ~347-355)
- `set_trading_mode` (lines ~356-?)

Use `from __future__ import annotations` and `TYPE_CHECKING` guard for any reference to `TradingSystem`:
```python
from __future__ import annotations
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from main import TradingSystem
```

Move ALL lazy imports from these methods to top-level imports in this file. Per research:
- Line 150: `import os` — already at top, remove duplicate
- Line 205: `from market_data.historical import download_historical_candles` — top-level
- Line 216: `from database.repositories.trade_repo import TradeRepository` — top-level
- Line 259: `from notifications.confirmation_handler import ConfirmationHandler` — top-level

**Step 3: Extract trading_loop.py**

Create `trading/trading_loop.py` with `TradingLoopMixin`:
- `_trading_loop` (lines ~356-420)
- `_trading_tick` (lines ~421-520)
- `_fetch_mtf_parallel` (lines ~521-570)

Fix lazy imports:
- Line 358: `from shared.exceptions import classify_error, ErrorCategory` — top-level
- Line 472: `import time as _time_mod` — use `import time` at top-level

**Step 4: Extract signal_generator.py**

Create `trading/signal_generator.py` with `SignalGeneratorMixin`:
- `_generate_signal` (lines ~573-656)
- `_save_signal` (lines ~657-658)

Fix lazy imports:
- Lines 633-634: `from database.models import Signal as SignalModel` and `from database.repositories.signal_repo import SignalRepository` — top-level

IMPORTANT: Keep the `AIPredictor` lazy-load in `_generate_signal` as-is — this is an intentional factory pattern for startup performance. Add a comment: `# Intentional lazy-load: AIPredictor imports heavy ML libs`

**Step 5: Extract monitors.py**

Create `trading/monitors.py` with `MonitorMixin`:
- `_daily_cleanup_loop` (lines ~659-710)
- `_position_monitor_loop` (lines ~711-740)
- `_handle_position_closed` (lines ~741-754)

Fix lazy imports:
- Line 661: `from datetime import timezone as _tz` — add `timezone` to datetime top-level import
- Line 673: `from database.repositories.candle_repo import CandleRepository` — top-level
- Line 717: `from database.repositories.trade_repo import TradeRepository` — top-level

**Step 6: Rewrite main.py as thin shell**

main.py should contain:
1. Top-level imports (stdlib + dotenv + config)
2. `TradingSystem` class inheriting from all 4 mixins:
   ```python
   from trading.lifecycle import LifecycleMixin
   from trading.trading_loop import TradingLoopMixin
   from trading.signal_generator import SignalGeneratorMixin
   from trading.monitors import MonitorMixin

   class TradingSystem(LifecycleMixin, TradingLoopMixin, SignalGeneratorMixin, MonitorMixin):
       """Main trading system orchestrator."""
       pass  # All methods provided by mixins
   ```
3. `main()` entry point function (lines ~755-824), with its lazy imports fixed:
   - Line 775: `import functools` — top-level
   - Line 779: `import time as _time` — `import time` at top-level
   - Line 802: `import contextlib` — top-level
   - Lines 785-786: `import uvicorn` and `from api.app import create_app` — KEEP as conditional (only when `settings.api_enabled`), this is legitimate

4. `if __name__ == "__main__"` block

Target: main.py under 200 lines.

**Critical constraints:**
- `from main import TradingSystem` MUST still work (tests/test_lifecycle.py and api/dependencies.py depend on it)
- Extracted modules must NEVER import from main.py at runtime (use TYPE_CHECKING guard only)
- No logic changes whatsoever — this is a pure structural move
- Run `python -c "from main import TradingSystem; print('OK')"` after to verify no circular imports
  </action>
  <verify>
    <automated>python -c "from main import TradingSystem; print('TradingSystem importable: OK')" && python -c "import ast, sys; tree=ast.parse(open('main.py').read()); lines=len(open('main.py').readlines()); print(f'main.py: {lines} lines'); sys.exit(0 if lines < 200 else 1)"</automated>
  </verify>
  <done>
- main.py is under 200 lines
- TradingSystem is importable via `from main import TradingSystem`
- trading/ package exists with lifecycle.py, trading_loop.py, signal_generator.py, monitors.py
- No lazy imports inside methods (except intentional AIPredictor factory pattern with comment)
- No circular imports
  </done>
</task>

</tasks>

<verification>
- `python -c "from main import TradingSystem"` succeeds (no import errors)
- `wc -l main.py` shows < 200 lines
- `ls trading/` shows __init__.py, lifecycle.py, trading_loop.py, signal_generator.py, monitors.py
- `grep -rn "^\s*import \|^\s*from " main.py trading/` shows all imports at top-level (except AIPredictor and uvicorn/create_app)
</verification>

<success_criteria>
- main.py < 200 lines with TradingSystem as thin mixin-composed shell (CODE-01)
- All lazy imports fixed to top-level, except documented intentional ones (CODE-03)
- `from main import TradingSystem` still works for all consumers
- No logic changes, pure structural refactor
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup-project-foundation/01-02-SUMMARY.md`
</output>
