---
phase: 01-code-cleanup-project-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/training/trainer.py
  - ai_engine/training/trade_filter.py
  - ai_engine/training/pipeline.py
autonomous: true
requirements:
  - CODE-02

must_haves:
  truths:
    - "trainer.py is under 300 lines"
    - "trade_filter.py contains the extracted static helper functions"
    - "pipeline.py contains the extracted train_all pipeline logic"
    - "ModelTrainer.train_all still works as the public API"
  artifacts:
    - path: "ai_engine/training/trainer.py"
      provides: "ModelTrainer class shell with thin train_all wrapper"
      contains: "class ModelTrainer"
      min_lines: 50
    - path: "ai_engine/training/trade_filter.py"
      provides: "Trade filter tuning functions (probs_to_trade_signals, trade_metrics_rank, tune_trade_filter)"
      min_lines: 40
    - path: "ai_engine/training/pipeline.py"
      provides: "TrainingPipeline class with the train_all logic"
      min_lines: 150
  key_links:
    - from: "ai_engine/training/trainer.py"
      to: "ai_engine/training/pipeline.py"
      via: "import and delegation"
      pattern: "from ai_engine.training.pipeline import TrainingPipeline"
    - from: "ai_engine/training/pipeline.py"
      to: "ai_engine/training/trade_filter.py"
      via: "import"
      pattern: "from ai_engine.training.trade_filter import"
---

<objective>
Split trainer.py (627 lines) into three files under 300 lines each: trainer.py (shell), trade_filter.py (static helpers), and pipeline.py (train_all logic).

Purpose: Satisfy CODE-02 (trainer.py split into sub-modules < 300 lines each).
Output: Three focused modules replacing the monolithic trainer.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-cleanup-project-foundation/01-RESEARCH.md
@ai_engine/training/trainer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract trade filter helpers to trade_filter.py</name>
  <files>ai_engine/training/trade_filter.py, ai_engine/training/trainer.py</files>
  <action>
Create `ai_engine/training/trade_filter.py` and move these three static methods from `ModelTrainer` into it as module-level functions:

1. `_probs_to_trade_signals` (lines 114-136) -> `probs_to_trade_signals(y_probs, min_confidence, min_margin) -> np.ndarray`
2. `_trade_metrics_rank` (lines 139-162) -> `trade_metrics_rank(metrics, *, min_trades) -> tuple`
3. `_tune_trade_filter` (lines 164-209) -> `tune_trade_filter(y_val_probs, y_val, ...) -> dict`

Remove the `@staticmethod` decorators and leading underscore since they are now module-level public functions. Keep all logic identical.

In `trainer.py`, remove these three methods from the class and add:
```python
from ai_engine.training.trade_filter import probs_to_trade_signals, trade_metrics_rank, tune_trade_filter
```

Update any calls within `trainer.py` from `self._probs_to_trade_signals(...)` or `ModelTrainer._probs_to_trade_signals(...)` to `probs_to_trade_signals(...)`.

Do NOT change any logic. Pure extraction.
  </action>
  <verify>
    <automated>python -c "from ai_engine.training.trade_filter import probs_to_trade_signals, trade_metrics_rank, tune_trade_filter; print('trade_filter imports OK')" && python -c "from ai_engine.training.trainer import ModelTrainer; print('ModelTrainer still importable')"</automated>
  </verify>
  <done>trade_filter.py exists with 3 exported functions. trainer.py no longer contains these static methods. ModelTrainer still importable.</done>
</task>

<task type="auto">
  <name>Task 2: Extract train_all pipeline to pipeline.py</name>
  <files>ai_engine/training/pipeline.py, ai_engine/training/trainer.py</files>
  <action>
Create `ai_engine/training/pipeline.py` containing a `TrainingPipeline` class that holds the body of `ModelTrainer.train_all()` (lines 211-558, ~350 lines of pipeline logic).

The class should:
1. Accept the same parameters as `train_all()` in its constructor or `run()` method
2. Have access to the ModelTrainer instance (pass as constructor arg) for accessing `self.settings`, `self.data_preparation`, `self.evaluation`, etc.
3. Contain the step-by-step pipeline logic currently in `train_all()`

Fix the step numbering that is out of order (per research Pitfall 3): renumber steps sequentially so log output makes sense.

In `trainer.py`, replace the `train_all()` body with delegation:
```python
def train_all(self, ...):
    """Train all models using walk-forward pipeline."""
    pipeline = TrainingPipeline(self)
    return pipeline.run(...)
```

Also keep `train_from_csv` (lines 560-573) and the `__main__` block (lines 576-627) in trainer.py.

After extraction, verify line counts:
- `trainer.py` should be ~150-200 lines (class init + thin wrappers + train_from_csv + __main__)
- `pipeline.py` should be ~280-350 lines (the pipeline logic)
- `trade_filter.py` should be ~80-100 lines

ALL files must be under 300 lines. If `pipeline.py` exceeds 300, look for natural split points in the pipeline steps and extract a helper, but only if needed.

Do NOT change any pipeline logic. Only restructure and fix step numbering in log messages.
  </action>
  <verify>
    <automated>python -c "from ai_engine.training.trainer import ModelTrainer; print('ModelTrainer OK')" && python -c "from ai_engine.training.pipeline import TrainingPipeline; print('TrainingPipeline OK')" && python -c "import sys; files={'ai_engine/training/trainer.py','ai_engine/training/pipeline.py','ai_engine/training/trade_filter.py'}; [print(f'{f}: {len(open(f).readlines())} lines') for f in files]; ok=all(len(open(f).readlines())<300 for f in files); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>
- trainer.py under 300 lines, contains ModelTrainer shell with thin train_all wrapper
- pipeline.py under 300 lines, contains TrainingPipeline with full pipeline logic
- trade_filter.py under 300 lines, contains trade filter helper functions
- All three files importable without errors
  </done>
</task>

</tasks>

<verification>
- `python -c "from ai_engine.training.trainer import ModelTrainer"` succeeds
- All 3 files under 300 lines each
- `ls ai_engine/training/` shows trade_filter.py and pipeline.py alongside trainer.py
</verification>

<success_criteria>
- trainer.py split into 3 files, all under 300 lines (CODE-02)
- ModelTrainer.train_all() still works as the public API via delegation
- No logic changes, pure structural extraction
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup-project-foundation/01-03-SUMMARY.md`
</output>
