---
phase: 01-code-cleanup-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - ai_engine/features/feature_engineer.py
  - ai_engine/features/indicators.py
  - ai_engine/features/market_features.py
  - ai_engine/features/multi_timeframe.py
  - ai_engine/features/pattern_features.py
  - ai_engine/features/time_features.py
  - ai_engine/models/base_model.py
  - ai_engine/models/lightgbm_model.py
  - ai_engine/models/xgboost_model.py
  - ai_engine/training/trainer.py
  - ai_engine/training/backtester.py
  - ai_engine/training/data_preparation.py
  - ai_engine/training/evaluation.py
  - ai_engine/training/data_source.py
  - ai_engine/training/hyperparameter.py
  - ai_engine/training/label_generator.py
  - config/settings.py
  - monitoring/watchdog_service.py
  - strategy/backtesting/advanced_backtester.py
autonomous: true
requirements:
  - CODE-04
  - CODE-05

must_haves:
  truths:
    - ".gitignore covers .venv, __pycache__, .env, saved_models, logs, *.pkl, data/, *.lock"
    - "All developer-facing comments and log messages in Python files are in English"
    - "German is only retained in user-facing strings (if any)"
  artifacts:
    - path: ".gitignore"
      provides: "Complete gitignore for Python ML project"
      contains: ".venv/"
    - path: "ai_engine/training/trainer.py"
      provides: "English log messages (was German)"
  key_links: []
---

<objective>
Housekeeping: update .gitignore for the Python ML project and translate all German developer-facing comments/log messages to English across ~17 source files.

Purpose: Establish clean project hygiene (CODE-05) and consistent English codebase (CODE-04) before structural refactoring begins.
Output: Updated .gitignore, all source files with English comments/logs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-cleanup-project-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and untrack cached files</name>
  <files>.gitignore</files>
  <action>
Replace the current .gitignore (which only has .env, logs/, __pycache__/, *.pyc, .idea/, .vscode/) with a comprehensive Python ML project .gitignore:

```gitignore
# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# ML Models
ai_engine/saved_models/
*.pkl
*.h5
*.onnx

# Data
data/

# Logs
logs/
*.log
*.lock

# OS
.DS_Store
Thumbs.db
```

After updating .gitignore, check if any now-ignored files are already tracked with `git ls-files --cached`. If so, run `git rm --cached` on them (especially ai_engine/saved_models/*.pkl if present). Do NOT delete the actual files, only untrack them from git.
  </action>
  <verify>
    <automated>python -c "content=open('.gitignore').read(); assert '.venv/' in content; assert 'saved_models' in content; assert '*.pkl' in content; assert 'data/' in content; assert '*.lock' in content; print('OK: .gitignore has all required entries')"</automated>
  </verify>
  <done>.gitignore contains entries for .venv/, __pycache__/, .env, ai_engine/saved_models/, *.pkl, data/, logs/, *.lock. Any previously tracked ignored files are untracked from git.</done>
</task>

<task type="auto">
  <name>Task 2: Translate German comments and log messages to English</name>
  <files>
    ai_engine/features/feature_engineer.py,
    ai_engine/features/indicators.py,
    ai_engine/features/market_features.py,
    ai_engine/features/multi_timeframe.py,
    ai_engine/features/pattern_features.py,
    ai_engine/features/time_features.py,
    ai_engine/models/base_model.py,
    ai_engine/models/lightgbm_model.py,
    ai_engine/models/xgboost_model.py,
    ai_engine/training/trainer.py,
    ai_engine/training/backtester.py,
    ai_engine/training/data_preparation.py,
    ai_engine/training/evaluation.py,
    ai_engine/training/data_source.py,
    ai_engine/training/hyperparameter.py,
    ai_engine/training/label_generator.py,
    config/settings.py,
    monitoring/watchdog_service.py,
    strategy/backtesting/advanced_backtester.py
  </files>
  <action>
For each of the ~17-19 files listed above:

1. Read the file fully.
2. Find all German-language strings in logger calls (logger.info, logger.warning, logger.error, logger.debug) and translate them to English.
3. Find all German code comments (# lines) and translate to English.
4. When translating f-string logger messages, REWRITE the entire log line. Do NOT just swap individual German words. Drop emoji and Unicode box-drawing characters (the codebase has encoding artifacts like "a]o", "a,!" etc.) and replace with plain ASCII.
5. Keep any German in truly user-facing strings if they exist (per CODE-04 spec), but in practice all German content is in logger calls which are developer-facing.
6. Do NOT change any logic, variable names, or function signatures. Only modify string literals and comments.

Example transformation:
- Before: `logger.info(f"ðŸ"¥ Training abgeschlossen fuer {model_name}")`
- After: `logger.info(f"Training completed for {model_name}")`

Use `ruff check --fix` after all changes to ensure import ordering stays correct.
  </action>
  <verify>
    <automated>python -c "import subprocess; result=subprocess.run(['grep','-rn','--include=*.py','-P','[\\x{00c0}-\\x{024f}]','ai_engine/','config/','monitoring/','strategy/backtesting/'],capture_output=True,text=True); lines=[l for l in result.stdout.splitlines() if '.venv' not in l and '__pycache__' not in l]; print(f'German-range chars found in {len(lines)} lines'); assert len(lines)==0, f'Still have German content:\\n' + '\\n'.join(lines[:10])"</automated>
  </verify>
  <done>All logger messages and code comments in the ~17 source files are in English. No German-language developer-facing strings remain. No logic changes made.</done>
</task>

</tasks>

<verification>
- .gitignore grep for all required entries passes
- No German-range Unicode characters in developer-facing strings across ai_engine/, config/, monitoring/, strategy/backtesting/
- `git diff --stat` shows only .gitignore and the ~17 translated files changed
</verification>

<success_criteria>
- .gitignore covers all required patterns (CODE-05)
- All developer-facing comments and log messages are in English (CODE-04)
- No functional code changes, only strings and comments modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup-project-foundation/01-01-SUMMARY.md`
</output>
