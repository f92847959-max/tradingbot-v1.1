---
phase: 01-code-cleanup-project-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified: []
autonomous: true
requirements:
  - CODE-06

must_haves:
  truths:
    - "All 171 previously passing tests still pass"
    - "No new test failures introduced by refactoring"
    - "The 7 pre-existing failures and 8 collection errors remain unchanged"
  artifacts: []
  key_links:
    - from: "tests/"
      to: "main.py"
      via: "import"
      pattern: "from main import TradingSystem"
    - from: "tests/"
      to: "ai_engine/training/trainer.py"
      via: "import"
      pattern: "from ai_engine.training"
---

<objective>
Run the full test suite to verify all 171 previously passing tests still pass after the refactoring in Plans 01-03. Fix any regressions.

Purpose: Satisfy CODE-06 (all existing tests pass after refactoring). The baseline is 171 passing, 7 pre-existing failures, 8 collection errors.
Output: Verified test suite with no regressions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-cleanup-project-foundation/01-RESEARCH.md
@.planning/phases/01-code-cleanup-project-foundation/01-01-SUMMARY.md
@.planning/phases/01-code-cleanup-project-foundation/01-02-SUMMARY.md
@.planning/phases/01-code-cleanup-project-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any regressions</name>
  <files></files>
  <action>
Run the full test suite:
```bash
pytest tests/ -v --tb=short 2>&1
```

**Expected baseline (pre-refactor):**
- 171 tests PASS
- 7 tests FAIL (pre-existing: 6 in test_indicators.py for missing `atr` column, 1 in test_risk_integration.py for kill switch behavior)
- 8 test files fail to collect (missing modules: scripts.train_ai, scripts.monitor_training_chart, ai_engine.prediction.prompt_builder)

**Verification steps:**

1. Count passing tests. Must be >= 171.
2. Count failing tests. The 7 pre-existing failures should remain. Any NEW failures are regressions that must be fixed.
3. Count collection errors. The 8 pre-existing collection errors should remain unchanged.

**If regressions found:**

Most likely causes after this phase's refactoring:
- Import path changes: `from main import X` broken by the mixin refactor -> fix the import path or re-export
- Missing module: a test imports something that moved -> add re-export or update import
- Circular import: extracted module imports from main.py -> fix with TYPE_CHECKING guard

For each regression:
1. Read the traceback
2. Identify whether it's caused by Plans 01-03 changes
3. Fix the issue (update import, add re-export, fix path)
4. Re-run the failing test to confirm fix
5. Re-run full suite to confirm no cascading issues

**If no regressions:** Simply document the test results.

Do NOT fix the 7 pre-existing failures or 8 collection errors — those are out of scope.
  </action>
  <verify>
    <automated>pytest tests/ --tb=line -q 2>&1 | tail -5</automated>
  </verify>
  <done>
- 171+ tests pass (no regression from refactoring)
- Pre-existing 7 failures unchanged
- Pre-existing 8 collection errors unchanged
- If any regressions were found, they are fixed and documented
  </done>
</task>

</tasks>

<verification>
- `pytest tests/ -q` shows 171+ passed, 7 failed, 8 errors (matching pre-refactor baseline)
- No new failures introduced by Phase 1 changes
</verification>

<success_criteria>
- All 171 previously passing tests still pass (CODE-06)
- No new test failures or collection errors introduced
- Refactoring is verified safe
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup-project-foundation/01-04-SUMMARY.md`
</output>
