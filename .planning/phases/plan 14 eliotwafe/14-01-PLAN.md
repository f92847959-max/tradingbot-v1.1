---
phase: 14-elliott-wave-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "ai_engine/elliott_wave/models.py",
  "ai_engine/elliott_wave/detector.py",
  "ai_engine/elliott_wave/rules.py",
  "tests/test_elliott_wave.py"
]
autonomous: true
requirements: [EWT-01]
must_haves:
  truths:
    - "System detects local price extrema (peaks and valleys) using scipy."
    - "Elliott Wave Oscillator (EWO) is calculated correctly."
    - "Impulse (1-5) and Zigzag (A-B-C) patterns are identified based on core rules."
  artifacts:
    - path: "ai_engine/elliott_wave/detector.py"
      provides: "Extrema detection and EWO calculation"
    - path: "ai_engine/elliott_wave/rules.py"
      provides: "Validation logic for EWT rules"
    - path: "tests/test_elliott_wave.py"
      provides: "Unit tests for wave detection"
  key_links:
    - from: "ai_engine/elliott_wave/detector.py"
      to: "scipy.signal.find_peaks"
      via: "Peak detection"
    - from: "ai_engine/elliott_wave/detector.py"
      to: "pandas_ta"
      via: "EWO calculation"
---

<objective>
Implement the core Elliott Wave detection engine, focusing on extrema detection, momentum filtering (EWO), and basic pattern recognition (Impulse and Zigzag).

Purpose: Provide the structural foundation for identifying market cycles.
Output: Wave detection module with rule-based validation for 1-5 and A-B-C patterns.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/plan 14 eliotwafe/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Data Models and Extrema Detection</name>
  <files>ai_engine/elliott_wave/models.py, ai_engine/elliott_wave/detector.py</files>
  <action>
    1. Create `ai_engine/elliott_wave/models.py` to define `WavePoint`, `Wave`, and `WavePattern` data classes.
    2. Create `ai_engine/elliott_wave/detector.py`.
    3. Implement `find_extrema(prices, atr, distance=10, prominence_factor=1.5)` using `scipy.signal.find_peaks`. Ensure it handles both peaks and valleys (invert prices for valleys).
    4. Implement `calculate_ewo(df, fast=5, slow=35)` using `pandas_ta.ewo`.
    5. Implement `WaveDetector` class that uses these functions to produce a list of "meaningful" swings from OHLCV data.
  </action>
  <verify>
    <automated>python -c "from ai_engine.elliott_wave.detector import calculate_ewo; import pandas as pd; import pandas_ta as ta; df = pd.DataFrame({'close': [1,2,3,4,5]*10}); ewo = calculate_ewo(df); print('OK' if not ewo.empty else 'FAIL')"</automated>
  </verify>
  <done>Extrema detection and EWO calculation are functional and return expected data structures.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Core Wave Rules (Impulse &amp; Zigzag)</name>
  <files>ai_engine/elliott_wave/rules.py</files>
  <action>
    1. Create `ai_engine/elliott_wave/rules.py`.
    2. Implement a `BaseRule` class.
    3. Implement `ImpulseRule` (1-5):
       - Rule 1: Wave 2 cannot retrace more than 100% of Wave 1.
       - Rule 2: Wave 3 cannot be the shortest of the three motive waves (1, 3, 5).
       - Rule 3: Wave 4 cannot overlap the price territory of Wave 1.
    4. Implement `ZigzagRule` (A-B-C):
       - B < 100% of A.
       - C usually goes beyond A.
    5. Implement a `RuleEngine` that validates sequences of price extrema against these rules.
  </action>
  <verify>
    <automated>python -m pytest tests/test_elliott_wave.py</automated>
  </verify>
  <done>Rule engine correctly validates 1-5 Impulse and A-B-C Zigzag patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit Testing for Wave Detection</name>
  <files>tests/test_elliott_wave.py</files>
  <behavior>
    - Test `find_extrema` with synthetic sine wave data.
    - Test `ImpulseRule` with valid and invalid wave sequences.
    - Test `ZigzagRule` with valid and invalid wave sequences.
    - Test `WaveDetector` end-to-end with a known historical gold price snippet.
  </behavior>
  <action>
    Create `tests/test_elliott_wave.py` and implement the tests described in the behavior section. Use `pytest`.
  </action>
  <verify>
    <automated>pytest tests/test_elliott_wave.py</automated>
  </verify>
  <done>All unit tests pass, confirming the reliability of the core detection engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data -> Engine | OHLCV data must be validated for NaNs and outliers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | OHLCV Input | mitigate | Validate input data range and type in `find_extrema`. |
| T-14-02 | Denial of Service | find_peaks | mitigate | Set reasonable limits on input data length to prevent excessive peak calculation time. |
</threat_model>

<verification>
Run `pytest tests/test_elliott_wave.py` to verify the detection logic.
</verification>

<success_criteria>
- Extrema detection correctly identifies major swings.
- EWO is calculated and visible in dataframes.
- Core rules (1-5, ABC) are correctly applied to extrema sequences.
</success_criteria>

<output>
After completion, create `.planning/phases/plan 14 eliotwafe/14-01-SUMMARY.md`
</output>
