---
phase: 14-elliott-wave-integration
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified: [
  "ai_engine/features/elliott_wave_features.py",
  "ai_engine/features/feature_engineer.py",
  "ai_engine/mirofish_client.py",
  "trading/signal_generator.py"
]
autonomous: true
requirements: [EWT-03, EWT-04]
must_haves:
  truths:
    - "Elliott Wave state is available as ML features for model training."
    - "MiroFish agents receive structural market context in their prompts."
    - "Trading signals are filtered or vetoed based on Elliott Wave count."
  artifacts:
    - path: "ai_engine/features/elliott_wave_features.py"
      provides: "ML feature extraction from wave state"
    - path: "ai_engine/mirofish_client.py"
      provides: "Wave context injection for MiroFish"
  key_links:
    - from: "ai_engine/features/feature_engineer.py"
      to: "ai_engine/features/elliott_wave_features.py"
      via: "Feature integration"
    - from: "trading/signal_generator.py"
      to: "ai_engine.elliott_wave"
      via: "Signal filtering"
---

<objective>
Integrate the Elliott Wave engine into the broader trading system. Expose wave states as ML features, provide structural context to MiroFish agents, and use wave counts to filter trading signals.

Purpose: Leverage structural market context for better signal accuracy and AI reasoning.
Output: Integrated ML features and signal filters based on EWT.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/plan 14 eliotwafe/14-RESEARCH.md
@.planning/phases/plan 14 eliotwafe/14-02-SUMMARY.md
@ai_engine/features/feature_engineer.py
@ai_engine/mirofish_client.py
@trading/signal_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Elliott Wave ML Features</name>
  <files>ai_engine/features/elliott_wave_features.py, ai_engine/features/feature_engineer.py</files>
  <action>
    1. Create `ai_engine/features/elliott_wave_features.py`.
    2. Implement `extract_ew_features(ohlcv_df)`:
       - Run `WaveDetector`.
       - Extract: `ew_pattern_type` (int), `ew_wave_number` (int), `ew_completion` (float), `ew_is_motive` (bool).
       - Ensure features are computed without lookahead bias.
    3. Register these features in `ai_engine/features/feature_engineer.py`.
  </action>
  <verify>
    <automated>python -c "from ai_engine.features.feature_engineer import FeatureEngineer; fe = FeatureEngineer(); print('ew_pattern_type' in fe.get_feature_names())"</automated>
  </verify>
  <done>Elliott Wave features are correctly extracted and integrated into the feature engineering pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Provide Structural Context to MiroFish</name>
  <files>ai_engine/mirofish_client.py</files>
  <action>
    1. Update `ai_engine/mirofish_client.py`.
    2. Modify the prompt generation logic to include a "Structural Context" section.
    3. Use `WaveDetector` to get the current primary count and its targets.
    4. Format the context as text: "Gold is in Wave 3 of a Bullish Impulse. Target 161.8% at 2345.00."
  </action>
  <verify>
    <automated>python -c "from ai_engine.mirofish_client import MiroFishClient; client = MiroFishClient(); print('Structural Context' in client._prepare_prompt({}))"</automated>
  </verify>
  <done>MiroFish agents now receive detailed Elliott Wave context for their simulations.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Wave Counts into Signal Generator</name>
  <files>trading/signal_generator.py</files>
  <action>
    1. Update `trading/signal_generator.py`.
    2. Import the Elliott Wave engine.
    3. Implement a filter/veto mechanism:
       - Veto BUY signals if the primary count suggests an imminent Wave 4 or Wave B correction.
       - Support BUY signals if the count suggests we are starting Wave 3 or Wave 5.
    4. Ensure the EW filter is optional and governable (via config).
  </action>
  <verify>
    <automated>python -m pytest tests/test_signal_generator_ewt.py</automated>
  </verify>
  <done>Signal generator uses Elliott Wave counts as an additional layer of verification for trades.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EW Features -> ML Model | Feature values must be scaled or normalized if required by the model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Spoofing | MiroFish Prompt | mitigate | Sanitize wave context string before injection into the prompt. |
| T-14-06 | Denial of Service | Feature Engineering | mitigate | Set a timeout for the WaveDetector run during real-time feature extraction. |
</threat_model>

<verification>
Run a backtest using the new EW features and verify that the bot's decisions align with structural wave context.
</verification>

<success_criteria>
- EW features appear in training datasets.
- MiroFish logs show wave context being passed to agents.
- Trading signals reflect structural market awareness.
</success_criteria>

<output>
After completion, create `.planning/phases/plan 14 eliotwafe/14-03-SUMMARY.md`
</output>
