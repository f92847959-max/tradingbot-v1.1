---
phase: 14.1-dow-theory-trend-confirmation
plan: 01
type: execute
wave: 1
depends_on: ["14-03"]
files_modified: [
  "ai_engine/dow_theory/__init__.py",
  "ai_engine/dow_theory/trend.py",
  "ai_engine/features/dow_theory_features.py",
  "ai_engine/features/feature_engineer.py",
  "trading/signal_generator.py",
  "config/settings.py",
  "tests/test_dow_theory.py",
  "tests/test_signal_generator_dow.py"
]
requirements: [DOW-01, DOW-02]
must_haves:
  truths:
    - "Dow Theory primary trend (UP/DOWN/RANGE) is classified from swing high/low structure (HH/HL vs LH/LL)."
    - "Volume/order-flow confirmation and multi-timeframe confirmation are computed for the trend."
    - "Dow Theory's verdict confirms or contradicts the Elliott Wave primary count and is exposed as a feature (dow_confirms_ew)."
    - "Dow trend state is available as ML features and as an opt-in (default-off) signal layer."
  artifacts:
    - path: "ai_engine/dow_theory/trend.py"
      provides: "Dow trend classification + EW-agreement + confirmation logic (pure)"
    - path: "ai_engine/features/dow_theory_features.py"
      provides: "Causal Dow ML feature extraction"
  key_links:
    - from: "ai_engine/features/feature_engineer.py"
      to: "ai_engine/features/dow_theory_features.py"
      via: "Feature integration"
    - from: "ai_engine/dow_theory/trend.py"
      to: "ai_engine.elliott_wave"
      via: "EW master-count agreement"
    - from: "trading/signal_generator.py"
      to: "ai_engine.dow_theory"
      via: "Opt-in trend filter"
---

<objective>
Add a Dow Theory trend-confirmation layer that classifies the prevailing trend from price
structure and confirms or contradicts the Elliott Wave master count. Expose it as causal ML
features and an opt-in signal filter, mirroring the Phase 14-03 EW integration.

Purpose: a second, independent structural engine that corroborates (or flags) the EW count.
Output: a Dow trend classifier, 4 ML features, and a governable BUY filter.
</objective>

<context>
@.planning/phases/14.1-dow-theory-trend-confirmation/14.1-CONTEXT.md
@ai_engine/features/elliott_wave_features.py
@trading/signal_generator.py
@ai_engine/features/feature_engineer.py
@config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dow Theory trend classifier</name>
  <files>ai_engine/dow_theory/__init__.py, ai_engine/dow_theory/trend.py</files>
  <action>
    1. Create the `ai_engine/dow_theory/` package.
    2. In `trend.py` implement pure, never-raising logic:
       - `DowTrend` enum/constants: DOWN, RANGE, UP.
       - `classify_primary_trend(swings) -> DowTrend`: UP when recent swing highs rising AND lows rising (HH+HL); DOWN when LH+LL; else RANGE. Operate on the last few pivots from `WaveDetector.detect_swings`.
       - `confirm_with_ew(dow_trend, ew_is_motive, ew_wave_number, ew_direction) -> int`: +1 confirms, -1 contradicts, 0 neutral (derive EW bias from the primary count).
       - `volume_confirms(df, dow_trend) -> bool`: heavier participation on trend legs, using `flow_*` columns if present else `volume`; graceful when absent.
       - `mtf_confirms(primary_trend, higher_tf_trend) -> bool`.
    3. Keep files < 500 lines; no heavy imports at module top (lazy-import the EW detector).
  </action>
  <verify>
    <automated>python -m pytest tests/test_dow_theory.py -q</automated>
  </verify>
  <done>Dow trend is classified from structure and an EW-agreement verdict is produced, all branch-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Dow ML features + FeatureEngineer registration</name>
  <files>ai_engine/features/dow_theory_features.py, ai_engine/features/feature_engineer.py</files>
  <action>
    1. Create `dow_theory_features.py` mirroring `elliott_wave_features.py` (window cap, extract-once-broadcast, never raises):
       - `extract_dow_features(ohlcv_df, mtf=None) -> dict` with `dow_trend` (0/1/2), `dow_confirms_ew` (0/1), `dow_volume_confirmed` (0/1), `dow_mtf_confirmed` (0/1). Reuse `extract_ew_features` for the EW side of the agreement check.
       - `DowTheoryFeatures` group with `get_feature_names()` + `calculate(df)`.
    2. Register the group in `FeatureEngineer`.
  </action>
  <verify>
    <automated>python -c "from ai_engine.features.feature_engineer import FeatureEngineer; print('dow_trend' in FeatureEngineer().get_feature_names())"</automated>
  </verify>
  <done>Dow features appear in the FeatureEngineer feature set.</done>
</task>

<task type="auto">
  <name>Task 3: Opt-in Dow signal filter + settings</name>
  <files>trading/signal_generator.py, config/settings.py, tests/test_signal_generator_dow.py</files>
  <action>
    1. In `config/settings.py` add `dow_filter_enabled: bool = False` (opt-in, graceful fallback).
    2. In `trading/signal_generator.py` add `apply_dow_filter(signal, candle_df, *, enabled, ...)` mirroring `apply_ew_filter`: BUY-only; veto (BUY->HOLD) when the Dow primary trend contradicts the BUY (DOWN trend), annotate when it confirms; never mutate the input. Wire it into `_generate_signal` after the EW filter, gated by `settings.dow_filter_enabled`.
    3. Write `tests/test_dow_theory.py` (classifier + agreement branches) and `tests/test_signal_generator_dow.py` (gating/veto/passthrough, monkeypatching the detection shell).
  </action>
  <verify>
    <automated>python -m pytest tests/test_signal_generator_dow.py tests/test_dow_theory.py -q</automated>
  </verify>
  <done>Live BUY signals can be filtered by Dow trend when enabled; default-off path is unchanged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14.1-01 | DoS | Dow feature extraction | mitigate | Cap detector input window (reuse EW_MAX_WINDOW pattern). |
| T-14.1-02 | Tampering | Trend thresholds | accept | Thresholds are module constants; no caller override. |
</threat_model>

<verification>
Run the new Dow tests plus the EW suite (no regression); confirm features register and the
opt-in filter is inert by default.
</verification>

<success_criteria>
- Dow primary trend classified from HH/HL/LH/LL structure.
- Volume/order-flow + multi-timeframe confirmation computed.
- Confirms/contradicts the EW primary count, exposed as `dow_confirms_ew`.
- ML features registered; opt-in signal filter (default off) wired in.
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-dow-theory-trend-confirmation/14.1-01-SUMMARY.md`.
</output>
