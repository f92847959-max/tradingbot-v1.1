---
phase: 12.1-ai-confidence-calibration-decision-governance
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - database/models.py
  - database/repositories/governance_repo.py
  - database/repositories/__init__.py
  - trading/trading_loop.py
  - ai_engine/governance/promotion.py
  - monitoring/model_monitor.py
  - tests/test_decision_logging.py
  - tests/test_champion_challenger.py
autonomous: true
requirements:
  - CONF-04
  - CONF-05
must_haves:
  truths:
    - "HOLD, blocked, weakened, vetoed, and passed decisions are persisted with audit details"
    - "Champion/challenger promotion compares calibrated metrics, drawdown impact, and minimum trade count"
    - "Retraining triggers are based on calibrated degradation evidence, not raw confidence alone"
    - "Decision logs never store secrets or unvalidated external paths"
  artifacts:
    - path: "database/models.py"
      provides: "Governance decision audit ORM model"
      contains: "class GovernanceDecision"
    - path: "database/repositories/governance_repo.py"
      provides: "Persistence helpers for governance audit rows"
      exports: ["GovernanceDecisionRepository"]
    - path: "ai_engine/governance/promotion.py"
      provides: "Champion/challenger comparison and retraining trigger logic"
      exports: ["evaluate_candidate_promotion", "evaluate_retraining_trigger"]
    - path: "tests/test_decision_logging.py"
      provides: "Persistence coverage for pass/block/hold decisions"
    - path: "tests/test_champion_challenger.py"
      provides: "Promotion and retraining trigger coverage"
  key_links:
    - from: "database/models.py"
      to: "database/models.py::Signal"
      via: "reuse JSON_COMPAT and indexed timestamp patterns"
      pattern: "JSON_COMPAT"
    - from: "trading/trading_loop.py"
      to: "ai_engine/governance/types.py"
      via: "persists the returned DecisionAudit before early-return HOLD exits"
      pattern: "DecisionAudit"
---

<objective>
Add persistence and monitoring around governance decisions: database audit logging for all outcomes, champion/challenger comparison logic, and calibrated retraining triggers wired into the runtime/monitoring path.

Purpose: CONF-04 and CONF-05 are the operational layer that turns calibrated governance from a local algorithm into something auditable and safe to evolve.

Output: governance audit persistence, promotion/retraining helpers, and focused tests for decision logging and calibrated challenger evaluation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-VALIDATION.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-PATTERNS.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-01-PLAN.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-02-PLAN.md

<interfaces>
From database/models.py:
```python
JSON_COMPAT = JSON().with_variant(JSONB, "postgresql")
class Signal(Base)
class RiskEvent(Base)
```

From monitoring/model_monitor.py:
```python
class ModelMonitor
```
</interfaces>
</context>

<threat_model>
- T-12.1-05: Secrets or unsafe paths leak into decision logs or artifact references
  mitigation: store only audit-safe fields, validate artifact paths under saved model directories, never serialize env values
- T-12.1-06: Weak challenger or noisy calibration drift triggers unsafe promotion/retraining
  mitigation: compare calibrated metrics, drawdown, and trade-count gates before promotion or retraining
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED persistence and monitoring tests</name>
  <files>tests/test_decision_logging.py, tests/test_champion_challenger.py</files>
  <read_first>
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-VALIDATION.md
    - database/models.py
    - monitoring/model_monitor.py
  </read_first>
  <action>
    Add focused tests for:
    - persistence of HOLD/rejected/pass outcomes with gate reason, conflict ratio, artifact version, and threshold source
    - safe retrieval/query patterns for recent governance decisions
    - candidate promotion requiring calibrated metric non-regression plus minimum trade count
    - retraining trigger firing only on sustained calibrated degradation evidence
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_logging.py tests/test_champion_challenger.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_decision_logging.py exists with at least 3 test functions
    - tests/test_champion_challenger.py exists with at least 3 test functions
    - Tests assert artifact version, threshold source, and gate reasons explicitly
  </acceptance_criteria>
  <done>Operational tests define the persistence and promotion contracts for CONF-04/CONF-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement governance audit persistence and trading-loop integration</name>
  <files>database/models.py, database/repositories/governance_repo.py, database/repositories/__init__.py, trading/trading_loop.py, tests/test_decision_logging.py</files>
  <read_first>
    - database/models.py
    - database/repositories/base.py
    - trading/trading_loop.py
    - ai_engine/governance/types.py
  </read_first>
  <action>
    Add a governance audit ORM/repository layer and wire the trading loop so governance decisions are persisted even when the action is HOLD or an execution is blocked early. Persist only audit-safe fields:
    - action and final gate decision
    - gate reasons
    - conflict ratio and regime
    - threshold source / artifact version
    - challenger or retraining evaluation summary if available
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_logging.py -q</automated>
  </verify>
  <acceptance_criteria>
    - database/models.py contains a governance audit model with indexed timestamp field
    - trading/trading_loop.py persists HOLD/rejected/pass governance outcomes before returning
    - `python -m pytest tests/test_decision_logging.py -q` exits 0
  </acceptance_criteria>
  <done>Governance decisions are durably persisted for monitoring and later retraining review.</done>
</task>

<task type="auto">
  <name>Task 3: Implement champion/challenger and retraining evaluators</name>
  <files>ai_engine/governance/promotion.py, monitoring/model_monitor.py, tests/test_champion_challenger.py</files>
  <read_first>
    - ai_engine/training/model_versioning.py
    - monitoring/model_monitor.py
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
  </read_first>
  <action>
    Implement utility functions that compare candidate vs champion based on:
    - calibrated metric deltas (for example Brier/log-loss non-regression)
    - aggregate profit factor and drawdown deltas
    - minimum trade count and evidence window

    Extend monitoring/model_monitor.py or the closest monitoring entrypoint so retraining triggers are expressed in calibrated degradation terms rather than raw confidence alone.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_champion_challenger.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/governance/promotion.py exists and exports candidate promotion and retraining evaluation helpers
    - monitor logic references calibrated degradation or artifact-backed metrics, not only raw confidence
    - `python -m pytest tests/test_champion_challenger.py -q` exits 0
  </acceptance_criteria>
  <done>Operational governance can compare challengers safely and trigger retraining from calibrated evidence.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_decision_logging.py tests/test_champion_challenger.py -q`
- `python -c "from ai_engine.governance.promotion import evaluate_candidate_promotion, evaluate_retraining_trigger; print('promotion OK')"`
</verification>

<success_criteria>
- CONF-04 addressed by auditable challenger evaluation before rollout
- CONF-05 addressed by calibrated degradation-based retraining logic
- Runtime path persists governance decisions including HOLD/rejected outcomes
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-03-SUMMARY.md`.
</output>
