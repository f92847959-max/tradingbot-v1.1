---
phase: 12.1-ai-confidence-calibration-decision-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/calibration/__init__.py
  - ai_engine/calibration/calibrator.py
  - ai_engine/calibration/metrics.py
  - ai_engine/calibration/threshold_tuner.py
  - ai_engine/calibration/artifacts.py
  - ai_engine/training/walk_forward.py
  - ai_engine/training/pipeline.py
  - ai_engine/training/model_versioning.py
  - tests/test_calibration.py
  - tests/test_threshold_tuner.py
  - tests/test_walk_forward_calibration.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
must_haves:
  truths:
    - "Model calibration is fit on validation/calibration data, never on the final test slice"
    - "Threshold selection is separate from probability estimation and is persisted as a versioned artifact"
    - "Calibration metrics report Brier/log-loss and reliability-oriented summaries"
    - "Version directories contain calibration.json and thresholds.json beside version.json"
  artifacts:
    - path: "ai_engine/calibration/calibrator.py"
      provides: "Sklearn-based fit/load/apply calibrator utilities"
      exports: ["fit_calibrator", "apply_calibrator", "load_calibrator"]
    - path: "ai_engine/calibration/threshold_tuner.py"
      provides: "Regime-aware threshold search and threshold table persistence"
      exports: ["tune_thresholds", "lookup_threshold"]
    - path: "ai_engine/calibration/artifacts.py"
      provides: "Calibration and threshold artifact schema helpers"
      exports: ["write_calibration_artifact", "write_threshold_artifact", "load_threshold_artifact"]
    - path: "tests/test_walk_forward_calibration.py"
      provides: "Integration coverage for walk-forward calibration and artifact persistence"
  key_links:
    - from: "ai_engine/calibration/threshold_tuner.py"
      to: "strategy/regime_detector.py"
      via: "reuses MarketRegime labels for threshold keys"
      pattern: "MarketRegime"
    - from: "ai_engine/training/model_versioning.py"
      to: "ai_engine/calibration/artifacts.py"
      via: "stores calibration artifacts inside the existing version directory"
      pattern: "version.json"
---

<objective>
Build the training-side calibration and thresholding foundation for Phase 12.1: reusable calibration utilities, regime-aware threshold tuning, artifact schemas, and walk-forward/pipeline integration that persists calibration metadata with each model version.

Purpose: CONF-01 and CONF-02 must be solved before runtime governance can trust confidence scores or load versioned thresholds.

Output: new `ai_engine/calibration/` module, wave-0 tests, and training integration that writes `calibration.json` and `thresholds.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-VALIDATION.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-PATTERNS.md

<interfaces>
From ai_engine/training/model_versioning.py:
```python
def create_version_dir(base_dir: str) -> str
def write_version_json(version_dir: str, version_data: dict) -> str
def update_production_pointer(base_dir: str, version_dir: str) -> None
```

From ai_engine/training/walk_forward.py:
```python
class WalkForwardValidator:
    def run_window(self, window, X, y, feature_names, trainer) -> Dict[str, Any]
```

From strategy/regime_detector.py:
```python
class MarketRegime(enum.Enum):
    TRENDING = "trending"
    RANGING = "ranging"
    VOLATILE = "volatile"
```
</interfaces>
</context>

<threat_model>
- T-12.1-01: Data leakage during calibration or threshold tuning inflates confidence quality
  mitigation: fit calibrators and threshold tables only on train/validation windows, never final test rows
- T-12.1-02: Tampered calibration/threshold artifacts enable unsafe production decisions
  mitigation: validate schema, numeric ranges, class labels, and artifact path before loading
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED tests and artifact schemas</name>
  <files>tests/test_calibration.py, tests/test_threshold_tuner.py, tests/test_walk_forward_calibration.py, ai_engine/calibration/__init__.py, ai_engine/calibration/artifacts.py</files>
  <read_first>
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-VALIDATION.md
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
    - ai_engine/training/model_versioning.py
    - ai_engine/training/trade_filter.py
    - tests/test_model_versioning.py
  </read_first>
  <action>
    Create the calibration package marker and artifact schema helpers. Add RED/initial pytest coverage for:
    - calibration metrics shape and class-order safety
    - threshold artifact schema, fallback behavior, and regime-key lookup
    - walk-forward integration proving artifacts are written to the version directory

    Use deterministic fixture data and exact BUY/HOLD/SELL class-label expectations.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_calibration.py tests/test_threshold_tuner.py tests/test_walk_forward_calibration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_calibration.py exists and contains at least 3 test functions
    - tests/test_threshold_tuner.py exists and contains at least 3 test functions
    - tests/test_walk_forward_calibration.py exists and contains at least 2 test functions
    - ai_engine/calibration/artifacts.py exports read/write helpers for calibration and threshold artifacts
    - Tests assert BUY/HOLD/SELL label order explicitly
  </acceptance_criteria>
  <done>Wave-0 calibration tests and artifact schema helpers exist and define the expected contracts for CONF-01/CONF-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement calibration utilities and threshold tuning</name>
  <files>ai_engine/calibration/calibrator.py, ai_engine/calibration/metrics.py, ai_engine/calibration/threshold_tuner.py, tests/test_calibration.py, tests/test_threshold_tuner.py</files>
  <read_first>
    - ai_engine/training/walk_forward.py
    - ai_engine/training/trade_filter.py
    - strategy/regime_detector.py
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
  </read_first>
  <action>
    Implement sklearn-based calibration utilities using the repo-approved approach from research. Add metric helpers for Brier/log-loss/reliability-oriented summaries. Implement threshold tuning that:
    - scores candidate thresholds by regime and direction
    - supports fallback to a global threshold when regime data is sparse
    - persists a versioned threshold artifact with schema_version, class_labels, source, and threshold tables

    Keep threshold tuning separate from model fitting and separate from runtime decision logic.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_calibration.py tests/test_threshold_tuner.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/calibration/calibrator.py exists and imports sklearn calibration APIs
    - ai_engine/calibration/metrics.py computes Brier/log-loss style metrics
    - ai_engine/calibration/threshold_tuner.py exposes tune and lookup helpers
    - `python -m pytest tests/test_calibration.py tests/test_threshold_tuner.py -q` exits 0
  </acceptance_criteria>
  <done>Calibration and threshold tuning utilities exist with passing focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate walk-forward, pipeline, and version persistence</name>
  <files>ai_engine/training/walk_forward.py, ai_engine/training/pipeline.py, ai_engine/training/model_versioning.py, ai_engine/calibration/artifacts.py, tests/test_walk_forward_calibration.py</files>
  <read_first>
    - ai_engine/training/walk_forward.py
    - ai_engine/training/pipeline.py
    - ai_engine/training/model_versioning.py
    - ai_engine/calibration/calibrator.py
    - ai_engine/calibration/threshold_tuner.py
  </read_first>
  <action>
    Extend the training flow so each walk-forward window can:
    - fit calibrators on validation/calibration data only
    - score calibrated outputs on test data
    - tune thresholds from validation results
    - report calibration metrics in the training report
    - save calibration.json and thresholds.json in the same version directory as version.json

    Update version metadata so downstream runtime loading can identify artifact versions cleanly.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_walk_forward_calibration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - walk_forward integration writes calibration and threshold artifacts into the version directory
    - training report includes calibration metrics or calibration summary fields
    - version metadata references threshold/calibration artifact locations
    - `python -m pytest tests/test_walk_forward_calibration.py -q` exits 0
  </acceptance_criteria>
  <done>Training pipeline persists calibrated confidence artifacts and threshold tables for runtime governance.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_calibration.py tests/test_threshold_tuner.py tests/test_walk_forward_calibration.py -q`
- `python -c "from ai_engine.calibration.threshold_tuner import tune_thresholds, lookup_threshold; print('calibration OK')"`
- `python -c "from ai_engine.calibration.artifacts import load_threshold_artifact; print('artifacts OK')"`
</verification>

<success_criteria>
- CONF-01 addressed by calibration utilities plus walk-forward integration
- CONF-02 addressed by data-derived, versioned threshold artifacts
- Calibration artifacts live in the existing model version directory structure
- Focused pytest coverage exists for calibrator, threshold tuner, and walk-forward persistence
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-01-SUMMARY.md`.
</output>
