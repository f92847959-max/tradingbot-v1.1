---
phase: 12.1-ai-confidence-calibration-decision-governance
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ai_engine/governance/__init__.py
  - ai_engine/governance/types.py
  - ai_engine/governance/decision_governor.py
  - ai_engine/prediction/ensemble.py
  - tests/test_decision_governor.py
  - tests/test_specialist_governance.py
autonomous: true
requirements:
  - CONF-03
must_haves:
  truths:
    - "Runtime gating uses calibrated confidence and threshold artifacts instead of raw hardcoded confidence gates"
    - "DecisionGovernor is a pure policy layer that can pass, block, weaken, or veto"
    - "Regime-specific threshold lookup falls back safely when regime-specific data is missing"
    - "Specialist evidence cannot create a standalone trade when the core action is HOLD"
  artifacts:
    - path: "ai_engine/governance/types.py"
      provides: "DecisionAudit, GateDecision, SpecialistSignal runtime contracts"
      exports: ["DecisionAudit", "GateDecision", "SpecialistSignal"]
    - path: "ai_engine/governance/decision_governor.py"
      provides: "Pure final gate policy based on confidence, thresholds, regime, and conflicts"
      exports: ["DecisionGovernor"]
    - path: "tests/test_decision_governor.py"
      provides: "Focused runtime gate behavior coverage"
    - path: "tests/test_specialist_governance.py"
      provides: "Phase 12.3 guardrails for specialist-only blocking"
  key_links:
    - from: "ai_engine/governance/decision_governor.py"
      to: "strategy/regime_detector.py"
      via: "reuses MarketRegime labels and existing regime vocabulary"
      pattern: "MarketRegime"
    - from: "ai_engine/prediction/ensemble.py"
      to: "ai_engine/governance/decision_governor.py"
      via: "delegates the final pass/block/weaken/veto decision"
      pattern: "DecisionGovernor"
---

<objective>
Implement runtime decision governance for calibrated scores: typed decision audit objects, a pure DecisionGovernor, and ensemble integration that replaces the current fixed min_confidence / decision_threshold / max_conflict_ratio gates with artifact-driven policy.

Purpose: CONF-03 needs a deterministic, inspectable runtime gate before persistence and monitoring can build on it.

Output: new `ai_engine/governance/` module, ensemble integration, and focused tests for pass/block/weaken/veto behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-VALIDATION.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-PATTERNS.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-01-PLAN.md

<interfaces>
From ai_engine/prediction/ensemble.py:
```python
class EnsemblePredictor:
    min_confidence: float
    decision_threshold: float
    max_conflict_ratio: float
```

From strategy/regime_detector.py:
```python
class MarketRegime(enum.Enum)
```
</interfaces>
</context>

<threat_model>
- T-12.1-03: Specialist or noisy side-signal bypasses the core ensemble and opens trades alone
  mitigation: DecisionGovernor must reject specialist-only trade creation and require a core non-HOLD action
- T-12.1-04: Regime-specific thresholds silently misroute to the wrong regime or drop gate reasons
  mitigation: typed DecisionAudit object, explicit fallback order, exact tests for gate reasons and fallback semantics
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create governance types and RED tests</name>
  <files>ai_engine/governance/__init__.py, ai_engine/governance/types.py, tests/test_decision_governor.py, tests/test_specialist_governance.py</files>
  <read_first>
    - strategy/regime_detector.py
    - ai_engine/prediction/ensemble.py
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-VALIDATION.md
  </read_first>
  <action>
    Define typed runtime contracts for gate results and add RED tests covering:
    - pass/block/weaken/veto decision outcomes
    - regime-specific threshold fallback to global or RANGING defaults
    - specialist contradiction and specialist-only rejection behavior
    - presence of gate reasons, threshold source, and conflict ratio in the returned audit
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_governor.py tests/test_specialist_governance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/governance/types.py exists and exports typed runtime contracts
    - tests/test_decision_governor.py exists with at least 4 test functions
    - tests/test_specialist_governance.py exists with at least 2 test functions
  </acceptance_criteria>
  <done>Runtime governance contracts and red tests define the required behavior for CONF-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DecisionGovernor policy</name>
  <files>ai_engine/governance/decision_governor.py, ai_engine/governance/types.py, tests/test_decision_governor.py, tests/test_specialist_governance.py</files>
  <read_first>
    - ai_engine/prediction/ensemble.py
    - strategy/regime_detector.py
    - ai_engine/calibration/threshold_tuner.py
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-RESEARCH.md
  </read_first>
  <action>
    Implement a pure DecisionGovernor that accepts calibrated scores, current regime, conflict ratio, optional specialist evidence, and loaded threshold artifacts. It must:
    - apply regime-specific threshold lookup with safe fallback
    - return pass/block/weaken/veto plus exact reasons
    - preserve specialist veto/weaken semantics without allowing specialist-only trades
    - remain deterministic and side-effect free
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_governor.py tests/test_specialist_governance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/governance/decision_governor.py exists and exports DecisionGovernor
    - DecisionGovernor returns a typed audit object including action, gate reason, and threshold source
    - `python -m pytest tests/test_decision_governor.py tests/test_specialist_governance.py -q` exits 0
  </acceptance_criteria>
  <done>DecisionGovernor enforces runtime gate policy using calibrated artifacts and explicit specialist safeguards.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate DecisionGovernor into EnsemblePredictor</name>
  <files>ai_engine/prediction/ensemble.py, ai_engine/governance/decision_governor.py, tests/test_decision_governor.py</files>
  <read_first>
    - ai_engine/prediction/ensemble.py
    - ai_engine/governance/decision_governor.py
    - strategy/regime_detector.py
  </read_first>
  <action>
    Refactor EnsemblePredictor so it keeps score aggregation but delegates the final pass/block/weaken/veto decision to DecisionGovernor. Replace fixed final gate behavior with artifact-driven policy while preserving:
    - global score and conflict calculation
    - higher timeframe alignment checks
    - explainable gate reasons in the returned aggregation payload
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_governor.py tests/test_specialist_governance.py tests/test_ensemble.py -q</automated>
  </verify>
  <acceptance_criteria>
    - EnsemblePredictor imports or instantiates DecisionGovernor
    - final aggregation payload includes gate reasons, threshold source, and conflict ratio
    - fixed raw confidence gate logic is replaced by DecisionGovernor policy
  </acceptance_criteria>
  <done>Runtime ensemble path uses DecisionGovernor as the single source of gating truth.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_decision_governor.py tests/test_specialist_governance.py -q`
- `python -m pytest tests/test_ensemble.py -q`
- `python -c "from ai_engine.governance.decision_governor import DecisionGovernor; print('governor OK')"`
</verification>

<success_criteria>
- CONF-03 addressed by a typed, explainable, artifact-driven runtime governance layer
- EnsemblePredictor no longer owns ad hoc final hard-gate policy directly
- Specialist guardrails for Phase 12.3 are present before the specialist model exists
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-02-SUMMARY.md`.
</output>
