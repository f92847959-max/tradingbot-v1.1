---
phase: 07-control-app-dashboard-history
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - goldbot-control-app/shared/contracts.py
  - goldbot-control-app/shared/types.ts
  - goldbot-control-app/backend/app/services.py
  - goldbot-control-app/backend/app/routers/realtime.py
  - goldbot-control-app/backend/app/main.py
  - goldbot-control-app/tests/api/test_api_endpoints.py
autonomous: true
requirements: [CTRL-06]

must_haves:
  truths:
    - "Control app exposes an authenticated websocket endpoint for trade events."
    - "Trade open/close updates are emitted as typed events with stable dedupe keys."
    - "Websocket lifecycle is robust: connect, heartbeat, disconnect cleanup, reconnect-safe semantics."
    - "Realtime delivery uses source DB deltas instead of high-frequency full refresh loops."
  artifacts:
    - path: "goldbot-control-app/backend/app/routers/realtime.py"
      provides: "WebSocket endpoint /api/v1/ws/trades"
      exports: ["trades_ws"]
    - path: "goldbot-control-app/backend/app/services.py"
      provides: "Connection manager plus trade-event polling/broadcast pipeline"
    - path: "goldbot-control-app/shared/contracts.py"
      provides: "Trade event payload contract used by websocket route"
    - path: "goldbot-control-app/tests/api/test_api_endpoints.py"
      provides: "Websocket auth/payload/lifecycle test coverage"
  key_links:
    - from: "goldbot-control-app/backend/app/services.py"
      to: "goldbot-control-app/backend/app/routers/realtime.py"
      via: "router subscribes websocket clients through service connection manager"
      pattern: "connect_trade_socket|broadcast_trade_event"
    - from: "goldbot-control-app/backend/app/services.py"
      to: "goldbot-control-app/backend/app/main.py"
      via: "startup/shutdown hooks start and stop trade event producer"
      pattern: "on_event\\(\"startup\"\\)|on_event\\(\"shutdown\"\\)"
    - from: "goldbot-control-app/shared/contracts.py"
      to: "goldbot-control-app/shared/types.ts"
      via: "same event fields consumed by frontend stream hook"
      pattern: "TradeEvent"
---

<objective>
Add websocket-based realtime trade updates with typed payloads and operational safety.

Purpose: Fulfill CTRL-06 with true event streaming rather than dashboard-wide rapid polling.
Output: Authenticated websocket endpoint, event broadcaster/poller, and websocket API tests.
</objective>

<execution_context>
@C:/Users/roder/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/roder/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-control-app-dashboard-history/07-CONTEXT.md
@.planning/phases/07-control-app-dashboard-history/07-RESEARCH.md
@goldbot-control-app/backend/app/main.py
@goldbot-control-app/backend/app/services.py
@goldbot-control-app/backend/app/auth.py
@goldbot-control-app/shared/contracts.py
@goldbot-control-app/shared/types.ts
@goldbot-control-app/tests/api/test_api_endpoints.py
@.planning/phases/07-control-app-dashboard-history/07-01-PLAN.md

<interfaces>
Locked constraints for this plan:
- Frontend can not set custom headers for browser WebSocket connections.
- Existing protected REST endpoints use `X-Control-Token`.

WebSocket auth contract in this plan:
- Accept token via `?token=` query parameter and validate with the same backend token rule.
- Emit event payload fields: `event_id`, `event_type`, `deal_id`, `status`, `opened_at`, `closed_at`, `net_pnl`, `message`, `emitted_at`.
- Include `dedupe_key` string for client-side merge safety.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define realtime event contracts and auth-compatible websocket payload shape</name>
  <files>goldbot-control-app/shared/contracts.py, goldbot-control-app/shared/types.ts</files>
  <action>Add `TradeEvent` and optional heartbeat message DTOs to shared contracts. Include a required `dedupe_key` and explicit UTC timestamps for deterministic client merge logic. Keep Python and TypeScript contracts structurally identical and avoid introducing a second event format.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd exec tsc -- --noEmit</automated>
  </verify>
  <done>Shared realtime DTOs compile and are ready for backend and frontend use without type casting hacks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement websocket route, connection manager, and trade delta producer</name>
  <files>goldbot-control-app/backend/app/services.py, goldbot-control-app/backend/app/routers/realtime.py, goldbot-control-app/backend/app/main.py</files>
  <action>Implement `/api/v1/ws/trades` with token-query authentication, connection tracking, and disconnect cleanup. Add a background producer in `ControlService` that checks source DB for newly opened/closed trades on a short interval, converts deltas to `TradeEvent`, and broadcasts to connected clients. Register router plus startup/shutdown lifecycle hooks so the producer starts once and terminates cleanly.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2 &amp;&amp; python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q -k "websocket or ws or realtime"</automated>
  </verify>
  <done>WebSocket clients receive trade events without leaking connections, and server lifecycle hooks manage producer start/stop safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add websocket integration tests for auth, payload, and lifecycle</name>
  <files>goldbot-control-app/tests/api/test_api_endpoints.py</files>
  <action>Add tests that verify unauthorized socket rejection, authorized socket connection, event payload schema, and clean disconnect behavior. Include at least one test proving a new trade row inserted into source DB is emitted as a websocket event with stable `dedupe_key` and `event_type` semantics.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2 &amp;&amp; python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q</automated>
  </verify>
  <done>API tests include deterministic websocket coverage and pass together with existing REST tests.</done>
</task>

</tasks>

<verification>
- `python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q` passes with websocket tests included.
- `npm.cmd exec tsc -- --noEmit` passes in `goldbot-control-app/frontend`.
- Manual local websocket smoke check confirms events arrive while keeping REST polling as fallback only.
</verification>

<success_criteria>
- CTRL-06 backend contract exists and is stable under connect/disconnect cycles.
- Event payload includes dedupe-safe identifiers and timestamps for frontend merge logic.
- Realtime stream no longer depends on 250ms full dashboard polling.
</success_criteria>

<output>
After completion, create `.planning/phases/07-control-app-dashboard-history/07-02-SUMMARY.md`.
</output>

