---
phase: 07-control-app-dashboard-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - goldbot-control-app/shared/contracts.py
  - goldbot-control-app/shared/types.ts
  - goldbot-control-app/backend/app/services.py
  - goldbot-control-app/backend/app/routers/trades.py
  - goldbot-control-app/backend/app/routers/models.py
  - goldbot-control-app/backend/app/main.py
  - goldbot-control-app/tests/api/test_api_endpoints.py
autonomous: true
requirements: [CTRL-04, CTRL-05]

must_haves:
  truths:
    - "Trade history API supports server-side filtering by date range, direction, and net P&L range."
    - "Trade history API returns deterministic ordering and limit handling for dashboard rendering."
    - "Model performance API returns normalized metrics per model/version with version-json-first fallback."
    - "Frontend-consumable TS and Python contracts for history and model performance stay aligned."
  artifacts:
    - path: "goldbot-control-app/backend/app/routers/trades.py"
      provides: "GET /api/v1/trades/history with validated filter query parameters"
      exports: ["trade_history"]
    - path: "goldbot-control-app/backend/app/routers/models.py"
      provides: "GET /api/v1/models/performance endpoint"
      exports: ["model_performance"]
    - path: "goldbot-control-app/shared/contracts.py"
      provides: "Pydantic schemas for history filter/result and model performance payload"
    - path: "goldbot-control-app/shared/types.ts"
      provides: "TypeScript interfaces matching new backend contracts"
  key_links:
    - from: "goldbot-control-app/backend/app/routers/trades.py"
      to: "goldbot-control-app/backend/app/services.py"
      via: "router delegates validated filter object to service.get_trade_history"
      pattern: "service\\.get_trade_history"
    - from: "goldbot-control-app/backend/app/routers/models.py"
      to: "goldbot-control-app/backend/app/services.py"
      via: "router delegates to service.get_model_performance"
      pattern: "service\\.get_model_performance"
    - from: "goldbot-control-app/shared/contracts.py"
      to: "goldbot-control-app/shared/types.ts"
      via: "same field names and enum-like values for API payload symmetry"
      pattern: "TradeHistory|ModelPerformance"
---

<objective>
Build backend data contracts for Phase 7 history and model metrics without expanding scope beyond dashboard/history needs.

Purpose: Satisfy CTRL-04 and CTRL-05 with stable API contracts before websocket and UI wiring.
Output: Filter-capable trade history endpoint, model performance endpoint, aligned shared contracts, and passing API tests.
</objective>

<execution_context>
@C:/Users/roder/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/roder/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-control-app-dashboard-history/07-CONTEXT.md
@.planning/phases/07-control-app-dashboard-history/07-RESEARCH.md
@goldbot-control-app/backend/app/services.py
@goldbot-control-app/backend/app/routers/trades.py
@goldbot-control-app/shared/contracts.py
@goldbot-control-app/shared/types.ts
@goldbot-control-app/tests/api/test_api_endpoints.py

<interfaces>
Existing contracts to extend:
- `TradeChartPoint` in `shared/contracts.py` and `shared/types.ts` already models trade rows.
- `ControlService.get_trade_chart_points(days, limit)` already reads from source SQLite `trades`.

New contracts in this plan:
- `TradeHistoryQuery` with `date_from`, `date_to`, `direction`, `pnl_min`, `pnl_max`, `limit`.
- `TradeHistoryEntry` payload for table rows.
- `ModelPerformanceEntry` payload with `model_name`, `version`, `trained_at`, `accuracy`, `profit_factor`, `win_rate`, `source`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared history/model DTOs in Python and TypeScript</name>
  <files>goldbot-control-app/shared/contracts.py, goldbot-control-app/shared/types.ts</files>
  <action>Create strongly-typed request/response contracts for filtered trade history and model performance. Keep field names and optionality exactly mirrored between Python and TypeScript to prevent frontend/backend contract drift. Include explicit UTC timestamp fields and a `source` flag on model metrics (`version_json` or `legacy_metadata`) for fallback transparency.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd exec tsc -- --noEmit</automated>
  </verify>
  <done>Both shared contract files export the new DTOs and type-check passes without frontend type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement filtered history and model performance endpoints</name>
  <files>goldbot-control-app/backend/app/services.py, goldbot-control-app/backend/app/routers/trades.py, goldbot-control-app/backend/app/routers/models.py, goldbot-control-app/backend/app/main.py</files>
  <action>Extend `ControlService` with `get_trade_history(filters)` and `get_model_performance(limit)` methods. In history retrieval, apply server-side filters for date range, direction, and P&amp;L bounds before response shaping, then return newest-first with deterministic limit behavior. Add `GET /api/v1/trades/history` and `GET /api/v1/models/performance` routers and register the models router in `main.py`. For model metrics, read `ai_engine/saved_models/production.json` then `version.json`; fallback to legacy `model_metadata.json` when needed, normalizing output shape.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2 &amp;&amp; python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q -k "history or performance"</automated>
  </verify>
  <done>New endpoints return typed JSON with validated query handling, including fallback-safe model metrics responses.</done>
</task>

<task type="auto">
  <name>Task 3: Extend API tests for filtering semantics and model metrics fallback</name>
  <files>goldbot-control-app/tests/api/test_api_endpoints.py</files>
  <action>Add endpoint tests that verify (1) direction/date/P&amp;L filter combinations, (2) ordering and limit behavior, (3) empty-result behavior, and (4) model performance fallback from version files to legacy metadata. Keep fixtures deterministic by creating synthetic source DB rows and temporary model metadata files in test setup.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2 &amp;&amp; python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q</automated>
  </verify>
  <done>API test suite covers happy-path and edge cases for both new endpoints and passes in isolation.</done>
</task>

</tasks>

<verification>
- `python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q` passes.
- `npm.cmd exec tsc -- --noEmit` passes in `goldbot-control-app/frontend`.
- Manual curl sanity checks for `GET /api/v1/trades/history` and `GET /api/v1/models/performance` return expected JSON shape.
</verification>

<success_criteria>
- CTRL-04 backend contract is complete: history can be filtered server-side by date, direction, and P&L.
- CTRL-05 backend contract is complete: model performance endpoint returns normalized, version-aware metrics.
- Shared TS/Python contracts are aligned and compile cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-control-app-dashboard-history/07-01-SUMMARY.md`.
</output>

