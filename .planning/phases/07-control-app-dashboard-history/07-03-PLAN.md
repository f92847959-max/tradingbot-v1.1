---
phase: 07-control-app-dashboard-history
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx
  - goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx
  - goldbot-control-app/frontend/src/components/ActivityFeed.tsx
  - goldbot-control-app/frontend/src/components/ErrorPanel.tsx
  - goldbot-control-app/frontend/src/components/ChartPanel.tsx
  - goldbot-control-app/frontend/src/App.tsx
  - goldbot-control-app/frontend/src/styles.css
autonomous: true
requirements: [CTRL-04, CTRL-05, CTRL-06]

must_haves:
  truths:
    - "Dashboard presents status plus KPIs as one horizontal strip with scroll-snap behavior."
    - "Activity and error panes use adaptive viewport-based heights instead of fixed short/long sizes."
    - "Trade history and model performance UI sections exist with clear filter/metric placeholders ready for data wiring."
    - "Motion and visual language follow locked decisions: minimal dark glass, restrained accenting, controlled micro-animations."
    - "Deferred ideas stay out of scope (no startup Lottie sequence, no wow-only effects that hurt readability)."
  artifacts:
    - path: "goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx"
      provides: "History layout with filter controls and compact table shell"
    - path: "goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx"
      provides: "Model metrics panel shell for per-model/per-version cards or rows"
    - path: "goldbot-control-app/frontend/src/App.tsx"
      provides: "Page composition enforcing one-line status strip and dashboard section order"
    - path: "goldbot-control-app/frontend/src/styles.css"
      provides: "Minimal-dark token updates and required interaction/motion classes"
  key_links:
    - from: "goldbot-control-app/frontend/src/App.tsx"
      to: "goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx"
      via: "dashboard page mounts history panel in main live-data flow"
      pattern: "TradeHistoryPanel"
    - from: "goldbot-control-app/frontend/src/App.tsx"
      to: "goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx"
      via: "dashboard page mounts model performance panel near history context"
      pattern: "ModelPerformancePanel"
    - from: "goldbot-control-app/frontend/src/styles.css"
      to: "goldbot-control-app/frontend/src/components/ActivityFeed.tsx"
      via: "compact grouped log styling and timestamp de-emphasis"
      pattern: "activity-group|activity-time"
---

<objective>
Build the Phase 7 UI structure and visual system according to locked context decisions, independent of backend contract timing.

Purpose: Establish final dashboard/history layout and interaction language before data wiring work.
Output: New history/model panels, updated dashboard composition, and CSS/motion foundation aligned with CONTEXT.md.
</objective>

<execution_context>
@C:/Users/roder/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/roder/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-control-app-dashboard-history/07-CONTEXT.md
@goldbot-control-app/frontend/src/App.tsx
@goldbot-control-app/frontend/src/styles.css
@goldbot-control-app/frontend/src/components/ActivityFeed.tsx
@goldbot-control-app/frontend/src/components/ErrorPanel.tsx
@goldbot-control-app/frontend/src/components/ChartPanel.tsx

<interfaces>
UI decision constraints to implement exactly:
- Status + KPI strip: single horizontal track, scroll-snap, airy 16px major spacing.
- Dashboard main flow must prioritize live data; action controls stay in risk/settings contexts.
- Chart defaults: EMA + VWAP visible; event markers remain small and status-colored.
- Logs/errors: compact grouping, smaller timestamps, JSON details as smooth accordion (~300ms target).
- Motion feedback: status fade/pulse, 3s heartbeat pulse, latency glow/wobble, chart layer fade/slide, log slide+flash, guard-blocked shake, nav underline, theme cross-fade.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create history and model performance presentation components</name>
  <files>goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx, goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx</files>
  <action>Create two new presentational components with explicit props interfaces for filters, rows, loading state, and empty state. Keep components data-source agnostic in this plan (receive data via props only) so backend wiring can be completed later without redesign churn. Use compact density and table/card typography suitable for frequent scanning.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd exec tsc -- --noEmit</automated>
  </verify>
  <done>Both components render with typed props, compile cleanly, and expose stable interfaces for integration plan 07-04.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard composition for one-line status rhythm and adaptive panes</name>
  <files>goldbot-control-app/frontend/src/App.tsx, goldbot-control-app/frontend/src/components/ActivityFeed.tsx, goldbot-control-app/frontend/src/components/ErrorPanel.tsx, goldbot-control-app/frontend/src/components/ChartPanel.tsx</files>
  <action>Update `App.tsx` so dashboard uses a single horizontal status+KPI strip, then chart/history/performance/feed sections in live-data-first order. Ensure quick actions are not placed in dashboard main flow. Update feed/error/chart components for compact grouped entries, de-emphasized timestamps, and marker defaults (EMA+VWAP). Keep all strings and behavior aligned to current app conventions where possible.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd run build</automated>
  </verify>
  <done>Dashboard structure matches locked visual hierarchy and compiles/builds without layout regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Implement minimal-dark motion and interaction rules from CONTEXT</name>
  <files>goldbot-control-app/frontend/src/styles.css, goldbot-control-app/frontend/src/App.tsx</files>
  <action>Apply monochrome-first minimal dark tokens with restrained accents and glass surfaces. Implement required micro-animations: status transition pulse, heartbeat every 3s, latency healthy glow/warning wobble, chart layer fade/slide, new marker drop-in, new log entry slide/highlight, guard-blocked shake, button hover lift, animated nav underline, theme cross-fade (~300ms). Do not add startup Lottie or extra wow-only effects deferred in context.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd run build</automated>
  </verify>
  <done>Visual and motion behavior follows CONTEXT decisions and deferred ideas remain excluded.</done>
</task>

</tasks>

<verification>
- `npm.cmd exec tsc -- --noEmit` passes.
- `npm.cmd run build` passes.
- Manual UI smoke check confirms dashboard shows one-line status strip, adaptive pane heights, and required micro-feedback.
</verification>

<success_criteria>
- Locked UX decisions from 07-CONTEXT are materially implemented in UI structure and styling.
- Trade history and model performance panels exist and are integration-ready.
- No deferred visual scope items are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-control-app-dashboard-history/07-03-SUMMARY.md`.
</output>

