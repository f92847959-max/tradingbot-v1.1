---
phase: 07-control-app-dashboard-history
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - goldbot-control-app/frontend/src/api/client.ts
  - goldbot-control-app/frontend/src/hooks/useDashboardData.ts
  - goldbot-control-app/frontend/src/hooks/useTradeStream.ts
  - goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx
  - goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx
  - goldbot-control-app/frontend/src/App.tsx
  - goldbot-control-app/frontend/src/types/viewModels.ts
autonomous: true
requirements: [CTRL-04, CTRL-05, CTRL-06]

must_haves:
  truths:
    - "Trade history filters in UI call backend history API and update displayed rows without full-page reload."
    - "Model performance panel displays normalized metrics returned by backend endpoint."
    - "New trade events appear in UI and toast stream in real time through websocket updates."
    - "Realtime state merge is resilient to reconnects and avoids duplicate event rendering."
  artifacts:
    - path: "goldbot-control-app/frontend/src/hooks/useTradeStream.ts"
      provides: "WebSocket connection lifecycle with reconnect and dedupe handling"
    - path: "goldbot-control-app/frontend/src/api/client.ts"
      provides: "Typed REST and websocket client functions for history, performance, and trade stream"
    - path: "goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx"
      provides: "Bound filter controls plus table rendering of live history rows"
    - path: "goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx"
      provides: "Bound rendering of model metrics returned by backend"
  key_links:
    - from: "goldbot-control-app/frontend/src/api/client.ts"
      to: "goldbot-control-app/backend/app/routers/trades.py"
      via: "fetchTradeHistory -> /api/v1/trades/history"
      pattern: "/trades/history"
    - from: "goldbot-control-app/frontend/src/api/client.ts"
      to: "goldbot-control-app/backend/app/routers/models.py"
      via: "fetchModelPerformance -> /api/v1/models/performance"
      pattern: "/models/performance"
    - from: "goldbot-control-app/frontend/src/hooks/useTradeStream.ts"
      to: "goldbot-control-app/backend/app/routers/realtime.py"
      via: "WebSocket URL /api/v1/ws/trades with token query auth"
      pattern: "ws/trades"
---

<objective>
Wire Phase 7 dashboard/history UI to the new REST and websocket contracts so data and notifications are genuinely live.

Purpose: Complete CTRL-04, CTRL-05, and CTRL-06 behavior end to end.
Output: Integrated filterable history, model metrics panel, realtime event stream merge, and in-app trade notifications.
</objective>

<execution_context>
@C:/Users/roder/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/roder/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-control-app-dashboard-history/07-CONTEXT.md
@.planning/phases/07-control-app-dashboard-history/07-RESEARCH.md
@.planning/phases/07-control-app-dashboard-history/07-01-PLAN.md
@.planning/phases/07-control-app-dashboard-history/07-02-PLAN.md
@.planning/phases/07-control-app-dashboard-history/07-03-PLAN.md
@goldbot-control-app/frontend/src/api/client.ts
@goldbot-control-app/frontend/src/hooks/useDashboardData.ts
@goldbot-control-app/frontend/src/App.tsx
@goldbot-control-app/frontend/src/types/viewModels.ts

<interfaces>
Expected backend contracts from dependent plans:
- `GET /api/v1/trades/history` with filter query params and typed row payload.
- `GET /api/v1/models/performance` with normalized metrics rows.
- `WS /api/v1/ws/trades?token=...` emitting `TradeEvent` payloads with `dedupe_key`.

Client merge rule:
- Use REST snapshot as baseline.
- Apply websocket deltas by `dedupe_key` to avoid duplicates after reconnect.
- Keep lightweight polling fallback for resilience when websocket is unavailable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API client and view models for history, performance, and websocket contracts</name>
  <files>goldbot-control-app/frontend/src/api/client.ts, goldbot-control-app/frontend/src/types/viewModels.ts</files>
  <action>Add typed client methods `fetchTradeHistory(filters)`, `fetchModelPerformance(limit)`, and websocket URL/connection helpers for `/ws/trades`. Serialize filter params explicitly (including optional date and P&amp;L bounds), and expose typed DTOs in `viewModels.ts` so UI components stop relying on loosely shaped data.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd exec tsc -- --noEmit</automated>
  </verify>
  <done>Client exports typed functions for all new Phase 7 endpoints and TypeScript compilation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement realtime stream hook and integrate with dashboard data state</name>
  <files>goldbot-control-app/frontend/src/hooks/useTradeStream.ts, goldbot-control-app/frontend/src/hooks/useDashboardData.ts</files>
  <action>Create `useTradeStream` that handles websocket connect/disconnect, heartbeat awareness, exponential reconnect backoff, and event deduplication by `dedupe_key`. Integrate this hook with `useDashboardData` so history rows, toast events, and latency/heartbeat status update immediately on stream events while preserving a slower fallback polling path if stream drops.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd run build</automated>
  </verify>
  <done>Realtime hook is active, reconnect-safe, and merged into dashboard state without duplicate entries.</done>
</task>

<task type="auto">
  <name>Task 3: Bind new data flows into dashboard panels and trade notifications</name>
  <files>goldbot-control-app/frontend/src/components/TradeHistoryPanel.tsx, goldbot-control-app/frontend/src/components/ModelPerformancePanel.tsx, goldbot-control-app/frontend/src/App.tsx</files>
  <action>Wire filter state to `fetchTradeHistory`, render backend model metrics in `ModelPerformancePanel`, and route websocket trade events into the existing toast stack with concise buy/sell/open/close messages. Ensure new trades and closed-trade P&amp;L updates appear without page refresh and that filter changes do not break live updates.</action>
  <verify>
    <automated>cd C:/Users/roder/goldbot2/goldbot-control-app/frontend &amp;&amp; npm.cmd exec tsc -- --noEmit &amp;&amp; npm.cmd run build</automated>
  </verify>
  <done>Dashboard demonstrates end-to-end behavior: filterable history, model metrics display, and realtime notifications/update flow.</done>
</task>

</tasks>

<verification>
- `npm.cmd exec tsc -- --noEmit` passes.
- `npm.cmd run build` passes.
- `python -m pytest goldbot-control-app/tests/api/test_api_endpoints.py -q` still passes to confirm backend contracts used by UI.
</verification>

<success_criteria>
- CTRL-04 is met in UI: user can filter and inspect trade history.
- CTRL-05 is met in UI: user can inspect model performance metrics.
- CTRL-06 is met end to end: trade events update UI in real time without manual refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/07-control-app-dashboard-history/07-04-SUMMARY.md`.
</output>

