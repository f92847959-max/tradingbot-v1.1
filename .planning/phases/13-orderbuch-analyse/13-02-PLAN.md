---
phase: 13-orderbuch-analyse
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - market_data/orderflow_stream.py
  - config/settings.py
  - tests/test_orderflow_stream.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-04
must_haves:
  truths:
    - "Live quote enrichment is optional and must remain a no-op when bidQty/ofrQty are missing"
    - "The broker client already streams quote payloads; Phase 13 should add a safe aggregator instead of rewriting authentication or websocket lifecycle"
    - "L1 quote imbalance is a feature enrichment, not a standalone trade signal"
  artifacts:
    - path: "market_data/orderflow_stream.py"
      provides: "Quote payload normalization, L1 imbalance calculation, and candle-bucket aggregation"
      exports: ["compute_l1_imbalance", "QuoteFlowAggregator"]
    - path: "tests/test_orderflow_stream.py"
      provides: "Unit tests for quote payload handling, missing quantity fallback, and candle-aligned aggregation"
  key_links:
    - from: "market_data/orderflow_stream.py"
      to: "market_data/broker_client.py"
      via: "consumes quote callback payloads from CapitalComClient.start_price_stream"
      pattern: "payload callback"
    - from: "market_data/orderflow_stream.py"
      to: "ai_engine/features/orderflow_features.py"
      via: "emits `flow_l1_imbalance` columns for optional feature enrichment"
      pattern: "neutral fallback when columns are absent"
---

<objective>
Add the optional live quote-flow enrichment path for Phase 13.

Purpose: The project should process the order-flow data Capital.com actually provides (`quote` payloads with bid/ask and optional quantities) without over-scoping into unavailable Level 2 DOM. This wave turns live L1 quote data into candle-aligned `flow_l1_imbalance` inputs while preserving the OHLCV-only fallback from Plan 13-01.

Output: quote imbalance helper, candle-bucket aggregator, conservative settings, and tests proving missing-data no-op behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orderbuch-analyse/13-RESEARCH.md
@.planning/phases/13-orderbuch-analyse/13-VALIDATION.md
@.planning/phases/13-orderbuch-analyse/13-PATTERNS.md
@market_data/broker_client.py
@config/settings.py
@ai_engine/features/orderflow_features.py
</context>

<threat_model>
- T-13-04: Quote enrichment logs credentials or changes broker websocket auth behavior
  mitigation: new pure helper consumes payload dicts only; broker token handling stays untouched
- T-13-05: Missing `bidQty`/`ofrQty` crashes live trading or emits misleading extreme imbalance
  mitigation: missing, zero, or invalid quantities return neutral 0.0 and are covered by tests
- T-13-06: L1 imbalance is misrepresented as Level 2 wall/depth data
  mitigation: names and docs use `flow_l1_imbalance`; no `l2_` or DOM-depth claims
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for quote imbalance and aggregation</name>
  <files>tests/test_orderflow_stream.py</files>
  <read_first>
    - market_data/broker_client.py
    - .planning/phases/13-orderbuch-analyse/13-VALIDATION.md
    - .planning/phases/13-orderbuch-analyse/13-RESEARCH.md
  </read_first>
  <action>
    Add tests for:
    - `compute_l1_imbalance(4976, 5000)` returns a bounded value near zero
    - bid-heavy payload returns positive imbalance and offer-heavy payload returns negative imbalance
    - missing, zero, or malformed quantities return exactly 0.0
    - aggregator buckets quotes by candle timeframe and emits average `flow_l1_imbalance`
    - payloads never require broker credentials in the helper API
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_stream.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_orderflow_stream.py` exists with at least 5 test functions
    - tests assert exact neutral fallback value `0.0` for missing quantities
    - tests assert all imbalance outputs are within [-1.0, 1.0]
  </acceptance_criteria>
  <done>Quote-flow enrichment behavior is specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement quote payload normalization and candle aggregation</name>
  <files>market_data/orderflow_stream.py, tests/test_orderflow_stream.py</files>
  <read_first>
    - market_data/broker_client.py
    - tests/test_orderflow_stream.py
    - ai_engine/features/orderflow_features.py
  </read_first>
  <action>
    Implement a small pure module that:
    - exposes `compute_l1_imbalance(bid_qty, ofr_qty) -> float`
    - normalizes Capital.com quote payloads with `bid`, `ofr`, `bidQty`, `ofrQty`, and timestamp
    - aggregates quote imbalance into candle buckets for `1m`, `5m`, and `15m`
    - emits DataFrames or records containing `timestamp` and `flow_l1_imbalance`
    - avoids any dependency on authenticated client state
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_stream.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `market_data/orderflow_stream.py` exports `compute_l1_imbalance` and `QuoteFlowAggregator`
    - aggregation is deterministic for repeated payload order
    - invalid payloads are ignored or neutralized without raising
    - `python -m pytest tests/test_orderflow_stream.py -q` exits 0
  </acceptance_criteria>
  <done>Live quote payloads can be converted into safe candle-aligned order-flow enrichment.</done>
</task>

<task type="auto">
  <name>Task 3: Add conservative flow settings and feature handoff contract</name>
  <files>config/settings.py, market_data/orderflow_stream.py, tests/test_orderflow_stream.py</files>
  <read_first>
    - config/settings.py
    - market_data/orderflow_stream.py
    - .planning/phases/13-orderbuch-analyse/13-PATTERNS.md
  </read_first>
  <action>
    Add default-safe configuration and API documentation for:
    - `orderflow_enabled: bool = False`
    - `orderflow_quote_enrichment_enabled: bool = False`
    - flow profile/window thresholds used by Plan 13-01
    - explicit handoff shape: `timestamp`, `flow_l1_imbalance`, and optional `flow_l1_imbalance_ema_10`
    Keep defaults off/neutral so existing live trading behavior is unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_stream.py tests/test_orderflow_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Settings` exposes order-flow config fields with conservative defaults
    - quote enrichment remains disabled unless explicitly enabled
    - no test requires live broker credentials
  </acceptance_criteria>
  <done>Order-flow live enrichment is configurable, safe by default, and ready for FeatureEngineer integration.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_orderflow_stream.py -q`
- `python -m pytest tests/test_orderflow_stream.py tests/test_orderflow_features.py -q`
- `python -c "from market_data.orderflow_stream import compute_l1_imbalance; print(compute_l1_imbalance(3, 1))"`
</verification>

<success_criteria>
- FLOW-01 addressed by processing available Capital.com quote-flow payloads without true DOM assumptions
- FLOW-04 prepared by producing `flow_l1_imbalance` inputs consumable by feature engineering
- Live quote enrichment is disabled/neutral by default and cannot break OHLCV-only training
</success_criteria>

<output>
After completion, create `.planning/phases/13-orderbuch-analyse/13-02-SUMMARY.md`.
</output>
