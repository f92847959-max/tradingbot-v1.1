---
phase: 13-orderbuch-analyse
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - ai_engine/features/feature_engineer.py
  - ai_engine/features/__init__.py
  - tests/test_orderflow_integration.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - "`flow_*` features are first-class ML inputs exposed through FeatureEngineer"
    - "FeatureEngineer integration must not require live quote data and must remain NaN-free on OHLCV-only candles"
    - "Order-flow features are data/features only; runtime trade policy remains unchanged in Phase 13"
  artifacts:
    - path: "ai_engine/features/feature_engineer.py"
      provides: "FeatureEngineer integration and feature group exposure for order-flow features"
      exports: ["FeatureEngineer"]
    - path: "tests/test_orderflow_integration.py"
      provides: "Integration tests for feature names, feature groups, ML matrix safety, and UAT coverage"
  key_links:
    - from: "ai_engine/features/feature_engineer.py"
      to: "ai_engine/features/orderflow_features.py"
      via: "instantiates and calls OrderFlowFeatures"
      pattern: "feature group orchestration"
    - from: "tests/test_orderflow_integration.py"
      to: "ai_engine/features/feature_engineer.py"
      via: "asserts flow feature exposure and clean matrix output"
      pattern: "FeatureEngineer.create_features"
---

<objective>
Wire Phase 13 order-flow features into the ML feature pipeline and close UAT coverage.

Purpose: Order Flow work only helps the bot if the new `flow_*` columns are exposed through the same `FeatureEngineer` path used by training and prediction. This wave integrates the feature group, verifies ML compatibility, and proves all FLOW-01 through FLOW-04 requirements are covered.

Output: `FeatureEngineer` integration, package export, and integration tests that make the new order-flow feature group usable by the model pipeline.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orderbuch-analyse/13-RESEARCH.md
@.planning/phases/13-orderbuch-analyse/13-VALIDATION.md
@.planning/phases/13-orderbuch-analyse/13-PATTERNS.md
@ai_engine/features/feature_engineer.py
@ai_engine/features/__init__.py
@ai_engine/features/orderflow_features.py
@ai_engine/training/pipeline.py
@tests/test_microstructure_features.py
</context>

<threat_model>
- T-13-07: FeatureEngineer exposes `flow_*` names but does not actually calculate them
  mitigation: integration tests assert both name exposure and concrete DataFrame columns after `create_features()`
- T-13-08: New feature group breaks training/prediction by producing NaN/inf values or duplicate feature names
  mitigation: full integration test checks finite matrix, duplicate-free names, and no overlap with microstructure names
- T-13-09: Runtime trade behavior changes before validation
  mitigation: no changes to `trading/`, `risk/`, or execution policy in this phase
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED integration tests for FeatureEngineer exposure</name>
  <files>tests/test_orderflow_integration.py</files>
  <read_first>
    - ai_engine/features/feature_engineer.py
    - ai_engine/features/microstructure_features.py
    - tests/test_microstructure_features.py
    - .planning/phases/13-orderbuch-analyse/13-VALIDATION.md
  </read_first>
  <action>
    Add integration tests for:
    - `FeatureEngineer.get_feature_groups()` contains `orderflow`
    - `FeatureEngineer.get_feature_names()` includes all `OrderFlowFeatures.FEATURE_NAMES`
    - no `flow_*` feature overlaps with microstructure feature names
    - `create_features()` adds `flow_*` columns on OHLCV-only data
    - output feature matrix has no NaN/inf values in `flow_*` columns
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_orderflow_integration.py` exists with at least 5 test functions
    - tests assert exact group key `orderflow`
    - tests fail if any required `flow_*` feature is missing from FeatureEngineer
  </acceptance_criteria>
  <done>ML integration expectations are defined before wiring the feature group.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire OrderFlowFeatures into FeatureEngineer</name>
  <files>ai_engine/features/feature_engineer.py, ai_engine/features/__init__.py, tests/test_orderflow_integration.py</files>
  <read_first>
    - ai_engine/features/feature_engineer.py
    - ai_engine/features/orderflow_features.py
    - ai_engine/features/__init__.py
  </read_first>
  <action>
    Update feature engineering so:
    - `OrderFlowFeatures` is imported and instantiated
    - order-flow feature names are included in the default feature list
    - `create_features()` calls `self._orderflow.calculate(df)` before final cleanup
    - `get_feature_groups()` returns an `orderflow` group
    - `ai_engine/features/__init__.py` exports the new class if that file exposes feature classes
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_integration.py tests/test_orderflow_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `FeatureEngineer().get_feature_groups()["orderflow"]` is present
    - every `OrderFlowFeatures.FEATURE_NAMES` value appears in `FeatureEngineer().get_feature_names()`
    - `python -m pytest tests/test_orderflow_integration.py tests/test_orderflow_features.py -q` exits 0
  </acceptance_criteria>
  <done>Order-flow features are first-class ML inputs through the standard feature pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 13 UAT and import/compile gates</name>
  <files>tests/test_orderflow_integration.py, ai_engine/features/feature_engineer.py</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/13-orderbuch-analyse/13-VALIDATION.md
    - ai_engine/training/pipeline.py
  </read_first>
  <action>
    Add final coverage assertions and import checks so UAT can be closed:
    - FLOW-01: OHLCV and optional quote-flow data processed
    - FLOW-02: delta columns generated
    - FLOW-03: liquidity/profile/absorption columns generated
    - FLOW-04: FeatureEngineer exposes order-flow features as model inputs
    Ensure no runtime trading policy files are modified in this phase.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_features.py tests/test_orderflow_stream.py tests/test_orderflow_integration.py -q</automated>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m compileall ai_engine/features/orderflow_features.py market_data/orderflow_stream.py ai_engine/features/feature_engineer.py</automated>
  </verify>
  <acceptance_criteria>
    - targeted Phase 13 test suite exits 0
    - compileall exits 0 for all new/modified Phase 13 modules
    - UAT criteria in Roadmap can be checked off based on automated evidence
  </acceptance_criteria>
  <done>Phase 13 is ready for verification and execution summary generation.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_orderflow_features.py tests/test_orderflow_stream.py tests/test_orderflow_integration.py -q`
- `python -m compileall ai_engine/features/orderflow_features.py market_data/orderflow_stream.py ai_engine/features/feature_engineer.py`
- `python -c "from ai_engine.features.feature_engineer import FeatureEngineer; print('orderflow' in FeatureEngineer().get_feature_groups())"`
</verification>

<success_criteria>
- FLOW-01 through FLOW-04 are all covered by at least one executable plan
- `flow_*` features are available as ML model inputs through `FeatureEngineer`
- Existing runtime trading behavior remains unchanged until a later execution/strategy phase explicitly uses these features
</success_criteria>

<output>
After completion, create `.planning/phases/13-orderbuch-analyse/13-03-SUMMARY.md`.
</output>
