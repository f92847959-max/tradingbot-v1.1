---
phase: 13-orderbuch-analyse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/features/orderflow_features.py
  - tests/test_orderflow_features.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
must_haves:
  truths:
    - "Capital.com does not provide true multi-level DOM; the required baseline is OHLCV-derived order-flow approximation"
    - "All new feature names use the `flow_` prefix and do not duplicate existing microstructure features"
    - "BVC delta, liquidity zones, FVG, and absorption features are NaN-free and safe on zero-range candles"
  artifacts:
    - path: "ai_engine/features/orderflow_features.py"
      provides: "Standalone order-flow feature family for OHLCV-derived flow signals"
      exports: ["OrderFlowFeatures"]
    - path: "tests/test_orderflow_features.py"
      provides: "Unit tests for delta, liquidity/profile, FVG, absorption, safe defaults, and no-lookahead behavior"
  key_links:
    - from: "ai_engine/features/orderflow_features.py"
      to: "ai_engine/features/microstructure_features.py"
      via: "reuses feature group structure and neutral optional-input defaults"
      pattern: "FEATURE_NAMES + calculate + get_feature_names"
    - from: "ai_engine/features/orderflow_features.py"
      to: "shared/utils.py"
      via: "uses cleanup_dataframe_features for NaN/inf cleanup"
      pattern: "cleanup_dataframe_features"
---

<objective>
Create the core Order Flow feature engine for Phase 13.

Purpose: Phase 13 cannot safely use "Orderbuch" signals until the project has a deterministic, leakage-safe, OHLCV-compatible feature family that works for both historical backtests and live candles without pretending that Capital.com exposes true Level 2 DOM.

Output: `OrderFlowFeatures` plus focused tests for BVC delta, cumulative delta, volume profile distances, liquidity zones, fair value gaps, absorption, and safe default behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orderbuch-analyse/13-RESEARCH.md
@.planning/phases/13-orderbuch-analyse/13-VALIDATION.md
@.planning/phases/13-orderbuch-analyse/13-PATTERNS.md
@ai_engine/features/microstructure_features.py
@ai_engine/features/market_structure_liquidity.py
@shared/utils.py
@tests/test_microstructure_features.py
</context>

<threat_model>
- T-13-01: Implementation claims true Level 2/DOM support from Capital.com even though only OHLCV and optional L1 quotes are available
  mitigation: module naming, docs, tests, and feature names must describe OHLCV-derived `flow_*` approximations, not true DOM depth
- T-13-02: BVC delta divides by zero on flat candles and injects inf/NaN into ML
  mitigation: denominator floor, cleanup_dataframe_features, and doji-specific tests
- T-13-03: FVG or liquidity-zone logic uses future candles at prediction time
  mitigation: shift confirmed event signals to closed-candle-safe rows and add future-row mutation tests
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for OHLCV order-flow features</name>
  <files>tests/test_orderflow_features.py</files>
  <read_first>
    - .planning/phases/13-orderbuch-analyse/13-VALIDATION.md
    - .planning/phases/13-orderbuch-analyse/13-RESEARCH.md
    - tests/test_microstructure_features.py
    - ai_engine/features/microstructure_features.py
  </read_first>
  <action>
    Add deterministic tests for:
    - OHLCV-only input creating all `flow_*` columns
    - positive/negative `flow_delta` direction on bullish/bearish candles
    - zero-range candle safety with no NaN/inf
    - rolling cumulative delta and divergence
    - volume-profile POC/VAH/VAL distance outputs after warm-up
    - liquidity-zone and absorption signals on synthetic swing/volume fixtures
    - future-row mutation safety for closed-candle FVG/liquidity outputs
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_orderflow_features.py` exists with at least 7 test functions
    - tests reference exact feature names starting with `flow_`
    - one test asserts no `flow_*` column contains NaN or inf
    - one test mutates future rows and asserts historical FVG/liquidity outputs are unchanged
  </acceptance_criteria>
  <done>Wave-0 tests define the order-flow feature contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `OrderFlowFeatures` core calculations</name>
  <files>ai_engine/features/orderflow_features.py, tests/test_orderflow_features.py</files>
  <read_first>
    - ai_engine/features/microstructure_features.py
    - ai_engine/features/market_structure_liquidity.py
    - shared/utils.py
    - .planning/phases/13-orderbuch-analyse/13-PATTERNS.md
  </read_first>
  <action>
    Implement `OrderFlowFeatures` with:
    - `FEATURE_NAMES` containing only `flow_*` columns
    - BVC `flow_delta`, `flow_buy_pressure`, `flow_delta_cumulative_20`, and `flow_delta_divergence`
    - rolling volume-profile distances for POC, VAH, and VAL
    - swing-cluster liquidity zones above/below current price
    - closed-candle-safe FVG distance features
    - bounded absorption and volume z-score features
    - neutral fallback columns for optional L1 quote enrichment
    - `cleanup_dataframe_features(out, self.FEATURE_NAMES)` at the end
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ai_engine/features/orderflow_features.py` exports `OrderFlowFeatures`
    - `OrderFlowFeatures().get_feature_names()` returns only names beginning with `flow_`
    - zero-volume and zero-range candles do not produce NaN or inf
    - `python -m pytest tests/test_orderflow_features.py -q` exits 0
  </acceptance_criteria>
  <done>Core OHLCV-derived order-flow feature engine is implemented and test-covered.</done>
</task>

<task type="auto">
  <name>Task 3: Harden causality, optional dependency, and fallback boundaries</name>
  <files>ai_engine/features/orderflow_features.py, tests/test_orderflow_features.py</files>
  <read_first>
    - .planning/phases/13-orderbuch-analyse/13-RESEARCH.md
    - ai_engine/features/orderflow_features.py
    - requirements.txt
  </read_first>
  <action>
    Finalize the core module so it:
    - does not require `smartmoneyconcepts` unless compatibility is explicitly proven later
    - uses numpy/pandas/scipy-compatible logic already available in the repo
    - documents that outputs are approximations, not exchange DOM truth
    - keeps all feature values finite and ML-scalable
    - preserves historical rows when future rows are changed
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_orderflow_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` is unchanged unless an install/import proof exists
    - tests include finite-output assertions for every `flow_*` feature
    - no feature name overlaps with `MicrostructureFeatures.FEATURE_NAMES`
  </acceptance_criteria>
  <done>Order-flow core is safe for both historical training and live feature generation.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_orderflow_features.py -q`
- `python -c "from ai_engine.features.orderflow_features import OrderFlowFeatures; print(OrderFlowFeatures().get_feature_names())"`
</verification>

<success_criteria>
- FLOW-01 addressed for OHLCV-derived order-flow processing
- FLOW-02 addressed by signed BVC delta and cumulative delta features
- FLOW-03 addressed by liquidity/profile/FVG/absorption features
- All core `flow_*` features are deterministic, finite, and leakage-aware
</success_criteria>

<output>
After completion, create `.planning/phases/13-orderbuch-analyse/13-01-SUMMARY.md`.
</output>
