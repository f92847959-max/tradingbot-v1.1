---
phase: 12.6-existing-ai-autonomy-distillation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/training/decision_snapshot_capture.py
  - ai_engine/training/decision_distill_dataset.py
  - tests/test_decision_snapshot_capture.py
autonomous: true
requirements:
  - AUTOAI-01
  - AUTOAI-02
must_haves:
  truths:
    - "Teacher snapshots are captured before broker side effects and keep observation separate from realized outcomes"
    - "Hierarchical labels preserve preliminary, policy, final action, and block stage instead of collapsing blocked decisions into HOLD"
    - "Structured block codes and failed-check details are retained for later distillation and shadow analysis"
  artifacts:
    - path: "ai_engine/training/decision_snapshot_capture.py"
      provides: "Decision-time teacher snapshot capture and normalization"
      exports: ["DecisionSnapshot", "build_decision_snapshot"]
    - path: "ai_engine/training/decision_distill_dataset.py"
      provides: "Materialized distillation dataset export and label manifest"
      exports: ["materialize_distill_dataset", "write_label_summary"]
    - path: "tests/test_decision_snapshot_capture.py"
      provides: "Causal capture, label hierarchy, and no-lookahead coverage"
  key_links:
    - from: "ai_engine/training/decision_snapshot_capture.py"
      to: "trading/trading_loop.py"
      via: "hooks immediately after raw signal generation and before execution side effects"
      pattern: "_generate_signal"
    - from: "ai_engine/training/decision_snapshot_capture.py"
      to: "risk/risk_manager.py"
      via: "reuses structured risk approvals and failed check details"
      pattern: "RiskApproval"
---

<objective>
Create the teacher-snapshot and distillation-dataset layer for Phase 12.6.

Purpose: The autonomy head cannot be trained credibly until the repo captures the current teacher chain at decision time with causal labels that separate true HOLD behavior from downstream guard blocks.

Output: snapshot capture module, materialized dataset export, and focused tests proving causal capture and hierarchical labels.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.6-existing-ai-autonomy-distillation/12.6-RESEARCH.md
@.planning/phases/12.6-existing-ai-autonomy-distillation/12.6-CONTEXT.md
@.planning/phases/12.6-existing-ai-autonomy-distillation/12.6-VALIDATION.md
@.planning/phases/12.6-existing-ai-autonomy-distillation/12.6-PATTERNS.md
@trading/trading_loop.py
@trading/signal_generator.py
@strategy/strategy_manager.py
@risk/risk_manager.py
@calendar/event_service.py
@order_management/order_manager.py
</context>

<threat_model>
- T-12.6-01: Snapshot capture happens after side effects and leaks future execution state
  mitigation: capture boundary fixed at raw-signal stage and explicit tests for observation vs realized outcome separation
- T-12.6-02: Blocked decisions collapse into HOLD and poison the student target
  mitigation: hierarchical labels with `block_stage`, `block_codes`, and explicit action-mask tests
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for causal decision snapshots and hierarchical labels</name>
  <files>tests/test_decision_snapshot_capture.py</files>
  <read_first>
    - .planning/phases/12.6-existing-ai-autonomy-distillation/12.6-VALIDATION.md
    - trading/trading_loop.py
    - risk/risk_manager.py
  </read_first>
  <action>
    Add focused tests for:
    - capture before broker-side effects
    - preservation of `preliminary_action`, `policy_action`, `final_action`
    - structured `block_stage` and `block_codes`
    - retention of failed risk checks without parsing human-readable strings
    - future-row mutation safety for realized outcomes
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_snapshot_capture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_decision_snapshot_capture.py exists with at least 5 test functions
    - at least one test proves blocked actions are not flattened to generic HOLD
    - at least one test mutates post-capture outcome fields and asserts unchanged policy inputs
  </acceptance_criteria>
  <done>Wave-0 tests define the distillation dataset contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement decision snapshot capture and hierarchical dataset export</name>
  <files>ai_engine/training/decision_snapshot_capture.py, ai_engine/training/decision_distill_dataset.py, tests/test_decision_snapshot_capture.py</files>
  <read_first>
    - trading/signal_generator.py
    - trading/trading_loop.py
    - database/repositories/governance_repo.py
  </read_first>
  <action>
    Implement a dedicated capture/export layer that:
    - normalizes teacher outputs from ensemble, governor, strategy, calendar, and risk
    - stores decision-time observations separate from realized outcomes
    - emits structured label fields and a manifest describing the schema
    - is reusable for offline walk-forward training without depending on live broker execution
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_snapshot_capture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - snapshot/export modules exist and expose explicit capture/export APIs
    - dataset export includes block stage, block codes, and action hierarchy
    - `python -m pytest tests/test_decision_snapshot_capture.py -q` exits 0
  </acceptance_criteria>
  <done>Teacher snapshots are causal, structured, and ready for distillation.</done>
</task>

<task type="auto">
  <name>Task 3: Add manifest and coverage summaries for downstream training</name>
  <files>ai_engine/training/decision_distill_dataset.py, tests/test_decision_snapshot_capture.py</files>
  <read_first>
    - ai_engine/training/decision_distill_dataset.py
    - .planning/phases/12.6-existing-ai-autonomy-distillation/12.6-RESEARCH.md
  </read_first>
  <action>
    Extend the dataset layer so it emits:
    - `distill_dataset_manifest.json`
    - `label_summary.json`
    - class/block balance summaries and capture completeness fields for shadow-mode readiness
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_decision_snapshot_capture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - manifest and summary artifacts are serializable and schema-stable
    - HOLD-vs-blocked-HOLD balance is measurable from exported summaries
  </acceptance_criteria>
  <done>Downstream training can consume an explicit, auditable distillation dataset.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_decision_snapshot_capture.py -q`
- `python -c "from ai_engine.training.decision_snapshot_capture import build_decision_snapshot; print('decision snapshot OK')"`
</verification>

<success_criteria>
- AUTOAI-01 addressed by causal teacher snapshot capture
- AUTOAI-02 addressed by hierarchical labels and manifest output
- Phase 12.6 now has a trustworthy offline distillation substrate
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-existing-ai-autonomy-distillation/12.6-01-SUMMARY.md`.
</output>
