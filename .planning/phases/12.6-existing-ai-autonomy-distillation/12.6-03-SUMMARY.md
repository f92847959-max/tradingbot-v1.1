---
phase: 12.6-existing-ai-autonomy-distillation
plan: 03
subsystem: ai_engine.prediction
tags: [autonomy, rollout, shadow, governance]
requirements: [AUTOAI-05, AUTOAI-06]
key_files:
  modified:
    - ai_engine/prediction/ensemble.py
    - trading/signal_generator.py
    - trading/trading_loop.py
  created:
    - tests/test_autonomy_rollout.py
tests:
  command: "python -m pytest tests/test_autonomy_rollout.py -q"
  result: "5 passed"
completed: "2026-04-25T08:45:00.000Z"
---

# Phase 12.6 Plan 03 Summary: Guarded Rollout

Added staged rollout metadata for the autonomy decision head.

One-liner: the decision head can now run as `shadow`, `agreement_guarded`, or `primary_with_challenger` while preserving downstream strategy, risk, event, and broker authority and logging compact rollout metadata into governance summaries.

## What Changed

- Added optional decision-head scoring in `SignalGeneratorMixin` behind rollout mode settings.
- Added `EnsemblePredictor.apply_autonomy_rollout` as a narrow public hook for champion/candidate selection.
- Added governance evaluation summaries for autonomy rollout metadata and pre-AI kill-switch/event blocks.
- Added tests proving shadow behavior, agreement fallback, guarded primary selection, missing-candidate fallback, and `guard_bypass_count = 0`.

## Verification

- `python -m pytest tests/test_autonomy_rollout.py -q` -> `5 passed`
