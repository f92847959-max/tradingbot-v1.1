---
phase: 14-elliott-wave-integration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [
  "ai_engine/elliott_wave/fibonacci.py",
  "ai_engine/elliott_wave/rules.py",
  "ai_engine/elliott_wave/scoring.py",
  "tests/test_elliott_wave_advanced.py"
]
autonomous: true
requirements: [EWT-02]
must_haves:
  truths:
    - "Fibonacci projections and retracements are calculated for all identified waves."
    - "Advanced patterns (Diagonals, Flats, Triangles) are identified."
    - "System ranks multiple valid wave counts using a Fibonacci confluence score."
  artifacts:
    - path: "ai_engine/elliott_wave/fibonacci.py"
      provides: "Fibonacci ratio math and target projection"
    - path: "ai_engine/elliott_wave/scoring.py"
      provides: "Confluence scoring for pattern selection"
---

<objective>
Enhance the Elliott Wave engine with Fibonacci projections and support for advanced corrective patterns. Implement a scoring system to handle market ambiguity.

Purpose: Provide price targets and handle complex market structures.
Output: Fibonacci target calculator and expanded rule set for complex patterns.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/plan 14 eliotwafe/14-RESEARCH.md
@.planning/phases/plan 14 eliotwafe/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Fibonacci Projection and Retracement Math</name>
  <files>ai_engine/elliott_wave/fibonacci.py</files>
  <action>
    1. Create `ai_engine/elliott_wave/fibonacci.py`.
    2. Implement `calculate_retracement(start, end, ratio)` (ratios: 0.382, 0.5, 0.618, 0.786).
    3. Implement `calculate_projection(w1_start, w1_end, w3_start, ratio)` (ratios: 1.0, 1.272, 1.618, 2.618).
    4. Implement `get_wave_targets(pattern)` which returns a dict of expected price levels based on standard EWT ratios for the detected pattern.
  </action>
  <verify>
    <automated>python -c "from ai_engine.elliott_wave.fibonacci import calculate_projection; print('OK' if calculate_projection(100, 200, 150, 1.618) == 311.8 else 'FAIL')"</automated>
  </verify>
  <done>Fibonacci math functions are accurate and return expected targets.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Rule Engine for Advanced Patterns</name>
  <files>ai_engine/elliott_wave/rules.py</files>
  <action>
    1. Update `ai_engine/elliott_wave/rules.py`.
    2. Implement `DiagonalRule` (Leading/Ending Diagonals):
       - Wave 4 overlaps Wave 1.
       - Contracting: Wave 1 > Wave 3 > Wave 5.
    3. Implement `FlatRule` (Regular, Expanded, Running):
       - Regular: B $\approx$ A (90-105%), C $\approx$ A.
       - Expanded: B > 100% A, C > 100% B.
    4. Implement `TriangleRule` (Contracting/Expanding):
       - 5 waves (a-b-c-d-e), each $\approx$ 61.8-78.6% of previous.
  </action>
  <verify>
    <automated>python -m pytest tests/test_elliott_wave_advanced.py</automated>
  </verify>
  <done>Advanced patterns are correctly validated by the rule engine.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Fibonacci Confluence Scoring</name>
  <files>ai_engine/elliott_wave/scoring.py</files>
  <action>
    1. Create `ai_engine/elliott_wave/scoring.py`.
    2. Implement `score_pattern(pattern)`:
       - Reward patterns where waves land near standard Fibonacci ratios (e.g., Wave 2 at 61.8%).
       - Penalize patterns that barely pass rules.
    3. Implement `get_primary_count(patterns)` which selects the highest-scoring valid pattern.
  </action>
  <verify>
    <automated>python -m pytest tests/test_elliott_wave_advanced.py</automated>
  </verify>
  <done>The system reliably selects the most probable wave count based on Fibonacci confluence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scoring -> Signal | Scoring logic must be robust to prevent low-confidence patterns from triggering signals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | Projections | accept | Price targets are derived from public EWT rules. |
| T-14-04 | Tampering | Scoring Logic | mitigate | Use hardcoded standard ratios; do not allow external injection of ratios without validation. |
</threat_model>

<verification>
Run `pytest tests/test_elliott_wave_advanced.py` to verify advanced pattern detection and scoring.
</verification>

<success_criteria>
- Fibonacci targets are calculated and align with EWT standards.
- Advanced patterns (Flats, Triangles) are detected in test data.
- Multiple counts are handled, and the "Primary Count" is the most logical one.
</success_criteria>

<output>
After completion, create `.planning/phases/plan 14 eliotwafe/14-02-SUMMARY.md`
</output>
