---
phase: 12.3-ai-indicator-specialist-training
plan: 01
subsystem: ai_engine.features
tags: [specialist, market-structure, liquidity, leakage-safe]
requirements: [AITRAIN-01]
key_files:
  created:
    - ai_engine/features/market_structure_liquidity.py
    - tests/test_market_structure_liquidity.py
  modified:
    - ai_engine/features/feature_engineer.py
tests:
  command: "python -m pytest tests/test_market_structure_liquidity.py -q"
  result: "4 passed"
completed: "2026-04-23T22:28:00.000Z"
---

# Phase 12.3 Plan 01 Summary: Specialist Feature Block

Implemented the leakage-safe specialist feature family for market structure and liquidity context.

One-liner: `MarketStructureLiquidityFeatures` now exposes an explicit deterministic feature manifest, uses confirmed swings and shifted candle events for causal sweep/FVG signals, and plugs into `FeatureEngineer` as an opt-in block that leaves the baseline feature set untouched when disabled.

## What Changed

- Added `ai_engine/features/market_structure_liquidity.py` with explicit `FEATURE_NAMES` and a deterministic `calculate(df)` interface.
- Reused confirmed-swing semantics and shifted event flags so specialist features remain causal under future-row mutation checks.
- Added specialist-only sweep, range-context, FVG-size, and wick/ATR features designed for a later LightGBM specialist model.
- Extended `FeatureEngineer` with additive `include_specialist=True` support plus explicit specialist feature-group access without mutating the default baseline path.
- Added focused tests for determinism, future-row mutation safety, edge-case signal detection, and baseline-preserving integration.

## Verification

- `python -m pytest tests/test_market_structure_liquidity.py -q` -> `4 passed`
- `python -c "from ai_engine.features.market_structure_liquidity import MarketStructureLiquidityFeatures; print('specialist features OK')"` -> `specialist features OK`
