---
phase: 12.3-ai-indicator-specialist-training
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - ai_engine/prediction/ensemble.py
  - ai_engine/prediction/specialist.py
  - tests/test_ensemble_specialist.py
autonomous: true
requirements:
  - AITRAIN-04
must_haves:
  truths:
    - "Runtime specialist integration preserves exact baseline behavior when disabled or missing"
    - "Final aggregation logs core and specialist fields separately"
    - "Specialist influence is governance-compatible and never creates standalone trades"
  artifacts:
    - path: "ai_engine/prediction/specialist.py"
      provides: "SpecialistPrediction loader/adapter contract"
      exports: ["SpecialistPrediction", "SpecialistRuntime"]
    - path: "tests/test_ensemble_specialist.py"
      provides: "Disabled baseline equality and logging coverage"
  key_links:
    - from: "ai_engine/prediction/ensemble.py"
      to: "ai_engine/prediction/specialist.py"
      via: "loads specialist prediction as optional overlay"
      pattern: "specialist_score"
    - from: "ai_engine/prediction/ensemble.py"
      to: "Phase 12.1 governance interface"
      via: "specialist effect must remain governance-compatible and no-trade-alone safe"
      pattern: "gate_reasons"
---

<objective>
Integrate the specialist into runtime prediction as an optional overlay that logs its own score/confidence/reason while preserving baseline behavior and respecting the Phase 12.1 governance dependency.

Purpose: AITRAIN-04 is the runtime contract. The specialist must be visible and testable without silently replacing the core ensemble.

Output: specialist runtime adapter, ensemble logging fields, and tests for baseline equality plus no-trade-alone behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.3-ai-indicator-specialist-training/12.3-RESEARCH.md
@.planning/phases/12.3-ai-indicator-specialist-training/12.3-VALIDATION.md
@.planning/phases/12.3-ai-indicator-specialist-training/12.3-PATTERNS.md
@.planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-02-PLAN.md
@ai_engine/prediction/ensemble.py
</context>

<threat_model>
- T-12.3-04: Specialist changes behavior even when disabled or unavailable
  mitigation: exact disabled baseline equality tests and missing-artifact no-op fallback
- T-12.3-05: Specialist becomes a hidden standalone trader before 12.1 governance exists
  mitigation: no-trade-alone enforcement and governance-compatible overlay semantics only
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for disabled baseline equality and specialist logging</name>
  <files>tests/test_ensemble_specialist.py</files>
  <read_first>
    - .planning/phases/12.3-ai-indicator-specialist-training/12.3-VALIDATION.md
    - ai_engine/prediction/ensemble.py
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-02-PLAN.md
  </read_first>
  <action>
    Add tests for:
    - exact baseline equality when specialist is disabled or artifacts are missing
    - specialist logging fields in final aggregation
    - no-trade-alone behavior when core action is HOLD
    - confirm/weaken/veto effect reporting once governance-compatible path exists
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_ensemble_specialist.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ensemble_specialist.py exists with at least 4 test functions
    - one test asserts baseline equality with specialist disabled
    - one test asserts specialist cannot trade alone
  </acceptance_criteria>
  <done>Runtime specialist contract is locked down with baseline-preservation tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement specialist runtime adapter</name>
  <files>ai_engine/prediction/specialist.py, tests/test_ensemble_specialist.py</files>
  <read_first>
    - ai_engine/models/lightgbm_model.py
    - ai_engine/training/model_versioning.py
    - .planning/phases/12.3-ai-indicator-specialist-training/12.3-RESEARCH.md
  </read_first>
  <action>
    Implement a runtime adapter that:
    - loads specialist model + scaler + metadata from the specialist root
    - returns `specialist_score`, `specialist_confidence`, `specialist_reason`
    - degrades gracefully to unavailable/no-op behavior when artifacts are absent
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_ensemble_specialist.py -q -k adapter</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/prediction/specialist.py exists and exports runtime adapter types
    - missing artifacts return safe no-op behavior rather than crashing
  </acceptance_criteria>
  <done>Specialist runtime adapter exists and can be loaded independently from the core ensemble.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate specialist overlay into EnsemblePredictor behind governance-safe guardrails</name>
  <files>ai_engine/prediction/ensemble.py, ai_engine/prediction/specialist.py, tests/test_ensemble_specialist.py</files>
  <read_first>
    - ai_engine/prediction/ensemble.py
    - ai_engine/prediction/specialist.py
    - .planning/phases/12.1-ai-confidence-calibration-decision-governance/12.1-02-PLAN.md
  </read_first>
  <action>
    Extend final aggregation so it logs:
    - core score / action
    - specialist score / confidence / reason
    - specialist effect and final gate decision

    Keep runtime influence governance-compatible:
    - no specialist-only trade creation
    - disabled/missing specialist preserves current core behavior
    - if 12.1 governance interface is not yet implemented, specialist influence remains disabled/no-op while logging still works
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_ensemble_specialist.py -q</automated>
  </verify>
  <acceptance_criteria>
    - final aggregation contains specialist fields without breaking existing keys
    - disabled baseline equality tests pass
    - no-trade-alone tests pass
  </acceptance_criteria>
  <done>Runtime ensemble path can log and optionally host the specialist overlay without corrupting the core baseline.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ensemble_specialist.py -q`
- `python -c "from ai_engine.prediction.specialist import SpecialistRuntime; print('specialist runtime OK')"`
</verification>

<success_criteria>
- AITRAIN-04 addressed by explicit specialist runtime fields and final aggregation logging
- Baseline behavior is preserved when specialist is disabled or unavailable
- Specialist runtime remains blocked from standalone trading before governance is present
</success_criteria>

<output>
After completion, create `.planning/phases/12.3-ai-indicator-specialist-training/12.3-03-SUMMARY.md`.
</output>
