---
phase: 12.3-ai-indicator-specialist-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/features/market_structure_liquidity.py
  - ai_engine/features/feature_engineer.py
  - tests/test_market_structure_liquidity.py
autonomous: true
requirements:
  - AITRAIN-01
must_haves:
  truths:
    - "Specialist feature block is deterministic, causal, and versionable"
    - "Market-structure/liquidity features reuse confirmed structure semantics instead of future-looking pivots"
    - "Feature block exposes explicit FEATURE_NAMES and can be computed reproducibly"
  artifacts:
    - path: "ai_engine/features/market_structure_liquidity.py"
      provides: "MarketStructureLiquidityFeatures feature family"
      exports: ["MarketStructureLiquidityFeatures"]
    - path: "tests/test_market_structure_liquidity.py"
      provides: "Leakage and determinism coverage for specialist features"
  key_links:
    - from: "ai_engine/features/market_structure_liquidity.py"
      to: "ai_engine/features/support_resistance.py"
      via: "reuses confirmed swing / structure logic patterns"
      pattern: "FEATURE_NAMES"
    - from: "ai_engine/features/feature_engineer.py"
      to: "ai_engine/features/market_structure_liquidity.py"
      via: "registers specialist feature family without breaking existing outputs"
      pattern: "FeatureEngineer"
---

<objective>
Create the `market_structure_liquidity` specialist feature block with explicit causal/leakage discipline and hook it into the feature pipeline in a way that remains reproducible and versionable.

Purpose: AITRAIN-01 is the foundation. Without a deterministic feature family and leakage tests, all later specialist training claims are invalid.

Output: new feature module, feature-engineer integration, and focused tests for determinism, causality, and manifestable feature names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.3-ai-indicator-specialist-training/12.3-RESEARCH.md
@.planning/phases/12.3-ai-indicator-specialist-training/12.3-VALIDATION.md
@.planning/phases/12.3-ai-indicator-specialist-training/12.3-PATTERNS.md
@ai_engine/features/support_resistance.py
@ai_engine/features/microstructure_features.py
@ai_engine/features/feature_engineer.py
</context>

<threat_model>
- T-12.3-01: Silent feature leakage from future swing/FVG state
  mitigation: use confirmed or shifted event semantics and future-row mutation tests
- T-12.3-04: Specialist disabled path accidentally mutates baseline feature outputs
  mitigation: additive integration only, with exact disabled/no-op assertions
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for specialist feature determinism and leakage</name>
  <files>tests/test_market_structure_liquidity.py</files>
  <read_first>
    - .planning/phases/12.3-ai-indicator-specialist-training/12.3-VALIDATION.md
    - .planning/phases/12.3-ai-indicator-specialist-training/12.3-RESEARCH.md
    - ai_engine/features/support_resistance.py
  </read_first>
  <action>
    Add focused tests for:
    - deterministic output columns and `FEATURE_NAMES`
    - future-row mutation safety for event-like features
    - fair-value-gap / liquidity-sweep edge cases
    - integration into the feature-engineer path without breaking baseline columns
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_market_structure_liquidity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_market_structure_liquidity.py exists with at least 4 test functions
    - at least one test mutates future rows and asserts unchanged historical specialist features
    - tests assert explicit feature-column names
  </acceptance_criteria>
  <done>Wave-0 tests define the leakage-safe specialist feature contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MarketStructureLiquidityFeatures</name>
  <files>ai_engine/features/market_structure_liquidity.py, tests/test_market_structure_liquidity.py</files>
  <read_first>
    - ai_engine/features/support_resistance.py
    - ai_engine/features/microstructure_features.py
    - .planning/phases/12.3-ai-indicator-specialist-training/12.3-RESEARCH.md
  </read_first>
  <action>
    Implement a dedicated feature family for:
    - confirmed swing distance / age / range context
    - liquidity sweep flags and wick ratios
    - fair-value-gap style structure context using only causal data

    The module must expose explicit `FEATURE_NAMES` and a deterministic `calculate(df)` interface.
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_market_structure_liquidity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/features/market_structure_liquidity.py exists and exports MarketStructureLiquidityFeatures
    - module defines explicit FEATURE_NAMES list
    - `python -m pytest tests/test_market_structure_liquidity.py -q` exits 0
  </acceptance_criteria>
  <done>Specialist feature family exists and passes leakage/determinism tests.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate specialist feature family into feature pipeline</name>
  <files>ai_engine/features/feature_engineer.py, ai_engine/features/market_structure_liquidity.py, tests/test_market_structure_liquidity.py</files>
  <read_first>
    - ai_engine/features/feature_engineer.py
    - ai_engine/features/market_structure_liquidity.py
    - ai_engine/features/support_resistance.py
  </read_first>
  <action>
    Wire the specialist feature family into the existing feature-engineer path in a way that allows:
    - opt-in use for specialist training
    - stable baseline behavior when the specialist block is not requested
    - explicit access to the specialist feature list for version manifests
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_market_structure_liquidity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - feature engineer can expose the specialist feature family without breaking existing feature paths
    - specialist feature list can be serialized into a feature manifest later
  </acceptance_criteria>
  <done>Specialist feature block is usable by later training/runtime stages without mutating baseline outputs.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_market_structure_liquidity.py -q`
- `python -c "from ai_engine.features.market_structure_liquidity import MarketStructureLiquidityFeatures; print('specialist features OK')"`
</verification>

<success_criteria>
- AITRAIN-01 addressed by a reproducible and causal specialist feature block
- Leakage tests exist and fail if future rows influence historical specialist features
- Feature pipeline can access specialist columns without altering baseline behavior
</success_criteria>

<output>
After completion, create `.planning/phases/12.3-ai-indicator-specialist-training/12.3-01-SUMMARY.md`.
</output>
