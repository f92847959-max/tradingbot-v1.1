---
phase: 16-channel-formation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ai_engine/features/geometric_features.py, tests/test_geometric_features.py]
autonomous: true
requirements: [CHAN-01]
must_haves:
  truths:
    - "Linear Regression channel slope is calculated within 5% error of theoretical OLS"
    - "R² score correctly represents trend strength (0.0 to 1.0)"
    - "Channel bands (Upper/Lower) adapt dynamically to price volatility via Standard Deviation"
  artifacts:
    - path: "ai_engine/features/geometric_features.py"
      provides: "Linear Regression Channel implementation"
    - path: "tests/test_geometric_features.py"
      provides: "Unit tests for statistical features"
  key_links:
    - from: "ai_engine/features/geometric_features.py"
      to: "scipy.stats.linregress"
      via: "core fitting engine"
---

<objective>
Implement the Statistical Layer of the Channel Formation engine. This focuses on Linear Regression Channels to identify the primary market regime (slope) and trend reliability (R²).

Purpose: Provide the AI with statistically robust trend metrics and overextension levels.
Output: Initial `GeometricFeatures` class with LinReg logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/16kanalbioldung/16-RESEARCH.md

<interfaces>
From scipy.stats:
```python
def linregress(x, y=None):
    # Returns slope, intercept, rvalue, pvalue, stderr
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Linear Regression Channel</name>
  <files>ai_engine/features/geometric_features.py</files>
  <action>
    Create `ai_engine/features/geometric_features.py`.
    Implement `GeometricFeatures` class.
    Implement `calculate_lr_channel(self, prices: pd.Series, lookback: int = 50)`:
    - Use `scipy.stats.linregress` on a rolling window or fixed tail.
    - Calculate `midline = intercept + slope * x`.
    - Calculate `std_dev = np.std(prices - midline)`.
    - Return a dictionary or namedtuple with: `slope`, `r_squared`, `upper_band`, `lower_band`.
    - Ensure features are shift(1) to avoid look-ahead bias if used in feature engineering.
    - Handle edge cases (e.g., constant prices) to avoid division by zero.
  </action>
  <verify>
    <automated>python -c "from ai_engine.features.geometric_features import GeometricFeatures; print('GeometricFeatures created')"</automated>
  </verify>
  <done>Linear Regression Channel logic implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit Tests for LinReg Channels</name>
  <files>tests/test_geometric_features.py</files>
  <behavior>
    - Test 1: Linear upward trend (1, 2, 3...) returns positive slope and R² ≈ 1.0.
    - Test 2: Random noise returns R² close to 0.0.
    - Test 3: Standard deviation bands correctly contain ~95% of data points in a normal distribution if 2.0 multiplier is used.
  </behavior>
  <action>
    Create `tests/test_geometric_features.py`.
    Write tests for `calculate_lr_channel` using `numpy` generated synthetic data.
  </action>
  <verify>
    <automated>pytest tests/test_geometric_features.py</automated>
  </verify>
  <done>LinReg features verified with unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Data Intake | OHLCV prices must be validated for NaNs before passing to linregress. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Availability | geometric_features.py | mitigate | Clamp `lookback` to a maximum of 500 bars to prevent expensive OLS calculations on large windows. |
</threat_model>

<verification>
`pytest tests/test_geometric_features.py` passes all tests.
</verification>

<success_criteria>
Linear Regression channels correctly identify trend slope and strength on synthetic and historical-like data.
</success_criteria>

<output>
After completion, create `.planning/phases/16-channel-formation/16-01-SUMMARY.md`
</output>
