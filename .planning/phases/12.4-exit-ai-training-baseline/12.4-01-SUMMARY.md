---
phase: 12.4-exit-ai-training-baseline
plan: 01
subsystem: ai_engine.training
tags: [exit-ai, dataset, labels, smart-exit]
requirements: [EXITAI-01]
key_files:
  created:
    - ai_engine/training/exit_ai_labels.py
    - tests/test_exit_ai_labels.py
tests:
  command: "python -m pytest tests/test_exit_ai_labels.py -q"
  result: "4 passed"
completed: "2026-04-25T08:45:00.000Z"
---

# Phase 12.4 Plan 01 Summary: Exit-AI Dataset

Built the causal Exit-AI snapshot layer for trade-management decisions.

One-liner: `ExitSnapshotBuilder` now turns timestamped open-position context plus Smart Exit Engine outputs into bounded `HOLD`, `TIGHTEN_SL`, `PARTIAL_CLOSE`, and `FULL_EXIT` training samples with manifest and class-balance metadata.

## What Changed

- Added `ai_engine/training/exit_ai_labels.py` with deterministic snapshot generation, baseline dynamic-SL/trailing/partial-close context, feature schema, and action manifest.
- Added tests proving deterministic sample output, future-outcome mutation safety, exact action-space boundaries, and unsafe action rejection.

## Verification

- `python -m pytest tests/test_exit_ai_labels.py -q` -> `4 passed`
