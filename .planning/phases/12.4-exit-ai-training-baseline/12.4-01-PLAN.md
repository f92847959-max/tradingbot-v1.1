---
phase: 12.4-exit-ai-training-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_engine/training/exit_ai_labels.py
  - tests/test_exit_ai_labels.py
autonomous: true
requirements:
  - EXITAI-01
must_haves:
  truths:
    - "Exit-AI samples are generated from timestamped trade-management snapshots, not post-hoc summaries"
    - "Allowed actions are strictly HOLD, TIGHTEN_SL, PARTIAL_CLOSE, FULL_EXIT"
    - "Baseline exit-engine outputs are reused as deterministic context instead of duplicated heuristics"
  artifacts:
    - path: "ai_engine/training/exit_ai_labels.py"
      provides: "Exit snapshot builder and action-label generation"
      exports: ["ExitSnapshotBuilder", "build_exit_training_samples"]
    - path: "tests/test_exit_ai_labels.py"
      provides: "Causality, determinism, and action-manifest coverage for Exit-AI samples"
  key_links:
    - from: "ai_engine/training/exit_ai_labels.py"
      to: "exit_engine/dynamic_sl.py"
      via: "reuses tighten/stop context from deterministic baseline"
      pattern: "calculate_dynamic_sl"
    - from: "ai_engine/training/exit_ai_labels.py"
      to: "exit_engine/partial_close.py"
      via: "reuses one-shot partial-close semantics"
      pattern: "evaluate_partial_close"
---

<objective>
Create the causal Exit-AI dataset layer: timestamped trade-management snapshots, constrained action labels, and explicit manifests for the action space.

Purpose: 12.4 cannot produce a credible specialist model until the repo has a deterministic, leakage-safe dataset builder rooted in the current Smart Exit Engine.

Output: snapshot/label module plus focused tests proving causality, determinism, and safe action-set boundaries.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-RESEARCH.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-VALIDATION.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-PATTERNS.md
@exit_engine/dynamic_sl.py
@exit_engine/dynamic_tp.py
@exit_engine/trailing_manager.py
@exit_engine/partial_close.py
@order_management/order_manager.py
</context>

<threat_model>
- T-12.4-01: Exit labels leak future trade outcome into model inputs
  mitigation: explicit snapshot-time builder, future-row mutation tests, and separate target construction
- T-12.4-05: Action schema silently expands into unsafe decisions
  mitigation: persisted action manifest and hard assertions for allowed action set
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for causal Exit-AI snapshots and action manifest</name>
  <files>tests/test_exit_ai_labels.py</files>
  <read_first>
    - .planning/phases/12.4-exit-ai-training-baseline/12.4-VALIDATION.md
    - exit_engine/dynamic_sl.py
    - exit_engine/partial_close.py
  </read_first>
  <action>
    Add focused tests for:
    - deterministic sample generation from identical trade/candle inputs
    - future-row mutation safety for historical snapshots
    - explicit allowed action set (`HOLD`, `TIGHTEN_SL`, `PARTIAL_CLOSE`, `FULL_EXIT`)
    - rejection of unsafe actions such as stop widening or new-entry outputs
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_labels.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_exit_ai_labels.py exists with at least 4 test functions
    - at least one test mutates future rows and asserts unchanged historical samples
    - tests assert the exact allowed action set
  </acceptance_criteria>
  <done>Wave-0 tests define the Exit-AI dataset contract and safe action boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Exit snapshot builder and label generation</name>
  <files>ai_engine/training/exit_ai_labels.py, tests/test_exit_ai_labels.py</files>
  <read_first>
    - exit_engine/dynamic_sl.py
    - exit_engine/dynamic_tp.py
    - exit_engine/trailing_manager.py
    - exit_engine/partial_close.py
    - order_management/order_manager.py
  </read_first>
  <action>
    Implement a dedicated Exit-AI dataset module that:
    - builds timestamped trade-management snapshots from open positions and recent candles
    - derives baseline-aware context fields from the deterministic exit engine
    - emits constrained action labels and an `exit_action_manifest`
    - stays reusable for walk-forward training without depending on live broker state
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_labels.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/training/exit_ai_labels.py exists and exports a snapshot builder API
    - generated samples include action labels plus baseline exit context
    - `python -m pytest tests/test_exit_ai_labels.py -q` exits 0
  </acceptance_criteria>
  <done>Exit-AI samples are reproducible, causal, and bounded to the safe action space.</done>
</task>

<task type="auto">
  <name>Task 3: Persist sample schema and class-balance metadata for downstream training</name>
  <files>ai_engine/training/exit_ai_labels.py, tests/test_exit_ai_labels.py</files>
  <read_first>
    - ai_engine/training/exit_ai_labels.py
    - .planning/phases/12.4-exit-ai-training-baseline/12.4-RESEARCH.md
  </read_first>
  <action>
    Extend the dataset module so it can emit:
    - explicit feature/action manifest metadata
    - class-distribution summaries for HOLD-vs-action skew
    - machine-readable schema metadata consumable by the training pipeline
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_labels.py -q</automated>
  </verify>
  <acceptance_criteria>
    - action manifest and feature schema are serializable
    - class-balance summary is available for promotion-gate checks later
  </acceptance_criteria>
  <done>Downstream training can consume an explicit, auditable Exit-AI dataset schema.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_exit_ai_labels.py -q`
- `python -c "from ai_engine.training.exit_ai_labels import build_exit_training_samples; print('exit ai labels OK')"`
</verification>

<success_criteria>
- EXITAI-01 addressed by a causal, deterministic Exit-AI dataset layer
- Unsafe action types are excluded by manifest and tests
- Sample schema is explicit enough for isolated training/versioning in the next wave
</success_criteria>

<output>
After completion, create `.planning/phases/12.4-exit-ai-training-baseline/12.4-01-SUMMARY.md`.
</output>
