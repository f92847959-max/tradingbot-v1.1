---
phase: 12.4-exit-ai-training-baseline
plan: 02
type: execute
wave: 2
depends_on: [12.4-01]
files_modified:
  - ai_engine/training/exit_ai_pipeline.py
  - ai_engine/training/model_versioning.py
  - tests/test_exit_ai_model.py
  - tests/test_exit_ai_walk_forward.py
autonomous: true
requirements:
  - EXITAI-02
  - EXITAI-03
must_haves:
  truths:
    - "Exit-AI artifacts are stored under a specialist-local root and never overwrite core or other specialist artifacts"
    - "Baseline and Exit-AI candidate are evaluated on identical walk-forward windows"
    - "Per-window fit/save/report discipline stays leakage-safe and reproducible"
  artifacts:
    - path: "ai_engine/training/exit_ai_pipeline.py"
      provides: "Exit-AI training and walk-forward comparison pipeline"
      exports: ["train_exit_ai_specialist", "compare_exit_ai_to_baseline"]
    - path: "tests/test_exit_ai_model.py"
      provides: "Specialist artifact save/load and manifest validation"
    - path: "tests/test_exit_ai_walk_forward.py"
      provides: "Baseline-vs-specialist walk-forward comparison tests"
  key_links:
    - from: "ai_engine/training/exit_ai_pipeline.py"
      to: "ai_engine/training/walk_forward.py"
      via: "reuses expanding-window and train-only scaling discipline"
      pattern: "WalkForwardValidator"
    - from: "ai_engine/training/model_versioning.py"
      to: "ai_engine/saved_models/specialists/exit_ai"
      via: "specialist-local production pointer and version directories"
      pattern: "production.json"
---

<objective>
Train the Exit-AI specialist, save its artifacts in an isolated root, and prove it against the deterministic Smart Exit baseline with walk-forward parity.

Purpose: 12.4 only has value if the model can be trained and compared without corrupting existing artifact roots or inflating claims through mismatched windows.

Output: Exit-AI training pipeline, specialist-local artifact storage, and comparison tests/reports.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-RESEARCH.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-VALIDATION.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-PATTERNS.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-01-PLAN.md
@ai_engine/training/walk_forward.py
@ai_engine/training/model_versioning.py
@ai_engine/training/pipeline.py
</context>

<threat_model>
- T-12.4-02: Exit-AI artifacts collide with core or other specialist artifacts
  mitigation: dedicated storage root, manifest checks, and isolated pointer helpers
- T-12.4-03: Comparison claims are inflated by changed windows or scalers
  mitigation: run baseline and candidate on identical walk-forward splits with the same reporting schema
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for isolated artifact storage and walk-forward parity</name>
  <files>tests/test_exit_ai_model.py, tests/test_exit_ai_walk_forward.py</files>
  <read_first>
    - ai_engine/training/model_versioning.py
    - ai_engine/training/walk_forward.py
    - .planning/phases/12.4-exit-ai-training-baseline/12.4-VALIDATION.md
  </read_first>
  <action>
    Add tests for:
    - save/load under `ai_engine/saved_models/specialists/exit_ai`
    - manifest mismatch rejection
    - baseline-only and baseline+Exit-AI runs using identical walk-forward windows
    - report schema including protection and retained-upside metrics
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_model.py tests/test_exit_ai_walk_forward.py -q</automated>
  </verify>
  <acceptance_criteria>
    - both test files exist
    - at least one test asserts that the baseline control run uses identical windows
    - at least one test asserts specialist-root artifact isolation
  </acceptance_criteria>
  <done>Wave-0 tests define isolated storage and fair comparison behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Exit-AI training pipeline and isolated versioning</name>
  <files>ai_engine/training/exit_ai_pipeline.py, ai_engine/training/model_versioning.py, tests/test_exit_ai_model.py</files>
  <read_first>
    - ai_engine/training/pipeline.py
    - ai_engine/training/model_versioning.py
    - .planning/phases/12.4-exit-ai-training-baseline/12.4-RESEARCH.md
  </read_first>
  <action>
    Implement a dedicated training pipeline that:
    - consumes the 12.4 snapshot dataset
    - trains a separate Exit-AI model and scaler
    - saves versioned artifacts under a specialist-local root
    - persists feature/action manifests and version metadata
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_model.py -q</automated>
  </verify>
  <acceptance_criteria>
    - specialist artifacts save/load from a dedicated root
    - no core or 12.3 specialist artifact is overwritten
    - `python -m pytest tests/test_exit_ai_model.py -q` exits 0
  </acceptance_criteria>
  <done>Exit-AI specialist artifacts are reproducible and isolated from existing model roots.</done>
</task>

<task type="auto">
  <name>Task 3: Implement walk-forward baseline comparison and report output</name>
  <files>ai_engine/training/exit_ai_pipeline.py, tests/test_exit_ai_walk_forward.py</files>
  <read_first>
    - ai_engine/training/walk_forward.py
    - ai_engine/training/exit_ai_pipeline.py
    - .planning/phases/12.4-exit-ai-training-baseline/12.4-RESEARCH.md
  </read_first>
  <action>
    Extend the pipeline so it can execute:
    - baseline Smart-Exit control runs
    - baseline+Exit-AI candidate runs on the same windows
    - comparative reporting for drawdown containment, upside retention, early-exit cost, and trade retention
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_model.py tests/test_exit_ai_walk_forward.py -q</automated>
  </verify>
  <acceptance_criteria>
    - report output contains baseline and candidate metrics side by side
    - identical walk-forward windows are enforced for both paths
    - comparison result can be consumed by the promotion gate in the next plan
  </acceptance_criteria>
  <done>Exit-AI performance claims are grounded in leakage-safe baseline comparison.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_exit_ai_model.py tests/test_exit_ai_walk_forward.py -q`
- `python -c "from ai_engine.training.exit_ai_pipeline import train_exit_ai_specialist; print('exit ai pipeline OK')"`
</verification>

<success_criteria>
- EXITAI-02 addressed by isolated Exit-AI artifact storage and loadable versions
- EXITAI-03 addressed by fair baseline-vs-candidate walk-forward comparison
- Training outputs are auditable and ready for calibration/promotion gating
</success_criteria>

<output>
After completion, create `.planning/phases/12.4-exit-ai-training-baseline/12.4-02-SUMMARY.md`.
</output>
