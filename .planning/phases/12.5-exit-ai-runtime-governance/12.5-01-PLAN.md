---
phase: 12.5-exit-ai-runtime-governance
plan: 01
type: execute
wave: 1
depends_on: [12.4-01, 12.4-02, 12.4-03]
files_modified:
  - ai_engine/prediction/exit_ai_advisor.py
  - tests/test_exit_ai_runtime.py
autonomous: true
requirements:
  - EXITAI-05
  - EXITAI-06
must_haves:
  truths:
    - "Runtime Exit-AI can only propose risk-reducing actions"
    - "Missing artifacts, disabled config, or invalid proposals degrade to no-op"
    - "Advisor output stays structured and bounded before any broker-side integration"
  artifacts:
    - path: "ai_engine/prediction/exit_ai_advisor.py"
      provides: "Exit-AI runtime loader and bounded action contract"
      exports: ["ExitAIAdvisor", "ExitAIRecommendation"]
    - path: "tests/test_exit_ai_runtime.py"
      provides: "No-risk-widening, no-entry, and fallback coverage"
  key_links:
    - from: "ai_engine/prediction/exit_ai_advisor.py"
      to: "ai_engine/saved_models/specialists/exit_ai/production.json"
      via: "loads specialist-local production pointer"
      pattern: "production.json"
    - from: "ai_engine/prediction/exit_ai_advisor.py"
      to: "exit_engine/trailing_manager.py"
      via: "reuses monotonic stop-tightening semantics"
      pattern: "SmartTrailingManager"
---

<objective>
Create the runtime Exit-AI advisor layer that loads 12.4 artifacts and emits bounded, risk-reducing recommendations with strict no-op fallback.

Purpose: Before touching `OrderManager`, the repo needs a small, testable runtime contract proving that Exit-AI can only suggest safe actions and never become hidden entry logic.

Output: runtime advisor module plus focused guardrail tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-RESEARCH.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-VALIDATION.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-PATTERNS.md
@.planning/phases/12.4-exit-ai-training-baseline/12.4-03-PLAN.md
@exit_engine/trailing_manager.py
@exit_engine/partial_close.py
</context>

<threat_model>
- T-12.5-01: Advisor widens risk or emits entry actions
  mitigation: bounded action manifest, monotonic stop checks, and no-entry tests
- T-12.5-02: Missing artifacts crash runtime management
  mitigation: no-op fallback and missing-artifact tests
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for bounded Exit-AI runtime recommendations</name>
  <files>tests/test_exit_ai_runtime.py</files>
  <read_first>
    - .planning/phases/12.5-exit-ai-runtime-governance/12.5-VALIDATION.md
    - exit_engine/trailing_manager.py
    - exit_engine/partial_close.py
  </read_first>
  <action>
    Add tests for:
    - allowed runtime actions only
    - stop-tightening proposals must be monotonic
    - no new-entry or reverse-position outputs
    - missing-artifact and disabled-mode no-op fallback
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_runtime.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_exit_ai_runtime.py exists with at least 4 test functions
    - at least one test asserts no-op fallback when artifacts are missing
    - at least one test rejects risk-widening stop proposals
  </acceptance_criteria>
  <done>Runtime guardrails are defined before advisor implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Exit-AI runtime advisor and bounded recommendation schema</name>
  <files>ai_engine/prediction/exit_ai_advisor.py, tests/test_exit_ai_runtime.py</files>
  <read_first>
    - .planning/phases/12.4-exit-ai-training-baseline/12.4-02-PLAN.md
    - .planning/phases/12.5-exit-ai-runtime-governance/12.5-RESEARCH.md
  </read_first>
  <action>
    Implement a runtime advisor that:
    - loads Exit-AI artifacts from the specialist root
    - evaluates current open-position context
    - emits a structured recommendation object with bounded action, confidence, reason, and optional stop/close payload
    - degrades to no-op when artifacts or config are missing
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_runtime.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ai_engine/prediction/exit_ai_advisor.py exists and exports a runtime advisor API
    - advisor recommendations never include entry or risk-widening actions
    - `python -m pytest tests/test_exit_ai_runtime.py -q` exits 0
  </acceptance_criteria>
  <done>Exit-AI runtime contract is safe, explicit, and no-op capable.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_exit_ai_runtime.py -q`
- `python -c "from ai_engine.prediction.exit_ai_advisor import ExitAIAdvisor; print('exit ai advisor OK')"`
</verification>

<success_criteria>
- EXITAI-05 addressed by hard runtime guardrails
- EXITAI-06 partially addressed by advisor no-op fallback and bounded contract
- Exit-AI runtime is ready to be wired into `OrderManager` without bypassing existing side-effect paths
</success_criteria>

<output>
After completion, create `.planning/phases/12.5-exit-ai-runtime-governance/12.5-01-SUMMARY.md`.
</output>
