---
phase: 12.5-exit-ai-runtime-governance
plan: 02
type: execute
wave: 2
depends_on: [12.5-01]
files_modified:
  - order_management/order_manager.py
  - order_management/position_monitor.py
  - tests/test_order_manager_exit_ai.py
autonomous: true
requirements:
  - EXITAI-05
  - EXITAI-06
must_haves:
  truths:
    - "All broker-side effects still route through OrderManager"
    - "Exit-AI actions are applied only through existing modify/close/partial-close pathways"
    - "No-op fallback remains intact when runtime advisor is disabled or unavailable"
  artifacts:
    - path: "order_management/order_manager.py"
      provides: "Governed application of Exit-AI tighten/partial/full-exit actions"
    - path: "tests/test_order_manager_exit_ai.py"
      provides: "Integration coverage for Exit-AI application in the trade-management loop"
  key_links:
    - from: "order_management/order_manager.py"
      to: "ai_engine/prediction/exit_ai_advisor.py"
      via: "consumes bounded runtime recommendation objects"
      pattern: "ExitAIAdvisor"
    - from: "order_management/order_manager.py"
      to: "exit_engine/partial_close.py"
      via: "reuses one-shot partial-close behavior"
      pattern: "PartialCloseManager"
---

<objective>
Integrate Exit-AI runtime recommendations into the live order-management loop using the existing broker-side execution paths.

Purpose: 12.5 only becomes real once `OrderManager` can evaluate open positions, apply safe recommendations, and preserve the repo's existing side-effect and reconciliation discipline.

Output: `OrderManager`/`PositionMonitor` integration plus focused integration tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-RESEARCH.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-VALIDATION.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-PATTERNS.md
@order_management/order_manager.py
@order_management/position_monitor.py
@exit_engine/partial_close.py
</context>

<threat_model>
- T-12.5-03: Exit-AI bypasses centralized broker/DB orchestration
  mitigation: apply all side effects only through existing OrderManager methods
- T-12.5-01: Direction/sign bugs widen stop loss or misuse partial close
  mitigation: direction-aware assertions plus integration tests for BUY and SELL paths
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED integration tests for Exit-AI application inside OrderManager</name>
  <files>tests/test_order_manager_exit_ai.py</files>
  <read_first>
    - order_management/order_manager.py
    - order_management/position_monitor.py
    - .planning/phases/12.5-exit-ai-runtime-governance/12.5-VALIDATION.md
  </read_first>
  <action>
    Add tests for:
    - tighten-stop recommendation applied via existing modify path
    - partial-close recommendation applied only once
    - full-exit recommendation routed through existing close path
    - disabled or missing advisor leaves existing behavior unchanged
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_order_manager_exit_ai.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_order_manager_exit_ai.py exists with at least 4 test functions
    - at least one test asserts no behavior change when advisor is unavailable
    - at least one test verifies partial close is not duplicated
  </acceptance_criteria>
  <done>Integration expectations are locked before wiring broker-side paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Exit-AI recommendations into periodic position management</name>
  <files>order_management/order_manager.py, order_management/position_monitor.py, tests/test_order_manager_exit_ai.py</files>
  <read_first>
    - order_management/order_manager.py
    - ai_engine/prediction/exit_ai_advisor.py
    - exit_engine/partial_close.py
  </read_first>
  <action>
    Integrate the advisor into the existing management loop so that:
    - open positions can be evaluated for tighten/partial/full-exit actions
    - every broker-side effect still runs through `modify_position`, partial-close tracking, or `close_trade`
    - disabled/missing-advisor paths remain unchanged
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_order_manager_exit_ai.py -q</automated>
  </verify>
  <acceptance_criteria>
    - OrderManager can consume advisor recommendations without direct broker calls from the advisor
    - runtime no-op fallback preserves baseline behavior
    - `python -m pytest tests/test_order_manager_exit_ai.py -q` exits 0
  </acceptance_criteria>
  <done>Exit-AI recommendations are applied through the repo's established trade-management channels.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_order_manager_exit_ai.py -q`
</verification>

<success_criteria>
- EXITAI-05 addressed by guarded application of tighten/partial/full-exit actions only
- EXITAI-06 addressed by real integration through existing order-management paths with no-op fallback
- Live runtime remains centralized around OrderManager and PositionMonitor
</success_criteria>

<output>
After completion, create `.planning/phases/12.5-exit-ai-runtime-governance/12.5-02-SUMMARY.md`.
</output>
