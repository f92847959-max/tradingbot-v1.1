---
phase: 12.5-exit-ai-runtime-governance
plan: 03
type: execute
wave: 3
depends_on: [12.5-01, 12.5-02]
files_modified:
  - order_management/order_manager.py
  - tests/test_exit_ai_governance.py
autonomous: true
requirements:
  - EXITAI-07
  - EXITAI-08
must_haves:
  truths:
    - "Exit-AI runtime decisions are auditable against baseline context and applied broker outcome"
    - "Drift and reconciliation data survive modify/close failure branches"
    - "Operator-visible gate reasons explain why recommendations were applied, skipped, or rejected"
  artifacts:
    - path: "tests/test_exit_ai_governance.py"
      provides: "Audit, drift, and reconciliation coverage for Exit-AI runtime behavior"
  key_links:
    - from: "order_management/order_manager.py"
      to: "database.repositories.trade_repo.OrderLogRepository"
      via: "extends existing order-log path with Exit-AI audit payloads"
      pattern: "log_action"
    - from: "order_management/order_manager.py"
      to: "order_management/position_monitor.py"
      via: "preserves recommendation context across close/retry paths"
      pattern: "reconciliation"
---

<objective>
Add audit logging, drift monitoring, and reconciliation-aware persistence for Exit-AI runtime behavior.

Purpose: Exit-AI should be explainable after the fact. Operators and future retraining loops must be able to see what the baseline said, what Exit-AI recommended, what was executed, and how the trade resolved.

Output: runtime audit/drift instrumentation plus focused governance tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-RESEARCH.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-VALIDATION.md
@.planning/phases/12.5-exit-ai-runtime-governance/12.5-PATTERNS.md
@order_management/order_manager.py
@order_management/position_monitor.py
</context>

<threat_model>
- T-12.5-04: Audit payload is too weak for retraining or operator review
  mitigation: structured logs with baseline, recommendation, action result, and final outcome
- T-12.5-05: Reconciliation loses Exit-AI context across broker/DB failure paths
  mitigation: preserve recommendation context in retry-aware logging and tests
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for Exit-AI audit payloads and drift visibility</name>
  <files>tests/test_exit_ai_governance.py</files>
  <read_first>
    - .planning/phases/12.5-exit-ai-runtime-governance/12.5-VALIDATION.md
    - order_management/order_manager.py
  </read_first>
  <action>
    Add tests for:
    - audit payload includes baseline context, recommendation, confidence, and outcome
    - drift record differentiates baseline recommendation vs Exit-AI recommendation vs applied action
    - modify/close failure branches preserve enough context for reconciliation
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_governance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_exit_ai_governance.py exists with at least 4 test functions
    - at least one test covers a failure/retry branch
    - at least one test validates audit payload schema
  </acceptance_criteria>
  <done>Audit and drift expectations are fixed before instrumentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument Exit-AI audit logging and reconciliation context</name>
  <files>order_management/order_manager.py, tests/test_exit_ai_governance.py</files>
  <read_first>
    - order_management/order_manager.py
    - order_management/position_monitor.py
    - .planning/phases/12.5-exit-ai-runtime-governance/12.5-RESEARCH.md
  </read_first>
  <action>
    Extend runtime management so it records:
    - baseline exit context
    - Exit-AI recommendation and confidence
    - whether the recommendation was applied, skipped, or rejected
    - resulting broker-side and reconciliation context
  </action>
  <verify>
    <automated>cd "C:/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold" && python -m pytest tests/test_exit_ai_governance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - audit payload is serializable and operator-readable
    - failure/retry branches preserve recommendation context
    - `python -m pytest tests/test_exit_ai_governance.py -q` exits 0
  </acceptance_criteria>
  <done>Exit-AI runtime becomes explainable and reconciliation-aware.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_exit_ai_governance.py -q`
</verification>

<success_criteria>
- EXITAI-07 addressed by structured audit logging of Exit-AI runtime decisions
- EXITAI-08 addressed by drift/reconciliation visibility across success and failure paths
- Operators can explain how Exit-AI affected or did not affect each managed trade
</success_criteria>

<output>
After completion, create `.planning/phases/12.5-exit-ai-runtime-governance/12.5-03-SUMMARY.md`.
</output>
