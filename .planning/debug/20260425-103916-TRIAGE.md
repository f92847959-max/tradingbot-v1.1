---
status: WARNINGS
timestamp: 20260425-103916
depth: deep
counts:
  critical: 0
  high: 0
  medium: 4
  low: 4
  info: 1
checks_run: 9 (git-check, log-pattern, build-check, config-check, path-length, gitignore-gaps, stale-files, deps-pinning, ci-pipeline)
---

# Autonom Debug Triage — 20260425-103916

**Stack:** python | **Depth:** deep | **Status:** WARNINGS

## Executive Summary

- Critical: 0 | High: 0 | Medium: 4 | Low: 4 | Info: 1
- Build: **GREEN** — pytest 631 passed, 1 skipped (sentiment placeholders ignored)
- All 8 logic findings from earlier AUTONOM-DEBUG-REPORT confirmed fixed
- main.py:105 signal-handler hardened (idempotent, loop-closed-safe)
- Remaining issues are repo hygiene + Windows path lengths, not logic bugs

## Top-3 Actions (copy-paste)

1. `git mv ".planning/phases/14-elliott-wave-theorie-integration-..." ".planning/phases/14-elliott-wave"` — fix 228-char Windows MAX_PATH risk
2. Append local-tooling artifacts to `.gitignore`: `.claude/`, `.claude-flow/`, `.obsidian/`, `.mcp.json`, `pytest_*.txt`
3. `rm pytest_errors.txt pytest_failures.txt pytest_last_failure.txt .js` — clean stale files at repo root

## Findings (sorted by severity)

### [MEDIUM] 199 uncommitted files
- Evidence: `git status --porcelain | wc -l = 199`
- Source: git-check
- Next: `git status` — review and batch-commit Phase-12.x work; check no secrets slip in

### [MEDIUM] Phase-14 directory name 228 chars (Windows MAX_PATH risk)
- File: `.planning/phases/14-elliott-wave-theorie-integration-automatische-wellenzaehlung-1-5-impuls-a-b-c-korrektur-fibonacci-targets-aus-wellen-verhaeltnissen-wellen-position-als-ml-feature-integration-in-signal-generator-py-und-mirofish-seed-templates`
- Evidence: git status warning "Filename too long" — blocks full git scan
- Source: path-length
- Next: `git mv .planning/phases/14-elliott-wave-... .planning/phases/14-elliott-wave`

### [MEDIUM] .gitignore gaps: 5 local-tooling artifacts untracked
- File: `.gitignore`
- Evidence: `.claude/, .claude-flow/, .obsidian/, .mcp.json, pytest_*.txt` not in .gitignore but present in working tree
- Source: gitignore-gaps
- Next: append entries to .gitignore

### [MEDIUM] Recurring log msg: Traceback in last 7d
- File: `logs/`
- Evidence: 1 occurrence in last 7d
- Source: log-pattern
- Next: `grep "Traceback" logs/*.log | tail -5` — likely related to historical .env-missing era (resolved 2026-04-14, see TRIAGE-DEEP)

### [LOW] Stale pytest output files at repo root
- File: `pytest_*.txt`
- Evidence: pytest_errors.txt / pytest_failures.txt / pytest_last_failure.txt — headers show different machine (`C:\Users\roder\goldbot2`, Python 3.13), dated 2026-03-22
- Source: stale-files
- Next: `rm pytest_errors.txt pytest_failures.txt pytest_last_failure.txt`

### [LOW] Stray .js file at repo root
- File: `.js`
- Evidence: 1386-byte Obsidian plugin snippet, out-of-place at repo root
- Source: stale-files
- Next: review and `rm .js` (no hardcoded credentials per TRIAGE-DEEP scan)

### [LOW] 23/27 requirements.txt entries unpinned (>= without upper bound)
- File: `requirements.txt`
- Evidence: only `yfinance==1.1.0`, `shap==0.51.0`, `feedparser==6.0.12`, `vaderSentiment==3.3.2` are pinned. No lockfile.
- Source: deps-pinning
- Next: `pip-compile requirements.in` or adopt `uv`/`poetry`

### [LOW] No CI pipeline (.github/workflows missing)
- File: `.github/workflows/`
- Evidence: 57+ test files with no automated gate; no remote-tracked workflow
- Source: ci-pipeline
- Next: add `.github/workflows/tests.yml` running `pytest -q` on push

### [INFO] 142 unpushed commits
- Evidence: 142 local commits ahead of remote
- Source: git-check
- Next: `git push` after reviewing dirty files (`.gitignore` covers `.env*` — safe)

## Rule Engine Trace

- R1 no-match: CI=unavailable (no .github/workflows)
- R2 no-match: env_changes=0 (no .env touched in last 7d)
- R3 matched: dirty=199 → P2 git-check
- R4 matched: log_errors=1 → P2 log-pattern
- R5 skipped: no-lockfile detected (only requirements.txt)
- R6 no-match: no active GSD phase
- R7 matched: unpushed=142 → P3 (already P2 git-check)
- Forced inclusion: build-check (pytest, GREEN), config-check (env validation OK)
- Deep-mode extensions: path-length, gitignore-gaps, stale-files, deps-pinning, ci-pipeline

## Context Snapshot

```json
{
  "timestamp": "20260425-103916",
  "stacks": "python",
  "git": {
    "dirty_files": 199,
    "last_commit_age_h": "9",
    "unpushed_commits": 142
  },
  "ci_status": "unavailable",
  "env_changes_7d": 0,
  "deps_age_days": "no-lockfile",
  "log_error_count": 1,
  "gsd_active_phase": "",
  "depth": "deep"
}
```

## Recently Fixed in This Session (2026-04-25)

1. **`tests/conftest.py`** — added autouse fixture `_freeze_risk_clock_to_friday` that mocks `risk.risk_manager.datetime.now()` for `test_risk_manager.py` and `test_risk_integration_advanced.py`. Frees these 8+ tests from wall-clock dependency.
2. **`tests/test_risk.py`** — added `notional_value=10000.0, equity=10000.0` to `_run_all` defaults so the new leverage check evaluates correctly.
3. **`tests/test_risk_integration.py`** — updated stale `len(approval.checks) == 11` assertion to `== 12` (PreTradeChecker added leverage_exceeded).
4. **`tests/test_regime_integration.py`** — `_make_df` default `rows=30` → `rows=60` (StrategyManager.evaluate requires ≥50 candles before regime detection); added `ema_9`/`ema_21` columns expected by `strategy/multi_timeframe.py`.
5. **`tests/test_lifecycle.py`** — replaced `patch("main.close_db")` with `patch("trading.lifecycle.close_db")` (close_db lives in `database.connection` and is imported in `trading.lifecycle`, not `main`).
6. **`main.py:105`** — refactored signal handler: `(signum, frame)` renamed to `(_signum, _frame)`; added `shutdown_requested` idempotency guard so a second Ctrl+C doesn't spawn a second `system.stop()` task; added `loop.is_closed()` guard so signals arriving during teardown don't raise.

Test status after all fixes: **631 passed, 1 skipped, 0 failed** in `pytest tests/ --ignore=tests/sentiment`.
