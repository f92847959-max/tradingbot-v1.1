---
status: SUPERSEDED — see 20260424-131303-TRIAGE-DEEP.md
timestamp: 20260424-131303
depth: deep (initial pass, 36 additional iterations in DEEP addendum)
elapsed_s: ~30
counts_initial:
  critical: 2
  high: 1
  medium: 2
  low: 1
  info: 1
counts_revised:
  critical: 0
  high: 1
  medium: 3
  low: 3
  info: 2
checks_run: 3
checks_skipped: 1
---

> **REVISED**: see companion file `20260424-131303-TRIAGE-DEEP.md`. Extended research
> discovered that all 45 `Config error` log entries are stale (dated ≤2026-03-13),
> that `~/secrets/ai-trading-gold/.env` exists at the path `config/settings.py`
> resolves to, and that the system startup on 2026-04-23 22:26 completed healthy.
> The two "critical" items below were historically accurate but have no active impact.
> Retain this file as-is for traceability.

# Autonom Debug Triage — 20260424-131303 (initial — superseded)

**Stack:** python | **Depth:** deep | **Status:** CRITICAL

## Executive Summary

- Critical: 2 | High: 1 | Medium: 2 | Low: 1 | Info: 1
- Checks run: `git-check`, `log-pattern`, `config-check` (+ deep inline scan of pytest artefacts)
- Checks skipped: 1 (`deps-check` — no lockfile detected, only `requirements.txt`)
- Most pressing: `.env` fehlt komplett am Repo-Root → 45 Startup-Fehler + 4 asyncio-Exceptions + heutiger Abbruch. Ein Root-Cause erklärt die Mehrheit der Log-Fehler.

## Top-3 Actions (copy-paste)

1. `/gsd-debug "missing .env at repo root — 45 config errors + asyncio DB-init failures — create .env from config/settings.py requirements or set SQLITE_FALLBACK=true"`
2. `/gsd-debug "broker auth Server-disconnected — capital.com demo endpoint, retry/credential issue, last failure 2026-04-23 22:19"`
3. `/gsd-debug "asyncio task exception never retrieved in init_db — wrap await init_db() in try/except SystemExit or handle in main()"`

## Findings (sorted by severity)

### [CRITICAL] Missing `.env` at repo root — 45 startup failures + root cause for asyncio DB-init errors
- File: `.env`
- Evidence: `config/settings.py:256` reads `SQLITE_FALLBACK` / `POSTGRES_PASSWORD` etc.; `logs/trading.log` shows 45 occurrences of `Config error: CAPITAL_API_KEY / CAPITAL_EMAIL / CAPITAL_PASSWORD / POSTGRES_PASSWORD is required`. Neither `.env` nor `.env.example` exist at repo root. `mirofish/.env.example` exists under subfolder.
- Sources: config-check, log-pattern
- Correlation: **C-custom** — `.env` missing ⇒ `POSTGRES_PASSWORD` unset ⇒ `engine.begin()` in `database/connection.py:205` fails ⇒ `init_db()` raises ⇒ `main()` Task-1 exits `SystemExit(1)` while still awaited ⇒ asyncio-Finding below (shares root cause).
- Next: `/gsd-debug "missing .env at repo root — create from config/settings.py requirements or set SQLITE_FALLBACK=true"`

### [CRITICAL] Latest startup aborted — Broker authentication failed (Server disconnected)
- File: `logs/trading.log:6576`
- Evidence: `2026-04-23 22:19:23 CRITICAL Startup health check failed: Broker authentication: Server disconnected` — raises `RuntimeError` at `trading/lifecycle.py:180` via `_health_check`. A retry at 22:26:01 succeeded (`[OK] Broker authenticated (DEMO)`), so transient — but worth a retry-loop.
- Sources: log-pattern
- Next: `/gsd-debug "broker auth server-disconnected — capital.com demo endpoint, add retry loop or widen timeout in market_data/broker_client.py"`

### [HIGH] 4× asyncio "Task exception was never retrieved" — DB init swallows SystemExit
- File: `database/connection.py:205` (via `trading/lifecycle.py:107`)
- Evidence: Tracebacks on 2026-04-09, -10, -14 all share path `main.py:135 → lifecycle.start() → _health_check() → init_db() → engine.begin() → SystemExit(1)` left unretrieved on Task-1. Indicates bare `exit(1)` inside an awaited coroutine.
- Sources: log-pattern
- Correlation: Likely same root cause as CRITICAL #1 — Postgres creds missing, `init_db` exits hard.
- Next: `/gsd-debug "asyncio task exception never retrieved in init_db — wrap await init_db() in try/except SystemExit"`

### [MEDIUM] 188 uncommitted files
- Evidence: `git status --porcelain | wc -l = 188`. Working tree massively dirty — review before any reset/rebase.
- Sources: git-check
- Next: `git status`

### [MEDIUM] Stale pytest artifacts from different machine (user=roder)
- File: `pytest_errors.txt`, `pytest_failures.txt`, `pytest_last_failure.txt`
- Evidence: Headers show `rootdir: C:\Users\roder\goldbot2` + `platform: Python 3.13.12`, dated 2026-03-22. Current machine is `fuhhe` with `.venv` using Python 3.12. Errors reference `ModuleNotFoundError: No module named 'scripts.train_ai'` — that module no longer exists here (only `scripts/train_models.py`), so these failure reports are stale and misleading.
- Sources: log-pattern (deep-inline)
- Next: `rm pytest_errors.txt pytest_failures.txt pytest_last_failure.txt && pytest -q`

### [LOW] Stale `.pyc` files for deleted test modules in `tests/__pycache__`
- File: `tests/__pycache__/`
- Evidence: `test_train_ai_gate|hybrid_source|progress_jsonl|runtime|synthetic_mode*.pyc` present though their source `.py` files are gone. Pytest collection ignores `.pyc`, so no runtime impact — just clutter.
- Sources: log-pattern (deep-inline)
- Next: `find tests -name __pycache__ -type d -exec rm -rf {} +`

### [INFO] 134 unpushed commits
- Evidence: `git log --branches --not --remotes` reports 134 local commits ahead of remote. Last commit ~14h ago.
- Sources: git-check
- Next: `git push` (nach Review der dirty files + Vermeidung, dass `.env`-Secrets gepusht werden)

## Skipped Checks (with reasons)

- **deps-check**: no lockfile detected (only `requirements.txt` — no `poetry.lock` / pinned install). Consider adding `pip-compile` lockfile for reproducible CVE scans.

## Rule Engine Trace

_For debugging why checks were / were not selected._

- R1 no-match: CI=unavailable (no GitHub workflows + no gh remote)
- R2 no-match: env_changes=0 (no `.env*` touched in last 7 days — consistent with file being absent)
- R3 matched: dirty=188 → P2 git-check
- R4 matched: log_errors=1 → P2 log-pattern (deep-inline scan expanded this to 45 config errors + 38 tracebacks)
- R5 no-match: no lockfile present → deps-check skipped
- R6 no-match: no active GSD phase
- R7 matched: unpushed=134 → would add P3 git-check, but already P2
- R8 skipped (other rules matched)
- Deep-mode inline extensions: `pytest_*.txt` provenance check, tests/__pycache__ orphan scan, `.env` existence probe cross-referenced with `config/settings.py` + `trading.log`

## Context Snapshot

```json
{
  "timestamp": "20260424-131303",
  "repo_root": "/c/Users/fuhhe/OneDrive/Desktop/ai/ai/ai trading gold",
  "stacks": "python",
  "git": {
    "dirty_files": 188,
    "last_commit_age_h": "14",
    "unpushed_commits": 134
  },
  "ci_status": "unavailable",
  "env_changes_7d": 0,
  "deps_age_days": "no-lockfile",
  "log_error_count_raw": 1,
  "log_error_count_expanded": {
    "ERROR_lines": 64,
    "Config_error": 45,
    "asyncio_unretrieved": 4,
    "Traceback": 38
  },
  "gsd_active_phase": "",
  "depth": "deep"
}
```
