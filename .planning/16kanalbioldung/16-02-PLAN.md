---
phase: 16-channel-formation
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [ai_engine/features/geometric_features.py, tests/test_geometric_features.py]
autonomous: true
requirements: [CHAN-02]
must_haves:
  truths:
    - "Pivot detection finds significant local maxima and minima (fractals)"
    - "Trendlines connect at least two confirmed pivots of the same polarity"
    - "Trendline projection provides accurate price estimates for the current candle index"
  artifacts:
    - path: "ai_engine/features/geometric_features.py"
      provides: "Pivot trendline implementation"
  key_links:
    - from: "ai_engine/features/geometric_features.py"
      to: "scipy.signal.find_peaks"
      via: "pivot detection engine"
---

<objective>
Implement the Structural Layer of the Channel Formation engine by connecting significant pivots (Fractal Highs/Lows) to form trendlines. This identifies diagonal support and resistance levels.

Purpose: Detect geometric price structures like wedges, flags, and sloping channels.
Output: Extended `GeometricFeatures` class with pivot-based trendline logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/16kanalbioldung/16-RESEARCH.md
@.planning/phases/16-channel-formation/16-01-SUMMARY.md

<interfaces>
From scipy.signal:
```python
def find_peaks(x, height=None, threshold=None, distance=None, prominence=None, width=None):
    # Returns peaks, properties
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pivot Detection and Trendline Fitting</name>
  <files>ai_engine/features/geometric_features.py</files>
  <action>
    Extend `GeometricFeatures` class.
    Implement `find_pivots(self, highs: pd.Series, lows: pd.Series, window: int = 5)`:
    - Use `scipy.signal.find_peaks` or a fractal logic (H[i] > H[i-1]...H[i+1]).
    - Store pivots as a list of (index, price, type) tuples.
    Implement `fit_trendlines(self, pivots)`:
    - Select the last 2 confirmed pivots of the same type (Support or Resistance).
    - Calculate `slope = (p2.price - p1.price) / (p2.index - p1.index)`.
    - Calculate `intercept = p2.price - slope * p2.index`.
    Implement `project_trendline(self, slope, intercept, current_idx)`:
    - Returns `intercept + slope * current_idx`.
    Ensure all logic handles the "Look-ahead" pitfall by using confirmed pivots (i.e., pivots that have $N$ bars to their right).
  </action>
  <verify>
    <automated>python -c "from ai_engine.features.geometric_features import GeometricFeatures; print('find_pivots and fit_trendlines implemented')"</automated>
  </verify>
  <done>Pivot-to-Pivot trendline logic implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit Tests for Pivot Trendlines</name>
  <files>tests/test_geometric_features.py</files>
  <behavior>
    - Test 1: Verify pivot detection finds significant peaks in a sine wave or zigzag.
    - Test 2: Verify trendline slope matches manual calculation for two known points.
    - Test 3: Verify projection value matches expected geometric projection.
    - Test 4: Verify that a pivot is only detected if it has enough confirmation bars (no look-ahead).
  </behavior>
  <action>
    Add tests to `tests/test_geometric_features.py` for the new pivot and trendline methods.
  </action>
  <verify>
    <automated>pytest tests/test_geometric_features.py -k "pivot or trendline"</automated>
  </verify>
  <done>Trendline features verified with unit tests.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Availability | geometric_features.py | mitigate | Limit the lookback for pivot searching to prevent excessive computations on deep history. |
</threat_model>

<verification>
`pytest tests/test_geometric_features.py` passes all tests including the new trendline tests.
</verification>

<success_criteria>
Trendlines are correctly fitted to historical pivots and projected forward to the current price level.
</success_criteria>

<output>
After completion, create `.planning/phases/16-channel-formation/16-02-SUMMARY.md`
</output>
