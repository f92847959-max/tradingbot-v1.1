---
phase: 16-channel-formation
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified: [ai_engine/features/breakout_detector.py, ai_engine/features/feature_engineer.py, tests/test_breakout_integration.py]
autonomous: true
requirements: [CHAN-03, CHAN-04]
must_haves:
  truths:
    - "Breakouts are detected when price closes beyond a geometric boundary (LinReg band or Trendline)"
    - "Breakouts occurring during an SMC Liquidity Sweep are flagged as FAKEOUT"
    - "New geometric and breakout features are correctly available in the FeatureEngineer output"
  artifacts:
    - path: "ai_engine/features/breakout_detector.py"
      provides: "Breakout detection and SMC filtering logic"
    - path: "tests/test_breakout_integration.py"
      provides: "Integration tests for the breakout chain"
  key_links:
    - from: "ai_engine/features/breakout_detector.py"
      to: "ai_engine/features/market_structure_liquidity.py"
      via: "SMC fakeout filter (ms_buy_side_sweep/ms_sell_side_sweep)"
    - from: "ai_engine/features/feature_engineer.py"
      to: "ai_engine/features/geometric_features.py"
      via: "feature calculation"
---

<objective>
Implement the Breakout Detection layer and integrate it with Smart Money Concepts (SMC). This allows the bot to trade high-probability trendline/channel breaks while avoiding common traps like Liquidity Sweeps.

Purpose: Generate high-quality entry/exit signals and features from geometric breaks.
Output: `BreakoutDetector` module and updated feature engineering pipeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/16kanalbioldung/16-RESEARCH.md
@path/ai_engine/features/geometric_features.py
@path/ai_engine/features/market_structure_liquidity.py
@path/ai_engine/features/feature_engineer.py

<interfaces>
From market_structure_liquidity.py:
```python
class MarketStructureLiquidityFeatures:
    def calculate(self, df: pd.DataFrame) -> pd.DataFrame:
    # Returns columns: ms_buy_side_sweep, ms_sell_side_sweep, etc.
```

From feature_engineer.py:
```python
class FeatureEngineer:
    def create_features(self, df: pd.DataFrame, include_specialist: bool = False) -> pd.DataFrame:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BreakoutDetector and SMC Filtering</name>
  <files>ai_engine/features/breakout_detector.py</files>
  <action>
    Create `ai_engine/features/breakout_detector.py`.
    Implement `BreakoutDetector` class with `detect_breakouts(self, df: pd.DataFrame, geo_features: dict, smc_cols: pd.DataFrame)`:
    - `breakout_bullish`: Price Close > LR Upper Band OR Price Close > Resistance Trendline.
    - `breakout_bearish`: Price Close < LR Lower Band OR Price Close < Support Trendline.
    - `breakout_is_fakeout`: True if (breakout_bullish AND ms_buy_side_sweep) OR (breakout_bearish AND ms_sell_side_sweep).
    - Ensure all signals are shift(1) to reflect data available at the open of the next candle.
    Integration: Consume SMC sweep columns directly from the passed DataFrame.
  </action>
  <verify>
    <automated>python -c "from ai_engine.features.breakout_detector import BreakoutDetector; print('BreakoutDetector created')"</automated>
  </verify>
  <done>Breakout detection and SMC filtering logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Features into FeatureEngineer</name>
  <files>ai_engine/features/feature_engineer.py</files>
  <action>
    Update `FeatureEngineer.create_features` (or `calculate_features`) to:
    1. Instantiate `GeometricFeatures` and `BreakoutDetector`.
    2. Calculate geometric features: `geo_lr_slope`, `geo_lr_r2`, `geo_tl_support`, `geo_tl_resistance`.
    3. Calculate breakout features using the geometric context and existing SMC columns.
    4. Append all new features to the final DataFrame.
    5. Ensure all new features are registered in `get_feature_names`.
  </action>
  <verify>
    <automated>python -c "from ai_engine.features.feature_engineer import FeatureEngineer; print('geo_lr_slope' in FeatureEngineer().get_feature_names())"</automated>
  </verify>
  <done>Feature engineering pipeline updated with geometric features.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration Tests for Breakout Chain</name>
  <files>tests/test_breakout_integration.py</files>
  <behavior>
    - Test 1: Price crossing a flat trendline with no sweep triggers a VALID bullish breakout.
    - Test 2: Price crossing a trendline during an `ms_buy_side_sweep=True` triggers `breakout_is_fakeout=True`.
    - Test 3: `FeatureEngineer` successfully produces a DataFrame containing all `geo_` and `breakout_` columns without NaNs in the tail.
  </behavior>
  <action>
    Create `tests/test_breakout_integration.py`.
    Write integration tests that mock the price action and SMC columns to verify breakout/fakeout logic.
  </action>
  <verify>
    <automated>pytest tests/test_breakout_integration.py</automated>
  </verify>
  <done>Full geometric feature chain verified with integration tests.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Integrity | breakout_detector.py | mitigate | Strictly use `shift(1)` for all signal generation to avoid look-ahead bias (trading on the close of the candle that broke the line before it closes). |
</threat_model>

<verification>
`pytest tests/test_breakout_integration.py` and `pytest tests/test_geometric_features.py` both pass.
</verification>

<success_criteria>
The system correctly identifies channel/trendline breakouts, filters them using SMC logic, and exposes them as features for ML models.
</success_criteria>

<output>
After completion, create `.planning/phases/16-channel-formation/16-03-SUMMARY.md`
</output>
